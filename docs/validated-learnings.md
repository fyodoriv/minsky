# Validated learnings

Append-only log of experiments whose post-merge replay (rule #9 weekly–monthly layer)
found the predicted gain held at +7d / +30d. Maintained by `scripts/replay-experiment.mjs`
and the `experiment-tracker` GitHub Actions workflow. Rows are never deleted; superseded
learnings get a follow-up row, not a rewrite.

- `rule-9-iron-rule` — validated at +0d (2026-05-03, ref f87aeac, PR #9): constitutional rule #9 (pre-registered hypothesis-driven development) is iron — every change declares Hypothesis / Success / Pivot / Measurement / Anchor *before* code. Subsequent rule-#9 substrate (`@minsky/experiment-record`, `ci-experiment-runner-v0`, this tracker) all build on this baseline. The first validated learning is the discipline itself; future replays add quantitative entries against measurements declared in `EXPERIMENT.yaml`.

- `operator-vision-2026-05-22-canonical` — recorded 2026-05-22 from the operator's verbal directive *"The vision I have for minsky is: it's a 24/7 self-improving code factory that relies on OpenHands as an orchestrator. It comes up with tasks and finds the best way to solve them based on science."* This is a direct restatement of the already-canonical Path C reshape (`docs/plans/2026-05-22-path-c-openhands-reshape.md`) in operator-natural-language, mapped to four pillars: **(1) 24/7** (✅ substrate ships, 🟡 stability target M1.1 blocked on `spawn-failed-exit-minus-one-silent-empty-stderr`); **(2) self-improving code factory** (✅ MAPE-K L1 + dogfood loop + observer heals ship, 🟡 L2 closed-loop A/B is spec-only); **(3) relies on OpenHands as orchestrator** (🔴 0% runtime integration today — substrate ready, gated on OpenHands' Agent Canvas Initiative CLI release `2026-06-01`; M1.14 filed with full status); **(4) comes up with tasks** (🟡 agents file tasks *inside* iterations via rule #17 scout discipline + `corpus-discover-quarterly` quarterly autonomous filings, but daemon doesn't author tasks *between* ticks — gap filed as `autonomous-task-authoring-between-ticks`); **(5) finds the best way based on science** (✅ rule #9 pre-registered HDD enforced as CI lint on every PR — strongest pillar, the moat). README's "Where we are vs. the vision" section + vision.md § "Operator vision (2026-05-22 directive, verbatim)" + MILESTONES.md M1.14 row hold the honest scorecard with file-path evidence for each pillar. The operator's verbatim phrasing now drives the README tagline ("A 24/7 self-improving code factory"). The two gap tasks filed in this PR — `autonomous-task-authoring-between-ticks` (pillar 4) and `throughput-at-scale-benchmark` (the "code factory" claim in pillar 2) — are the ratchets that close the 🟡 cells. M1.14 is the ratchet that closes the 🔴 OpenHands cell on `2026-06-01`.
