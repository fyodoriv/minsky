# Vision

> A society of minds. Building yours.

## What Minsky is

Minsky is an **integration distribution**. It connects existing tools into a viable cybernetic system that produces software 24/7 on a Claude Code Max subscription, and stays alive — on-budget, on-mission, getting better — indefinitely.

Minsky is **not** a framework. It does not contain a multi-agent runtime. It does not own the task queue, the personas, the loop driver, the dashboard, or the mobile surface. Each of those is provided by an existing tool that someone else maintains. Minsky's job is to **choose them, configure them, wire them together through versioned interfaces, and add the small layers nobody else is building**.

Minsky is also explicitly **not** a vehicle for one-off quick fixes. Its scope is the long-tail viability of a cybernetic system that runs for years on a Claude Code subscription. Every change in this repo is a pre-registered experiment under rule #9 (hypothesis-driven development) — including bugfixes, including refactors, including docs. If a candidate change cannot state its expected metric movement in advance, either the metric source is missing (in which case ship a preparation PR first) or the change does not justify the discipline overhead and belongs elsewhere. See rule #9 below and `## What Minsky is not` for the explicit out-of-scope statement.

## The reframe: a distribution, not a framework

Linux distributions don't write the kernel. They curate, configure, integrate. Ubuntu's value isn't the kernel — it's that everything Just Works together.

Minsky is the Ubuntu of autonomous Claude Code: a curated, pre-configured stack with strong opinions about how the pieces fit, plus the small custom layers (MAPE-K loop, specification monitor, budget guard, handoff spec) that make the whole thing self-improving and viable.

The success criterion changes accordingly. Not "did we ship features faster" — that's a byproduct. The criterion is: **is the integration durable?** Every dependency behind an interface. Every novel layer extractable as its own OSS. Every choice defended by reference to a named, decades-tested pattern.

## The constitution

These principles are non-negotiable. The MAPE-K loop (the *autonomic manager*, in IBM autonomic-computing terms) reviews behavior against them. Specification drift triggers correction.

### 1. Don't reinvent the wheel

Strict preference order:

1. Use someone else's tool.
2. If you must build, extract reusable parts as separate OSS from day one — designed for others to use, not just us.
3. Only keep proprietary what's genuinely unique.

Apply continuously, not only at project start. Quarterly: scan whether each novel layer is now solvable upstream — and replace yourself.

### 2. Every dependency behind an interface

No tool we depend on is hardcoded. Each is accessed through an adapter — a Minsky module that defines an interface, then implements it against the currently chosen tool. Swapping OMC for a future better orchestrator is a one-file change. See `ARCHITECTURE.md` for the adapter pattern applied to all dependencies.

This is what makes principle 1 *tractable* over a decade. Without interfaces, "use someone else's tool" calcifies into vendor lock-in. With interfaces, the stack is a Ship of Theseus — every plank replaceable.

### 3. Test-first, metric-first, doc-first

Every feature begins with a failing test (red). Then minimal code to pass (green). Then refactor.

Before any code:

- The metric that will prove it works, with a numeric threshold
- The doc explaining why it exists
- The user-story file declaring what's being built and how we'll know it's done

No exceptions. Apply at every level: code, persona behavior, orchestration logic, even the autonomic manager's own decisions.

### 4. Everything measurable, everything visible

The system has no hidden state. Every component reports to OpenTelemetry. Every metric appears on a dashboard. The dashboard is reachable from your wrist.

If you can't see it, it doesn't exist. If you have to dig for it, you won't.

### 5. Theoretical grounding — every term has a CS anchor

Each architectural choice references a named, decades-tested pattern. We do not invent terminology when established literature has a word for it. We do not invent architecture when half a century of research has named the right one.

Concretely: **every Minsky-coined word has an entry in the [Glossary](#glossary--every-term-has-a-cs-anchor) below pointing to its computer-science / control-theory / formal-methods source.** If a metaphor (e.g., "the constitution") is useful for narrative, it must be introduced *as a label for* a precisely-named pattern (e.g., a behavioral specification monitored at runtime), and the precise term is what the code, package names, and tests use. New terminology requires either (a) an existing literature citation or (b) a justification for why no published term fits — recorded in `research.md`.

This is also a hedge: when something breaks at 3am, "this is supervision-tree pattern, restart strategy is one-for-one" is a debuggable answer. "This is how I happened to wire it" is not.

### 6. Stay alive

The system is designed to operate for years, not days. It throttles itself before hitting limits, recovers from crashes without human help, detects its own specification drift, and improves itself measurably over time.

"Stays useful indefinitely under constant pressure" is the goal. "Ships features fast" is a byproduct.

### 7. Chaos engineering — trust nothing unverified

Trust no component whose failure probability is not provably ≤1e-12. Until verified, every dependency is assumed hostile: it can crash, hang, lie about success, return malformed data, or slow to a crawl mid-call. Rule #6 ("stay alive") sets the goal; this rule makes it falsifiable. The system stays alive only because we enumerate the ways each piece can fail, exercise each failure in tests that run on every CI, and verify the supervisor handles them as designed.

Every novel package and every user-story file enumerates four things:

1. **Failure modes** — concrete things that can break, organized by *fault axis*: process death (`kill -9`), network (`tc qdisc netem` for latency / loss; `iptables -A OUTPUT … -j DROP` for partitions), clock (`libfaketime` for skew and jump), disk-full, OOM, dependency upstream-error, dependency upstream-malformed-response. The Netflix Simian Army catalog (Basiri et al., *Principles of Chaos Engineering*, IEEE Software 2016) is the starting list; extend per the package's surface.
2. **Expected behavior** for each failure — exactly one of:
   - `loud-crash-supervisor-restart` — let-it-crash (Armstrong, *Programming Erlang*, 2007); supervisor restarts the process per its policy. Never silent recovery.
   - `circuit-break-and-notify` — open the circuit (Nygard, *Release It!*, 2007); fire a notification at level ≥ user-set threshold; close on probe success.
   - `graceful-degrade` — switch to a reduced-capability mode (e.g., Haiku instead of Sonnet under budget pressure) while emitting an OTEL span tagged `degraded=true`.
3. **Deterministic chaos test** — a CI test that reproduces the failure exactly (same fault axis, same timing) and asserts the expected behavior. A *steady-state hypothesis* (Basiri et al. 2016) is declared per system: the metric whose value defines "healthy" *before* the fault is injected; the test passes only if the metric is restored within the package's recovery SLO after the fault is removed.
4. **Blast radius** — explicit upper bound on what one failure can damage (single tick / single user-story / single dependency / whole system) and the *operator escape hatch* — the one-command kill switch the user can invoke if a chaos test escapes its bounds.

Weekly production fault injection (Netflix Tech Blog, "Chaos Monkey released into the wild", 2012) on a low-stakes day: the supervisor randomly picks one declared failure mode and triggers it. If the system fails to recover per spec, the developer gets a Watch-level notification and the failure becomes the week's constraint per Goldratt TOC.

Two underlying patterns make recovery deterministic and are presumed by the rule: actor isolation (Hewitt, Bishop, Steiger, "A Universal Modular ACTOR Formalism for Artificial Intelligence", IJCAI 1973) — actors share no state, so one's death corrupts no one else; and log-replay (Kreps, "The Log: What every software engineer should know about real-time data's unifying abstraction", LinkedIn Engineering 2013) — durable input lets any actor recompute its state. Without both, "recovery" is wishful thinking.

**Anti-pattern**: silent retry-with-backoff loops that suppress the failure signal. They look like graceful handling and are in fact the opposite — they paper over a fault that should crash loudly so the supervisor can act and the developer can see. *Suppression of failure is itself a constitutional violation*; `scripts/check-rule-6-let-it-crash.mjs` is the deterministic CI gate that catches the structural shape (swallowing-catch / nested-try) per rule #10. The advisory `novel/spec-monitor/` Skill may surface judgement-heavy suppression patterns the AST linter cannot read, but its verdict never gates CI (row 35).

Sources: Basiri et al., "Principles of Chaos Engineering", IEEE Software 2016; Beyer et al., *Site Reliability Engineering*, Ch. 17 "Testing for Reliability", 2016; Armstrong, *Programming Erlang*, 2007; Nygard, *Release It!*, 2007; Hewitt, Bishop, Steiger, IJCAI 1973; Kreps, "The Log", 2013; Netflix Tech Blog, "Chaos Monkey released into the wild", 2012.

### 8. Pattern conformance — every artifact maps to a published pattern; deviations are declared

Rule #5 said *every Minsky-coined word has a CS anchor*. This rule generalises that commitment to *every artifact*: every file, every package, every interface, every architectural decision, every process step traces to a named, published pattern from computer science, control theory, cybernetics, formal methods, or systems engineering. The repo is 100% science-anchored, explicitly. There is no "we just decided to do it this way."

Two operational consequences:

1. **Pattern conformance index** (table immediately below) — a single living index lists each Minsky artifact with its (a) governing pattern, (b) literature source, (c) conformance level: `full` / `partial` / `deviation`. Every PR that adds a new file, package, interface, or architectural decision adds (or amends) a row in the same commit. Missing or stale rows are a CI failure (the linter scans for new top-level files / packages without index entries).
2. **Deviations are explicit, not silent.** Where Minsky's implementation deviates from the published pattern (timing constraints, single-machine scope, language idiom, performance tradeoff), the deviation is *declared* — in the index row, in the file's docstring or top-level comment, and in `research.md` if it represents a research-level departure. The declaration includes: which property of the published pattern is violated, why the deviation is acceptable for Minsky's scope, and what would have to change to restore full conformance. Silent deviation is itself a constitutional violation.

Code conventions that follow from this rule:

- **Identifiers match the pattern when the match is total.** When a class, function, type, or module *is* an instance of a published pattern, its identifier matches the pattern's canonical name (e.g., `aggregateStatus` for "worst-status aggregation"; future `MapeKLoop`, `SupervisionTree`, `CircuitBreaker`). When the match is partial, the identifier names what we built and a doc comment cites the pattern with the deviation declared.
- **Top-of-file pattern reference.** Every TS source file (and every shell script of substance) opens with a comment block stating: pattern, source, conformance, and (if not full) deviation rationale. JSDoc on public interfaces names the pattern.
- **Persona prompts** (when added under `.claude/agents/`) include a pattern reference for the role they play (e.g., `architect.md` → BDI rational agent; `qa-tester.md` → property-based testing; `verifier.md` → Hoare-style pre/post conditions).

Why this rule exists, separately from rule #5: at 3am, "this module implements X pattern with Y deviation because Z" is a debuggable answer. "It's named after a metaphor and the code does its own thing" is not. Rule #5 ensures the *vocabulary* points to literature; rule #8 ensures the *behavior* does.

Sources: Brooks, *No Silver Bullet*, 1986 (essential complexity comes from the problem; named patterns reduce accidental complexity); Lampson, "Hints for Computer System Design", SOSP 1983 ("use a good idea more than once"); Gabriel, *Patterns of Software*, 1996 (pattern languages as architectural commitment); the Gang of Four, *Design Patterns*, 1994 (the foundational catalogue).

### 9. Pre-registered hypothesis-driven development — *iron rule*; no exceptions, including bugfixes

Rule #3 said *every feature begins with a failing test, a metric with a numeric threshold, and the doc explaining why*. This rule formalises rule #3 into the discipline of **pre-registered hypothesis-driven development**: every change — every PR, every feature, every refactor, **every bugfix** — is a falsifiable experiment whose hypothesis, metric, success threshold, pivot threshold, and measurement command are *committed in advance of the change* (in the task block, the PR description, or a preparation PR). The repo develops by satisfying clearly measurable goals; if the metric doesn't move, the approach is abandoned, not the metric. Pre-registration (committing the prediction *before* observing the result) is what makes the experiment falsifiable; without it, the rule degenerates into post-hoc rationalisation. This discipline imports pre-registration from clinical-trials and reproducible-science methodology (Munafò et al. 2017) into day-to-day software change.

This rule is **iron**: it has no exemptions for "small fixes", "obvious changes", "just a refactor", or "blocked-by-business reasons". A bugfix's hypothesis is "the recurrence rate of this fault (or a stability metric to which it contributes) drops from X to Y after this change". If that statement cannot be made — including its threshold and its measurement command — the root cause has not been identified and the fix is not ready to ship. The only blanket exemption is a *trivially-correct* change whose existing CI gate IS the metric (typo fix in prose, formatting churn, no-op move covered by passing tests); even then the exemption is documented in the commit message rather than assumed.

Every change declares five things:

1. **Hypothesis.** The change improves which behaviour, by how much, why we believe so. Anchored in the Goal-Question-Metric paradigm (Basili, Caldiera, Rombach 1994): state the goal, derive the question that operationalises it, derive the metric that answers the question. Tasks that can't formulate a clear hypothesis aren't ready to start. Bugfix variant: the goal is the *stability metric* the bug threatened (error rate, MTTR, p99 latency, crash frequency, drift-rule-violation count); the question is "did this fix lower it"; the metric is the same observable, post-fix.
2. **Success threshold.** Numeric value (or rubric grade) at or above which the change is kept. Tracks the success-criterion column in `vision.md` § "Success criteria".
3. **Pivot threshold.** Value below which the *approach is abandoned* — not just the change reverted. The Lean Startup discipline (Ries 2011): build–measure–learn, validated learning, *pivot-or-persevere*. Without a pre-declared pivot, sunk-cost fallacy keeps a wrong approach alive long after the data has spoken.
4. **Measurement method (automated).** Exact shell command, OTEL query, or CI script that produces the metric. Reproducible by an outside observer with no manual steps. No English instructions ("count the lines"); literal syntax. If the prerequisite system isn't built yet, name the command and tag it `<TBD-AFTER: <task-id>>` linking to the task that lands the prerequisite.
5. **Literature anchor.** A paper or framework that justifies the metric's choice and the threshold's defensibility. Per rule #5 / rule #8.

The same five fields apply to the system-level table at `vision.md` § "Success criteria" — every metric carries them.

**Preparation-PR pattern.** When a candidate change targets a behaviour that is *not yet measurable* (no OTEL counter, no test harness, no log line that can be grepped), the discipline is to *first* ship a **preparation PR** that lands the instrumentation, *then* open the change PR against the now-measurable baseline. This is not bureaucratic overhead — it is the only way the change PR can carry a real before/after number. Preparation PRs are first-class work in `TASKS.md` and are explicitly endorsed (not deferred under "we'll instrument later"). When the metric source is missing and the change is non-trivial, propose the preparation PR as the next task instead of guessing the effect size.

Why a separate rule from #3: rule #3 says *do these things first*. Rule #9 says *make them executable, falsifiable, and pre-registered*. Without #9, "metric-first" degenerates into vague KPIs that nobody actually queries; without pre-registration, even measured changes degenerate into fishing expeditions.

**Anti-pattern: vanity metrics.** Counts that always go up (lines of code, commits made, hours spent, tasks "in flight") are *forbidden* as success metrics — they incentivise activity, not outcomes. Ries 2011 names this trap; Doerr 2018 (OKRs) reinforces: *measure outcomes, not activities*. `@minsky/experiment-record`'s parser (`VANITY_PHRASES` in `novel/experiment-record/src/parse.ts`) rejects any `EXPERIMENT.yaml` field carrying a forbidden phrase at parse time — a deterministic CI gate per rule #10, not a Skill verdict.

**Anti-pattern: post-hoc metrics.** Picking the metric *after* seeing the change's effect is forbidden. The metric is part of the contract the change is being judged against; choosing it after the fact lets the change always "succeed" by the metric most flattering to it. Pre-registration (Munafò et al. 2017) is the correction: declare in the task block / PR description, *before* code is written, what observable will move and by how much.

**Pre-registration without execution is half a rule.** Pre-registered hypotheses that are never re-measured collapse into wish-lists; pre-registered hypotheses that *are* re-measured but only by hand collapse into "we'll check eventually" and silently rot. Rule #9 therefore commits the repo to a three-timescale **automation layer** that closes the loop from declaration to verdict. Each timescale ships as its own task; together they form the operational substrate of the rule.

1. **Daily / per-PR layer — the experiment runner.**
   - Every non-trivial PR ships an `EXPERIMENT.yaml` (or equivalent structured frontmatter) parsed from the PR description, carrying the five fields. CI fails fast when the file is missing or malformed.
   - The runner executes `Measurement` against the merge-base ref (baseline), and again against the post-merge `main` ref (treatment), recording both numbers tagged with the experiment-id into the OTEL backend (or the lightweight `experiment-store` until the OTEL backend lands — see `otel-lite-backend`).
   - Verdict at this layer is provisional ("first observation"); the structural test it enforces is "the measurement command is runnable and produces a number". This is the executability gate — the rest of the layer assumes commands can be run.
   - Failure modes: missing YAML → block merge; non-runnable command → block merge; baseline ≈ treatment within instrument noise → flag as "no observable effect, requires longer window or larger sample" and route to the weekly layer.
   - Implementation: `ci-experiment-runner-v0` (in `TASKS.md`).

2. **Weekly / monthly layer — the sustained-gain check.**
   - A scheduled job re-runs each merged experiment's `Measurement` at +7 / +30 days (configurable per-experiment), compares against the declared `Success` and `Pivot` thresholds, and emits a verdict per experiment: `validated` / `regressed` / `inconclusive`.
   - The 7-day floor is the sustained-gain discipline already cited in `mape-k-loop-v0` (Ries 2011 build–measure–learn, validated learning); the 30-day window catches mid-term regressions that disappear from short A/B windows (Kohavi/Tang/Xu 2020 ch. 5–7 "trustworthy" rather than "fast").
   - `regressed` triggers an automated revert/pivot task in `TASKS.md` linking the experiment-id; `inconclusive` lengthens the window or asks for a re-design.
   - Implementation: `experiment-tracker-v0` (in `TASKS.md`).

3. **Quarterly layer — cross-experiment calibration.**
   - The store accumulates `{predicted Δ, observed Δ at +7, observed Δ at +30, observed Δ at +90}` tuples. A periodic analysis (folded into `mape-k-loop-v0`'s Knowledge phase and into the existing `review-q3-2026` cadence) tests *meta*-questions: are predictions tracking observations? Which categories of hypothesis are systematically over-optimistic (e.g., "this refactor reduces tokens-per-story" claims that never show up)? Which thresholds are calibrated, which are theatre?
   - The quarterly verdict feeds back into rule #9 itself: if a class of hypothesis cannot be calibrated after multiple iterations, the rule's pivot-threshold contract is wrong for that class and the rule must adapt (e.g., add a research-task exemption).
   - Implementation: scope expansion of `mape-k-loop-v0` (Knowledge phase) and `review-q3-2026`.

The three timescales together form a MAPE-K loop over the rule itself (Kephart & Chess 2003 applied recursively): the daily layer is Monitor, the weekly layer is Analyze, the quarterly layer is Knowledge + Plan. Without all three, rule #9 degrades to spec without an interpreter.

Sources: Basili, Caldiera, Rombach, "The Goal-Question-Metric Approach", *Encyclopedia of Software Engineering* 1994; Ries, *The Lean Startup*, 2011 (build-measure-learn; pivot-or-persevere); Kohavi, Tang, Xu, *Trustworthy Online Controlled Experiments*, Cambridge University Press 2020 (statistical rigour in A/B testing); Munafò et al., "A Manifesto for Reproducible Science", *Nature Human Behaviour* 1, 0021, 2017 (pre-registration; the falsifiability discipline imported from open science); Fagerholm, Sanchez Guinea, Mäenpää, Münch, "Building Blocks for Continuous Experimentation", *RCoSE* 2014 (continuous experimentation in software); Kitchenham, Dybå, Jørgensen, "Evidence-Based Software Engineering", *ICSE* 2004; Forsgren, Humble, Kim, *Accelerate*, 2018 (DORA's four-key-metrics for software-delivery performance — deployment frequency, lead time, MTTR, change-fail rate); Manzi, *Uncontrolled*, 2012 (causal inference outside the lab); Doerr, *Measure What Matters*, 2018 (OKR discipline; outcomes not activities).

### 10. Deterministic enforcement — every rule is a CI lint, not a hope

Rules #1–9 are the *contract*. Rule #10 is the *enforcement model*: every constitutional rule is enforced by a deterministic CI check, not by an LLM-judgement Skill, not by a human reviewer's vigilance, not by "the agent will remember". Whatever cannot be deterministically checked is *not* a constitutional rule — it is a heuristic, and it must be marked as such or rephrased until it is mechanisable. This is a tightening of the meta-rule that makes #1–9 load-bearing rather than aspirational.

The discipline:

1. **Every rule maps to one or more CI lint scripts** (under `scripts/check-rule-<n>-*.mjs` or equivalent), each runnable locally (`pnpm run check:rule-<n>`) and wired into `.github/workflows/ci.yml` as a *required* status check. The lint exits non-zero on violation; zero otherwise. No human triage, no LLM in the loop.
2. **LLM-driven checks are advisory only.** A Claude Skill (e.g., `claude-spec-monitor`) can *augment* the deterministic linters with judgement-heavy questions ("does this hypothesis pass the smell test?"), but its verdict is never load-bearing. If a Skill surfaces a violation that the deterministic linter doesn't catch, the response is to *write the deterministic linter*, not to trust the Skill's word. Skills are useful for *discovering* rule gaps; they are not useful for *enforcing* rules. (Determinism in this rule means: same input ⇒ same output, with no model call in the chain. A `grep` is deterministic; a Claude prompt is not.)
3. **Rules that resist mechanisation are reframed.** When a rule is genuinely impossible to deterministically enforce (e.g., "is this hypothesis a *good* hypothesis?"), it is split into a deterministic substrate (presence and shape of the hypothesis block) plus an explicit human-judgement layer (reviewer signs off; no LLM substitute). The judgement layer is documented as such — never quietly delegated to a Skill.
4. **Rule #9's automation layer is the template.** The three-timescale enforcement architecture (per-PR runner / weekly tracker / quarterly calibration) is the worked example: the contract is declared; the CI runs the measurement; the cron checks the threshold; the verdict is mechanical. Every other rule should aspire to the same shape.
5. **The ratchet rule.** When a deterministic linter is added for a rule, the matching Skill-based or human-vigilance enforcement is *removed* in the same PR — never run both, never let the Skill become a fallback. Two enforcement mechanisms competing produces ambiguity about which is authoritative; the deterministic one always wins.

This rule is also iron: no constitutional rule is permitted to ship without its deterministic check, and rules that lack one today are tracked in `TASKS.md` under `ci-rule-<n>-*` tasks until they ship.

Why a separate rule and not a clause inside others: because the *enforcement model* is itself a constitutional commitment. Without rule #10, the project drifts back toward "we'll review carefully" — which is the failure mode the entire constitution exists to prevent.

Sources: Lampson, "Hints for Computer System Design", *SOSP* 1983 (move the constraint to the cheapest possible point — CI is cheaper than human review); Beck, *Extreme Programming Explained*, 1999 (continuous integration as the constraint enforcer); Hunt & Thomas, *The Pragmatic Programmer*, 1999, Tip 32 ("crash early"); Brooks, *No Silver Bullet*, 1986 (accidental complexity comes from the absence of mechanisation); Forsgren, Humble, Kim, *Accelerate*, 2018 (deployment-pipeline determinism is a DORA prerequisite); Munafò et al. 2017 (the open-science argument that pre-registration *plus mechanical verification* is what changes outcomes — pre-registration alone does not).

### 11. No flaky metric is a load-bearing claim — iron rule

A metric is *flaky* if its output varies under fixed (input, code, environment-pin). Sources of flakiness include shared-CPU timing on hosted runners (lighthouse-mobile is the canonical example), network-dependent operations against rate-limited APIs, concurrency races between parallel jobs, state pollution from prior runs, wall-clock arithmetic at boundaries (midnight, DST, leap seconds), and order-dependent test pollution. A flaky metric *cannot* gate merges, drive pivots, or enter `validated-learnings.md` — anything else is theatre that erodes the entire deterministic-enforcement substrate of rule #10.

When a flake is detected — defined mechanically as ≥1 false-positive per 10 runs sustained over 30 days, OR variance > the metric's own pivot margin across same-SHA reruns, OR a single observed failed-then-passed-on-rerun-without-code-change — the response is mandatory and structural:

1. **Stop the world for that metric.** No PR merges through the flaky gate. The detection itself is the trigger; it is not a "we'll deal with it next sprint" signal.
2. **Fix the metric, not the change it gated.** The metric is the bug. Tighten the input pin, swap to a deterministic substrate, move to a pinned-CPU runner, fix the test pollution — whichever applies to the source of variance. Pivoting the change the metric gated, *while leaving the metric flaky*, is forbidden — that path silently legitimises every future flake.
3. **Pre-register the fix as an experiment** in `experiments/<id>.yaml` per rule #9. The hypothesis carries the form "after fix X, the metric varies <Y across N consecutive same-SHA reruns" with a numeric pivot threshold for variance and a runnable measurement command (typically `bash scripts/measure-flake-rate.sh <metric>`). The fix may ride in a separate PR or in whatever PR is in flight when the flake was detected — the rule is about *what* must happen, not *which* PR it ships in.
4. **Land the proof.** N consecutive same-SHA reruns at the pinned environment showing variance below the threshold. That is the validation; until that record exists in `experiment-store/<id>.jsonl`, the metric stays downgraded to *informational* (recorded but non-blocking). Restoring as a blocking gate requires the proof, not a hand-wave.
5. **Auto-detection ratchet.** A deterministic CI lint (`scripts/check-rule-11-no-flaky-gates.mjs`, tracked in `TASKS.md`) reads the last 30 days of CI runs, computes per-job false-positive rate, and surfaces any job whose rate is ≥10 %. Each detection auto-files a P0 task block + emits a patch downgrading the gate to informational; the operator merges the patch. No silent flake tolerance ever — rule #10 demands deterministic enforcement, and this rule is what keeps rule #10 honest about *which* metrics are deterministic enough to enforce *anything*.

This rule is iron — no exemption for "well-known flakes", "cosmetic gates", "we'll fix it after the release". A flaky gate is worse than no gate, because it teaches the team that red checks are noise to be re-run away. The auto-detection ratchet exists precisely so the rule cannot drift back into "we tolerate it for now" by silent attrition.

The first inaugural case under rule #11 is `lighthouse-mobile` on GitHub-hosted runners — observed failed-then-passed-on-rerun on identical SHA across multiple PRs (2026-05 — PR #66, PR #73, PR #123). The fix is the move to a pinned-CPU self-hosted runner (already filed at `lighthouse-self-hosted-runner-pivot` P3 in TASKS.md, now upgraded to follow this rule's discipline). Until the self-hosted runner ships *with the rule-#11 proof*, lighthouse-mobile runs as informational-only: the score is recorded for trend-tracking, the workflow does not block merges. Restoring as a gate requires 10 consecutive same-SHA reruns showing variance ≤0.03 on the self-hosted runner — pre-registered in `experiments/lighthouse-mobile-flake-fix.yaml` once the experiments directory ships per `experiments-directory-migration`.

Sources: Memon, A., et al., "Taming Google-Scale Continuous Testing", *ICSE-SEIP* 2017 (the "DETECTOR" system at Google — flake-rate measurement is itself a CI primitive); Forsgren, Humble, Kim, *Accelerate*, 2018, Ch. 3 (DORA test-reliability — a CI gate that doesn't run reliably teaches the team to ignore failure, which is the worst possible signal); Beyer, B., Jones, C., Petoff, J., Murphy, N. R. (eds.), *Site Reliability Engineering*, O'Reilly, 2016, Ch. 17 (post-mortem culture — the metric is the bug, not the change); Beck, K., *Extreme Programming Explained*, 1999, Ch. 17 (CI as the constraint enforcer — only deterministic checks are constraints; flaky checks are noise); Munafò et al., *Nature Human Behaviour* 1, 0021, 2017 (rule #9's pre-registration anchor — flaky-fix-pre-registration is the rule-#9 contract applied to the metric itself rather than a change the metric gates); rule #10 (the deterministic-enforcement substrate — rule #11 is what keeps rule #10 from being an empty assertion).

## Pattern conformance index

Operationalises rule #8. Each row maps a Minsky artifact (file path, package, interface, architectural decision, process step) to its governing pattern, the published source, and its conformance level. **Every PR that adds a new top-level artifact adds a row in the same commit** (a CI lint will eventually enforce this; tracked in TASKS.md).

Conformance levels:

- **full** — implementation matches the pattern as published in all properties relevant to our scope.
- **partial** — matches the structure / intent; one or more orthogonal properties differ. The differing property is named.
- **deviation** — substantive departure from the published pattern. Rationale is given in the same row plus, where load-bearing, in `research.md`.

| # | Artifact | Pattern | Source | Conformance | Notes / declared deviation |
|---|---|---|---|---|---|
| 1 | `vision.md` (this file) | Behavioral specification | Lamport, "Specifying Concurrent Program Modules", *TOPLAS* 1983 | full | Spec is the source of truth; runtime is monitored against it by the deterministic linters at rows 32–37 (load-bearing per rule #10), with the advisory-only `novel/spec-monitor/` Skill (row 35) covering the residual judgement share. |
| 2 | `ARCHITECTURE.md` § layered model | Viable System Model (VSM) | Beer, *Brain of the Firm*, 1972 | full | Five-level hierarchy (Identity / Intelligence / Control / Coordination / Operations) plus a cross-cutting Nervous System; layers map 1:1. |
| 3 | `novel/adapters/` (the adapter pattern itself) | Adapter (structural) + Strategy (behavioral) | Gamma, Helm, Johnson, Vlissides, *Design Patterns*, 1994 | full | Each adapter file pair = interface + Strategy implementation. Switching implementations is a one-line config change. |
| 4 | `ARCHITECTURE.md` § "Process supervision tree" | OTP supervision behaviour (one-for-one, restart-with-backoff) | Armstrong, *Programming Erlang*, 2007 | partial | Restart strategies match. Supervisor primitive is systemd / launchd (POSIX), not BEAM. **Deviation:** Erlang spawns processes in microseconds; systemd respawn is ~100ms. **Why acceptable:** tick cadence is minutes-to-hours, so 100ms respawn is invisible. **What would restore full conformance:** a BEAM-based supervisor — out of scope for solo-dev tier. |
| 5 | `claude-mape-k-loop` (planned) | MAPE-K reference architecture for autonomic computing | Kephart & Chess, "The Vision of Autonomic Computing", *IEEE Computer* 2003 | full (planned) | Monitor → Analyze → Plan → Execute over a Knowledge base; one OTEL span per phase. Plug for `PromptOptimizer` adapter (DSPy) is the Execute primitive. |
| 6 | Per-tick decision cycle inside personas | OODA loop | Boyd, "Discourse on Winning and Losing", 1976 | full | Tactical inner loop; distinct from the cross-tick MAPE-K outer loop (rule #4 of MAPE-K is the outer Execute, not OODA's Act). |
| 7 | Per-task structure | PDCA (Plan-Do-Check-Act) | Shewhart 1939; Deming 1986 | full | Each task has plan → implement → verify → ship phases; the `/next-task` skill enforces them. |
| 8 | `tasks.md` task queue (single file + git) | Blackboard architecture | Hayes-Roth, "A Blackboard Architecture for Control", *Artif. Intell.* 1985 | partial | Specialists write partial solutions to a shared file; controller (the picker rules in `/next-task`) decides who acts next. **Deviation:** no centralized arbiter process — git's optimistic concurrency is the arbiter. **Why acceptable:** solo-dev workflow has at most a handful of agents; merge conflicts are rare and recoverable. **What would restore full conformance:** a long-running blackboard controller process — over-engineered for our scope. |
| 9 | `claude-handoff-spec` (planned) | Actor message-passing with continuation | Hewitt, Bishop, Steiger, "A Universal Modular ACTOR Formalism for Artificial Intelligence", *IJCAI* 1973 | full (planned) | Status / artifacts / blockers / suggested next persona = the message; the next persona is the continuation. |
| 10 | `ARCHITECTURE.md` § "Token economy" | Error budget | Beyer et al., *Site Reliability Engineering*, 2016, Ch. 3 | full | Tokens treated as the budget you spend; `claude-budget-guard` is the SRE-style burn-rate alerting. |
| 11 | `claude-spec-monitor` | Runtime verification — split into deterministic + advisory layers | Havelund & Goldberg, "Verify Your Runs", *VSTTE* 2008 | partial | Per rule #10, the deterministic share lives in `scripts/check-rule-{1..7}-*.mjs` + `scripts/check-pattern-index.mjs` + `scripts/check-pr-self-grade.mjs` (each a load-bearing CI gate). The residual judgement share lives in `novel/spec-monitor/` as an advisory-only Claude Skill (row 35) capped at ≤5 advisory rules. Original "full runtime verification" framing is now declared a deviation: classical runtime verifiers produce deterministic verdicts uniformly; here the verdict is split by mechanisability, with "what cannot be deterministically checked" explicitly carved out per rule #10. |
| 12 | Watch surface (3 numbers) | Glanceable / ambient information display | Card & Mackinlay, *Readings in Information Visualization*, 1999; Weiser & Brown, "Calm Technology", 1995 | full | Three values, no chrome; design discipline forbids a fourth (story 005). |
| 13 | Dashboard methodology (CLI / web tiers) | USE method (utilization, saturation, errors) + RED method (rate, errors, duration) | Gregg, *Systems Performance*, 2014; Wilkie, "RED Method", 2018 | full | Resources use USE; services use RED; both surface on the same dashboard. |
| 14 | Constitutional rule #7 + per-package failure-mode tables | Chaos engineering principles | Basiri et al., "Principles of Chaos Engineering", *IEEE Software* 2016 | full | Steady-state hypothesis, fault axis, blast radius, operator escape hatch — all explicit per artifact. |
| 15 | `aggregateStatus()` in `novel/adapters/observability/src/index.ts` | Worst-status aggregation over a status lattice | Avizienis et al., "Basic Concepts and Taxonomy of Dependable and Secure Computing", *IEEE TDSC* 2004 (status hierarchies) | full | Red ⊐ yellow ⊐ green; meet operation in the lattice; same as Kubernetes pod-phase aggregation. |
| 16 | `selfTest()` adapter contract | Self-checking software / health probe | Avizienis, "The N-Version Approach to Fault-Tolerant Software", *IEEE TSE* 1985; Burns et al., "Borg, Omega, and Kubernetes", *ACM Queue* 2016 (liveness probe) | full | Each adapter exposes a `selfTest()` returning a status / message / latency / lastCheck record. |
| 17 | `setup.sh` ledger (`.minsky/state.json`) | Idempotent step ledger / write-ahead log idempotency | Helland, "Life beyond Distributed Transactions", *CIDR* 2007 (immutable log of completed steps) | partial | Single-file JSON ledger; `mkdir`-based atomic lock. **Deviation:** not durable across machine death (no fsync barriers; no replication). **Why acceptable:** single-machine bootstrap; if the laptop dies mid-setup, `--reset` is the recovery path. **What would restore full conformance:** SQLite + WAL mode for the ledger — over-engineered today, on the table when the multi-machine task lands. |
| 18 | `.mcp.json` (project-scope MCP registration) | Configuration as code / 12-factor config | Wiggins, "The Twelve-Factor App", 2011, factor III; HashiCorp Terraform IaC pattern | full | All adapter / MCP wiring lives in repo, version-controlled, identical for every contributor. |
| 19 | `/next-task` skill (queue-mode loop) | Fixed-priority preemptive scheduling | Liu & Layland, "Scheduling Algorithms for Multiprogramming in a Hard Real-Time Environment", *JACM* 1973; Liu, *Real-Time Systems*, 2000 | partial | Discrete priority levels (P0–P3) instead of numerical; preemption is at task boundaries (cooperative), not interrupt-level. **Why acceptable:** task durations are minutes-to-hours, so cooperative scheduling has no observable cost; discrete levels match human triage. |
| 20 | TypeScript strict++ + Biome + lefthook | Defensive programming + early-failure / fail-fast | Hunt & Thomas, *The Pragmatic Programmer*, 1999 (Tip 32 "crash early"); Lampson 1983 (above) hint "use exceptions only for exceptional conditions"; Beck, *Extreme Programming Explained*, 1999 (continuous integration) | full | Strict types catch defects at compile; Biome catches style/complexity at lint; lefthook fails fast on commit. The constraint (rule #6) is moved to the cheapest possible point. |
| 21 | The `(@agent-id)` claim convention in TASKS.md | Lease | Gray & Cheriton, "Leases: An Efficient Fault-Tolerant Mechanism for Distributed File Cache Consistency", *SOSP* 1989 | partial | Time-bounded exclusive access. **Deviation:** no explicit timeout; staleness is detected by the picker rule "Found + stale (no related code) → unclaim". **Why acceptable:** humans + git history are the timeout signal. |
| 22 | This index | Pattern language / pattern catalogue | Alexander et al., *A Pattern Language*, 1977; Gabriel, *Patterns of Software*, 1996 | full | A living catalogue indexed by artifact, anchored in source, with explicit conformance level — the form pattern languages take in software architecture per Gabriel. |
| 23 | Constitutional rule #9 + the per-task measurement contract | Goal-Question-Metric + pre-registration | Basili, Caldiera, Rombach, "The Goal-Question-Metric Approach", *Encyclopedia of Software Engineering* 1994; Munafò et al., "A Manifesto for Reproducible Science", *Nature Human Behaviour* 1, 0021, 2017 (pre-registration); Fagerholm et al., "Building Blocks for Continuous Experimentation", *RCoSE* 2014 | full | Goal = success criterion. Question = "what would tell us the goal is being met?". Metric = the OTEL query / shell command in the measurement-method cell. Pre-registration (committing the prediction before observing) is what makes the experiment falsifiable; pivot threshold operationalises Ries 2011's pivot-or-persevere. Iron rule: no exemption for bugfixes — preparation PRs land the metric source first when missing. |
| 24 | `@minsky/observability` (`Observability` interface + `OtelObservability` strategy) | Adapter (structural) + Strategy (behavioral); three-signal observability (traces / metrics / logs) | Gamma et al., *Design Patterns*, 1994; OpenTelemetry specification (CNCF 2020+) | partial | OTEL implementation deviates from the standard convention by *not* registering its providers globally (`trace.setGlobalTracerProvider`, etc.) — see the package README's "v0 deviation declared" section. Restoring full conformance: a single explicit `setup.sh`-driven global registration at process start, tracked as future task `register-otel-globals-at-bootstrap`. |
| 25 | `@minsky/token-monitor` (`TokenMonitor` interface + `StubTokenMonitor`) | Adapter + Strategy; test double / fake | Gamma et al. 1994; Meszaros, *xUnit Test Patterns*, 2007 (test double / fake) | full | Interface + in-memory stub for tests. Real Strategy against Maciek's `claude-monitor` Python tool ships in `budget-guard-maciek-impl` follow-up. |
| 26 | `@minsky/budget-guard` (`BudgetGuard` watchdog + `decide()` pure decision function) | Watchdog (periodic-deadline check loop, hardware / OS literature) + error-budget burn-rate alerting | Beyer et al., *Site Reliability Engineering*, Ch. 3, 2016 (error budgets); failure-mode response labels from rule #7 | partial | Core decision logic + watchdog loop + HTTP envelope (row 28) shipped. Flag-file envelope and Maciek-impl Strategy still pending sub-tasks (`budget-guard-flag-file`, `budget-guard-maciek-impl`). Path deviation: original task brief specified `/var/run/minsky/budget.flag` which would need root; v0 follow-up uses `${MINSKY_HOME}/.minsky/budget.flag` instead — root-required paths out of scope for solo-dev tier. |
| 27 | `@minsky/handoff-spec` (record format spec + parser + validator) | Actor message-passing with continuation; recursive-descent parsing; schema validation | Hewitt, Bishop, Steiger, *IJCAI* 1973 (actor model); Aho-Sethi-Ullman, *Compilers*, 1986 (recursive-descent) | full | The record IS the message; `Suggested next` IS the continuation. v0 covers all 5 reference fixtures + all 3 invalid fixtures at 100 % branch coverage. 1 MB input cap (configurable via `{ maxBytes }`) at parser entry returns `kind: "input-too-large"` instead of OOMing — Armstrong 2007 (let it crash, but with a precise error). |
| 28 | `@minsky/budget-guard` HTTP envelope (`BudgetServer` interface + `HonoBudgetServer` strategy + `budgetResponse` JSON renderer) | Adapter + Strategy + DTO (Data Transfer Object) | Gamma et al., *Design Patterns*, 1994; Fowler, *Patterns of Enterprise Application Architecture*, 2002 (DTO) | full | `BudgetServer` is the adapter; Hono is one Strategy (Fastify / native http would be alternatives). DTO shape is fixed by `ARCHITECTURE.md` § "Token economy". `cost` field is `null` until `budget-guard-maciek-impl` ships the real `TokenMonitor`. |
| 29 | `@minsky/experiment-record` (`EXPERIMENT.yaml` schema + parser + validator + CLI) | Pre-registration record + JSON-Schema-validated DTO + recursive-descent parser | Munafò et al., "A Manifesto for Reproducible Science", *Nature Human Behaviour* 1, 0021, 2017 (pre-registration); Fowler 2002 (DTO); Aho-Sethi-Ullman, *Compilers*, 1986 (parser shape); AsPredicted.org schema (concrete pre-registration template) | full | The metric source for the rule-#9 automation layer (`ci-experiment-runner-v0`, `experiment-tracker-v0`). Schema is intentionally tiny — every field already exists as a rule-#9 declaration; nothing new invented. Vanity-metric phrases are rejected at parse time per rule #9's anti-pattern list. `timeout_seconds` (default 60) is the per-experiment wall-clock cap consumed by `ci-experiment-runner-v0`. |
| 30 | `@minsky/adapter-types` (`SelfTestStatus` / `SelfTestResult` / `aggregateStatus()` leaf package) | Shared-types module + status lattice + acyclic dependency principle | Avizienis et al., "Basic Concepts and Taxonomy of Dependable and Secure Computing", *IEEE TDSC* 2004 (status lattice); Martin, *Clean Architecture*, 2017 (acyclic dependency principle); Wiggins, *The Twelve-Factor App*, 2011 (factor II — explicit dependencies) | full | Hoisted out of `@minsky/observability` so future adapters depend on this leaf directly instead of forming a `budget-guard → observability` cycle through a base type. `@minsky/observability` re-exports for back-compat; canonical home is the leaf. Zero internal Minsky deps — that is what makes it a leaf. Supersedes row 15's path (`aggregateStatus` now lives at `novel/adapters/types/src/index.ts`); row 15 kept for historical context until the next vision.md sweep. |
| 31 | `ci-experiment-runner-v0` (`scripts/run-experiment.mjs` + `.github/workflows/experiment.yml`) | Continuous experimentation runner (per-change experiment execution) + GitHub Actions deterministic CI | Fagerholm et al., "Building Blocks for Continuous Experimentation", *RCoSE* 2014 (the per-change experiment runner is the first building block); Kohavi/Tang/Xu, *Trustworthy Online Controlled Experiments*, 2020, ch. 4 (running every change as an experiment); Beck, *Extreme Programming Explained*, 1999 (CI as constraint enforcer); rule #10 (deterministic enforcement) | full | Closes the daily layer of rule #9. Job `gate` validates `EXPERIMENT.yaml` + measurement-runnability per PR; job `record` re-runs measurement against merge-base + main and appends a JSONL line to `experiment-store/<id>.jsonl`. Pure decision function `runExperiment(...)` is referentially transparent (rule #10); the CLI wrapper owns I/O. Trivial-PR exemption is two-factor (label + body comment, matching the rule-3 deferral pattern). |
| 32 | `scripts/check-rule-6-let-it-crash.mjs` (TS-AST diff-based lint for rule #6) | Deterministic CI gate over a TypeScript AST; let-it-crash supervision discipline | Armstrong, *Programming Erlang*, 2007 (let it crash); Lampson 1983 (use exceptions only for exceptional conditions); rule #10 (deterministic enforcement) | full | Walks every PR-touched `novel/**/*.ts` file (non-test); flags `try/catch` blocks nested deeper than 1 level (`nested-try`) or catch clauses whose body neither re-throws nor calls the bare-identifier supervisor `supervise(...)` (`swallowing-catch`). Per-catch opt-out: `// rule-6: handled-locally — <reason ≥3 chars>` immediately above the `catch` keyword. Diff-based — existing catch chains are grandfathered until the file is modified again (rule-1 / rule-3 precedent). |
| 33 | `scripts/check-rule-4-otel-coverage.mjs` (CI lint enforcing rule #4) | Deterministic conformance gate over a PR diff; TypeScript compiler-API AST walk + JSDoc-tag contract | OpenTelemetry specification (CNCF 2020+); Gregg, *Systems Performance*, 2014 (USE method — instrumentation as a structural property); Lampson, "Hints for Computer System Design", *SOSP* 1983 (move the constraint to the cheapest possible point); rule #10 (deterministic enforcement) | full | Diff-based: only files newly added or modified relative to `origin/main` are scanned. Existing un-annotated code is grandfathered — same precedent as rule-1, rule-3. Pure function (`checkOtelCoverage`) over `{ files: { path, source }[] }` is the load-bearing contract; the CLI is the I/O boundary. Tests cover all top-level export shapes (function declaration, default export, arrow + function expression assigned to `export const`, class methods including private/`#name`, constructors). |
| 34 | `scripts/check-pattern-index.mjs` (CI lint enforcing rule #8) | Deterministic CI gate over a PR diff; pattern-catalogue conformance check | Beck, *Extreme Programming Explained*, 1999 (CI as the constraint enforcer); Alexander et al., *A Pattern Language*, 1977 (catalogue indexed by artefact); rule #8 (pattern conformance — every artefact maps to a published pattern); rule #10 (deterministic enforcement) | full | Diff-based: every newly-added (status A) file under `novel/**`, root `*.md`, `setup.sh`, `distribution/**`, or `.github/workflows/**` must be mentioned by `vision.md` § "Pattern conformance index" (full path, package-prefix, or basename), unless the file's first ~20 lines carry `<!-- pattern: not-applicable — <reason> -->` (≥3-char reason). Test files (`*.test.ts`, `*.test.mjs`), fixture files, and node_modules are skipped. Modifications/renames/deletions are skipped — same grandfathering precedent as rule-1 / rule-3 / rule-4. Pure function `checkPatternIndex({ changedFiles, visionMdContent, optOuts })` is the load-bearing contract; CLI is the I/O boundary. |
| 35 | `novel/spec-monitor/` (`SKILL.md` + `test/judgement-only/` + `test/deterministic-overlap/` + `spec-advisories/`) | Runtime verification split into deterministic-monitor + advisory-judgement layers; advisory-only Claude Skill | Havelund & Goldberg, "Verify Your Runs", *VSTTE* 2008 (runtime verification); Hunt & Thomas, *The Pragmatic Programmer*, 1999, Tip 32 ("crash early"); rule #10 (vision.md § 10 — deterministic enforcement, ratchet rule: when a deterministic linter is added the matching Skill check is removed); rule #8 (pattern conformance — the Skill advises on conformance-level mismatches the linter cannot read). | partial | Advisory-only Skill, never gates CI — the deterministic linters at `scripts/check-rule-{1,2,3,4,5,6,7}-*.mjs`, `scripts/check-pattern-index.mjs` and `scripts/check-pr-self-grade.mjs` carry the load-bearing share. The Skill addresses ≤5 residual judgement-heavy concerns (A1 hypothesis vagueness, A5 conformance level mismatched to source — A2 pivot equals success was promoted to deterministic CI lint at row 39, A4 measurement output unchecked at row 40, A3 non-primary anchor at row 52, all retired from the Skill); growing past 5 requires either retiring one or shipping a deterministic linter (rule-#10 ratchet). **Deviation from Havelund-Goldberg 2008:** classical runtime verification produces deterministic verdicts; this Skill produces advisory verdicts only. The deterministic share of runtime verification lives in the linters above; this row covers the residual *judgement* share that rule #10 explicitly carves out as "what cannot be deterministically checked is not a constitutional rule" — kept as an advisory companion, not an enforcement mechanism. |
| 36 | `experiment-tracker-v0` (`scripts/replay-experiment.mjs` + `.github/workflows/experiment-tracker.yml`) | Scheduled batch job + verdict ladder over an append-only per-experiment store; MAPE-K Analyze phase scoped to rule #9 | Ries, *The Lean Startup*, 2011 (build-measure-learn; sustained-gain discipline — value at +1d is misleading, +7d is the floor); Kohavi/Tang/Xu, *Trustworthy Online Controlled Experiments*, 2020, ch. 5–7 (trustworthy over fast; novelty effect; mid-term regressions); Kephart & Chess, "The Vision of Autonomic Computing", *IEEE Computer* 2003 (MAPE-K Analyze phase); rule #9 (vision.md § 9 weekly–monthly layer); rule #10 (deterministic enforcement) | full | Closes the weekly–monthly layer of rule #9. Daily 09:00 UTC cron walks `experiment-store/*.jsonl`, finds the merge boundary, and re-runs the recorded `measurement` for any replay window whose boundary has elapsed. Pure function `replayExperiment(...)` is referentially transparent over `{ meta, record, priorReplays, currentValueStdout, now, ref, windowDays }`; the CLI is the I/O boundary. Verdict ladder: `validated` writes one line to `validated-learnings.md`; `regressed` (only after two consecutive pivot crossings — matches Risk mitigation) appends a `pivot-experiment-<id>` task to TASKS.md; `inconclusive` is the default. Corrupt JSONL surfaces as a warning, never crashes the run (rule #7). |
| 37 | `scripts/check-skill-rule-cap.mjs` (CI lint enforcing the ≤5 advisory-rule cap on `novel/spec-monitor/SKILL.md`) | Deterministic CI gate over an advisory-Skill scope cap — rule-#10 ratchet applied to the Skill itself | Beck, *Extreme Programming Explained*, 1999 (CI as the constraint enforcer); rule #10 (vision.md § 10 — deterministic enforcement; ratchet rule keeps the Skill bounded so it cannot grow back into a primary enforcement mechanism); rule #6 (failure-mode discipline — closes the `audit-spec-monitor-coverage` "did anyone notice?" failure mode mechanically) | full | Pure function `checkSkillRuleCap({ skillContent, maxRules })` over the locked `### A<N>.` heading shape used by the live SKILL.md; CLI is the I/O boundary. Missing SKILL.md is treated as a pass (retired Skill is the rule-#10 terminal state, not a violation). Runs on `pull_request` only — push-to-main is skipped (matching `pr-self-grade` / `pattern-index` precedent). |
| 38 | `distribution/test-supervisor.sh` + `linux-supervisor-integration` / `macos-supervisor-integration` CI jobs | Integration test driver / supervisor conformance harness — empirical chaos test against a real OS supervisor | Forsgren, Humble, Kim, *Accelerate*, 2018 (test reliability as DORA prerequisite — every check that gates a merge must run reliably or it teaches the team to ignore failure); Armstrong, *Programming Erlang*, 2007 (OTP supervision tree — the pattern under test); Basiri et al., "Principles of Chaos Engineering", *IEEE Software* 2016 (controlled-fault chaos test); rule #7 (failure-mode discipline) | full | Closes the "Integration test (deferred)" clause in `distribution/README.md`. Exercises rows 1–4 of the failure-mode table empirically: SIGKILLs `minsky-tick-loop` and asserts respawn within 10 s (row 1), kills `minsky-budget-guard` and asserts permanent restart (row 3), verifies graceful stop does not respawn (row 2 weak form), runs a kill storm and asserts blast-radius containment (row 4). Linux mode bootstraps systemd `--user` units with a `loginctl enable-linger` + `dbus-run-session` fallback chain; macOS mode bootstraps LaunchAgents into `gui/$(id -u)`. Driver renders the templates' stub `run-*.sh` runners on the fly because the real tick-loop / budget-guard implementations land in follow-up tasks — restart-policy verification only depends on process presence. **Caveat:** GH-hosted Ubuntu runners' user-systemd plumbing is fragile; the driver exits 77 (skipped) if no user-bus is reachable, and the CI gate accepts `skipped` to preserve the merge path while still surfacing the signal when it runs. Pivot to a self-hosted runner is filed as `supervisor-integration-self-hosted-runner` in TASKS.md. |
| 39 | `scripts/check-pivot-success-margin.mjs` + `pivot-success-margin` CI job | Deterministic CI gate over a rule-#9 record field (`success`/`pivot` margin); rule-#10 ratchet promoting spec-monitor advisory rule A2 | Ries, *The Lean Startup*, 2011 (build-measure-learn / pivot-or-persevere — a pivot threshold that equals or sits within noise of the success threshold carries no information; it is theatre); rule #10 (vision.md § 10 — deterministic enforcement; ratchet rule: when an advisory rule is promoted to a deterministic linter, the advisory counterpart is removed in the same PR); Beck, *Extreme Programming Explained*, 1999 (CI as the constraint enforcer); `spec-advisories/2026-05-03-quarterly-audit.md` (audit decision to promote A2) | full | Pure decision function `checkPivotSuccessMargin({ success, pivot })` over the `@minsky/experiment-record`-parsed EXPERIMENT.yaml; CLI is the I/O boundary. Numeric extraction: scans each field for the first signed-numeric token (optional `-`/`+`, integer + optional fractional part). Decision table: both numeric → fail when `\|Δ\| / max(\|success\|, 1) < 1 %`; neither numeric and equal-after-trim/lower → fail (binary equality); neither numeric and differ → ok with advisory warning (long-tail prose stays advisory because the deterministic layer cannot read meaning); mixed → ok with advisory warning. Opt-out for legitimately-binary metrics: a top-level YAML comment line `# rule: ci-lint-pivot-success-margin: skip <reason>`. Pairs with `scripts/check-pivot-success-margin.test.mjs` (19 tests across the decision table). Ratchet: spec-monitor's `### A2.` heading is replaced by a tombstone `### A2 (retired …)` whose shape is intentionally outside the `check-skill-rule-cap` regex (rule #10's "remove the advisory rule when its deterministic counterpart ships" clause). |
| 40 | `scripts/check-measurement-inspects-output.mjs` (CI lint promoting spec-monitor advisory rule A4 — measurement inspects output) | Deterministic CI gate over the rule-#9 `measurement` field; allowlist + blacklist token classifier over the parsed YAML record — rule-#10 ratchet applied to the spec-monitor Skill | rule #10 (vision.md § 10 — deterministic enforcement; ratchet rule: when a deterministic linter is added the matching advisory rule is removed in the same PR); Havelund & Goldberg, "Verify Your Runs", *VSTTE* 2008 (runtime verification — the inspect-output check is the deterministic-monitor layer carved out from the residual advisory share); Beck, *Extreme Programming Explained*, 1999 (CI as the constraint enforcer); `spec-advisories/2026-05-03-quarterly-audit.md` (audit decision: A4 admits a deterministic allowlist + blacklist) | full | Pure function `checkMeasurementInspectsOutput(measurementCmd) => { ok, level: "fail" \| "warn" \| "pass", reason }`; CLI is the I/O boundary (read EXPERIMENT.yaml, parse via `@minsky/experiment-record`, run the check, exit). Three-way verdict: `pass` (allowlist token present — `test`, `[`, `[[`, `jq -e`, `grep -q`/`-c`, `assert`, `vitest`, `pnpm test`/`typecheck`/`lint`, `@tasks-md/lint`, `markdownlint-cli2`, `gh run list ... --jq`, `node scripts/check-*.mjs`); `fail` (blacklist token present and no allowlist hit — `echo`, bare `true`, bare `curl`, bare `node`); `warn` (neither — residual judgement scope, exit 0 with stderr advisory). Word-boundary matching prevents `latest` from spuriously hitting `test`. Removed A4 from `novel/spec-monitor/SKILL.md` (the slot is reserved). |
| 41 | `user-stories/001-loop-runs-overnight.md` | OTP supervision behaviour (one-for-one supervisor / let-it-crash) composed with periodic-task scheduling | Armstrong, *Programming Erlang*, 2007 (OTP supervision); Liu & Layland, "Scheduling Algorithms for Multiprogramming in a Hard Real-Time Environment", *JACM* 1973 (periodic scheduling) | partial | The supervisor primitive is systemd / launchd (POSIX), not BEAM — same deviation already declared at row 4 for the architectural locus. Tick cadence is minutes-to-hours, so respawn latency (~100 ms vs Erlang's microseconds) is invisible at the user-story's success threshold (`overnight_uptime_pct` ≥ 99 %). |
| 42 | `user-stories/002-pause-from-iphone.md` | Remote procedure call as the control surface, combined with cooperative cancellation via flag-file checkpoint | Birrell & Nelson, "Implementing Remote Procedure Calls", *ACM TOCS* 2(1) 1984; Stevens, *Advanced Programming in the UNIX Environment*, Addison-Wesley, 1992, Ch. 10 (signals, graceful shutdown) | partial | Pause is delivered as an HTTPS request over Tailscale (RPC at the wire) but consumed as a flag-file rather than a synchronous RPC return — the in-flight tick checks the flag at the next safe point and self-suspends. Birrell-Nelson's transparency property is intentionally weakened: the caller does not block until the loop has paused; the Watch surface is the eventual-consistency read-back. |
| 43 | `user-stories/003-mape-k-improves-prompts.md` | MAPE-K reference architecture for autonomic computing, combined with Goal-Question-Metric for the rollout decision | Kephart & Chess, "The Vision of Autonomic Computing", *IEEE Computer* 36(1) 2003; Basili, Caldiera, Rombach, "The Goal-Question-Metric Approach", *Encyclopedia of Software Engineering* 1994 | full | Each phase (Monitor / Analyze / Plan / Execute) emits one OTEL span; Knowledge base is `constraints.md`. Cross-references row 5 (`claude-mape-k-loop` implementation contract) and row 23 (rule #9 GQM operationalisation) — this row anchors the user-story specification. |
| 44 | `user-stories/004-budget-auto-pause.md` | Error-budget burn-rate alerting with graceful degradation / load-shedding | Beyer, Jones, Petoff, Murphy (eds.), *Site Reliability Engineering*, O'Reilly, 2016, Ch. 3 (error budgets) and Ch. 24 (overload / load-shedding) | full | The 70 % / 85 % / weekly-cap thresholds map directly to the SRE burn-rate ladder; the model-downgrade (Sonnet to Haiku) is the load-shedding response. Cross-references rows 10 and 26 (`@minsky/budget-guard` watchdog and decision function); this row anchors the user-story specification. |
| 45 | `user-stories/005-watch-three-numbers.md` | Glanceable / ambient information display combined with Calm Technology | Card, Mackinlay, Shneiderman, *Readings in Information Visualization*, Morgan Kaufmann, 1999, Ch. 1; Weiser & Brown, "Designing Calm Technology", *PowerGrid Journal* 1995; Weiser, "The Computer for the 21st Century", *Scientific American* 265(3) 1991 | full | Three values, no chrome; the surface stays at the periphery of attention until needed. Inverted dwell metric (`wrist_dwell_seconds_per_day` ≤ 60 s) operationalises the calm-tech invariant — more attention to the read-out is a sign the surface or the system is failing. Cross-references row 12 (Watch surface implementation locus); this row anchors the user-story specification. |
| 46 | `competitors/composio-ao.md` | Multi-agent orchestration with isolated workspaces and a central supervisor / dashboard (the pattern AO implements) | Wooldridge, *An Introduction to MultiAgent Systems*, 2nd ed., Wiley, 2009, Ch. 6 (cooperative distributed problem solving); Ousterhout et al., "Sprite Network Operating System", *IEEE Computer* 1988 (per-agent isolation as the workspace primitive) | full | Conformance is full *for the pattern AO implements*. Minsky's relationship: don't adopt — objective function differs (PR-throughput vs years-long viability). Worktree-per-agent inspiration is taken from OMC (row 50), not AO. |
| 47 | `competitors/crewai.md` | Role-based multi-agent orchestration (role / goal / backstory / tools per agent; "crew" composition) — the pattern CrewAI implements | Wooldridge, *An Introduction to MultiAgent Systems*, 2nd ed., Wiley, 2009, Ch. 7 (methodologies for agent-oriented analysis and design) | full | Conformance is full *for the pattern CrewAI implements*. Minsky's relationship: don't adopt — Minsky is Claude-Code-native and coding-specific; CrewAI's role-play orchestration occupies the same surface as OMC (row 50) within the Anthropic stack, and we delegate to OMC. |
| 48 | `competitors/metagpt.md` | Simulated software company / Standard Operating Procedure role-play across PM / Architect / Engineer / QA — the pattern MetaGPT implements | Hong et al., "MetaGPT: Meta Programming for a Multi-Agent Collaborative Framework", *ICLR* 2024 (the primary paper); Brooks, *The Mythical Man-Month*, Addison-Wesley, 1975, Ch. 3 ("The Surgical Team", the older organisational-design pattern MetaGPT formalises for LLMs) | full | Conformance is full *for the pattern MetaGPT implements*. Minsky's relationship: don't adopt — shared persona-driven paradigm but Claude Code-native via OMC (row 50) rather than Python+GPT runtime. |
| 49 | `competitors/microsoft-agent-framework.md` | Graph-based workflow orchestration of agents (declarative pipelines, checkpointing, human-in-the-loop) combined with three-signal observability — the pattern MAF implements | van der Aalst & van Hee, *Workflow Management: Models, Methods, and Systems*, MIT Press, 2002 (workflow nets and the four perspectives); OpenTelemetry specification, CNCF (2020+) | full | Conformance is full *for the pattern MAF implements*. Minsky's relationship: don't adopt — wrong stack (.NET / Python) and enterprise framing. Minsky borrows the OTEL-as-first-class-concern lesson (row 24) but rejects the graph-workflow runtime in favour of MAPE-K (row 5) and OMC handoffs. |
| 50 | `competitors/omc.md` | Multi-agent orchestration with a shared task list and a manager-agent dispatcher — blackboard-style coordination across a generic role-based agent collective (the pattern OMC implements) | Hayes-Roth, "A Blackboard Architecture for Control", *Artificial Intelligence* 26(3) 1985; Wooldridge, *An Introduction to MultiAgent Systems*, 2nd ed., Wiley, 2009 | full | Conformance is full *for the pattern OMC implements*. Minsky's relationship: adopt — OMC is the `Orchestrator` dependency; the blackboard substrate maps directly to `TASKS.md` (row 8). Minsky adds the layers OMC explicitly does not cover (24 / 7 supervision, token-economy homeostasis, MAPE-K self-improvement, mobile / Watch surface) without reimplementing the orchestration layer. |
| 51 | `competitors/ralph-wiggum-official.md` | Read-Eval-Print Loop / fixed-point iteration with a bounded termination predicate — the pattern Ralph implements | McCarthy, "Recursive Functions of Symbolic Expressions and Their Computation by Machine, Part I", *Communications of the ACM* 3(4) 1960 (REPL and fixed-point iteration as the substrate of LISP); Knuth, *The Art of Computer Programming*, Addison-Wesley, 1968, Vol. 1 § 1.2.1 (bounded iteration discipline) | full | Conformance is full *for the pattern Ralph implements*. Minsky's relationship: adopt — Ralph is the `InnerLoop` primitive (`novel/adapters/inner-loop.ts`). The outer Minsky supervisor (row 4) wraps Ralph's per-session loop with cross-session continuity, token-budget awareness, and the MAPE-K self-improvement loop (row 5). |
| 53 | `novel/adapters/prompt-optimizer/` (`@minsky/prompt-optimizer` — `PromptOptimizer` interface, `StubPromptOptimizer` test fake, `AnthropicPromptOptimizer` Strategy backed by `@anthropic-ai/sdk`; sub-task 1 of 4 of `mape-k-loop-v0` per `research.md` § "DSPy fit" — the operationalised DSPy fallback) | Adapter (structural) + Strategy (behavioral) over a vendor LLM SDK, with an A/B-test primitive (`runABTest`) and a typed-call analogue of DSPy's `Signature` (`structured<T>`); `cache_control` on the system prefix per ARCHITECTURE.md § "Token economy" | Gamma, Helm, Johnson, Vlissides, *Design Patterns*, Addison-Wesley, 1994 (Adapter + Strategy); Khattab et al., "DSPy: Compiling Declarative Language Model Calls into Self-Improving Pipelines", *ICLR* 2024 (the rejected baseline whose optimizer-shape lessons survive in the interface); Kohavi, Tang, Xu, *Trustworthy Online Controlled Experiments*, Cambridge UP 2020, Ch. 3 (the sustained-gain window operationalised by `ABResult.sustainedGainAt7d`); Meszaros, *xUnit Test Patterns*, Addison-Wesley, 2007 (`StubPromptOptimizer` as a fake with the contract-test discipline); `research.md` § "DSPy fit" (the rejection + fallback record); rule #2 (every dep behind interface — the adapter IS the wrapping for `@anthropic-ai/sdk`) | full for `PromptOptimizer` interface, `StubPromptOptimizer`, `AnthropicPromptOptimizer`, and `selfTest()`; partial for `runABTest` (`sustainedGainAt7d` is always `false` in v0 — the Kohavi-Tang-Xu sustained-gain check ships in sub-task 3 against a verdict log spanning ≥7 d) and `structured<T>` (the schema is currently a system-prompt rider, not a tool-use response constraint — full conformance arrives when the Anthropic strategy adopts tool-use output enforcement) | Default Strategy is `AnthropicPromptOptimizer`; constructor accepts an injected `MessagesClient` so tests substitute a deterministic stub without an `ANTHROPIC_API_KEY`. `selfTest()` returns `yellow` (graceful-degrade per rule #7) when no API key is set and no client was injected; `green` on a healthy injected client; `red` on injected-client error. Interface is the DSPy-fallback shape pre-registered in `research.md` § "DSPy fit" — the operationalisation of the rule-#2 invariant that vendor names not appear in business logic. Sub-tasks 2-4 (Monitor / Analyze / Plan / Execute / Knowledge phases of `mape-k-loop-v0`) consume this adapter. |
| 52 | `scripts/check-anchor-primary-source.mjs` (CI lint promoting spec-monitor advisory rule A3 — anchor citation is a primary source) | Deterministic CI gate over the rule-#9 `anchor` field; deny-list + allowlist token classifier over the parsed YAML record — rule-#10 ratchet applied to the spec-monitor Skill | rule #10 (vision.md § 10 — deterministic enforcement; ratchet rule: when an advisory rule is promoted to a deterministic linter, the advisory counterpart is removed in the same PR); rule #5 / rule #8 (vision.md § 5 / § 8 — named, decades-tested pattern; the constitution must outlive any single source); Havelund & Goldberg, "Verify Your Runs", *VSTTE* 2008 (runtime verification — the primary-source check is the deterministic-monitor layer carved out from the residual advisory share); Beck, *Extreme Programming Explained*, 1999 (CI as the constraint enforcer); `spec-advisories/2026-05-03-quarterly-audit.md` (audit decision: A3 admits a deterministic deny-list + allowlist) | full | Pure function `checkAnchorPrimarySource(anchor) => { ok, level: "fail" \| "warn" \| "pass", reason }`; CLI is the I/O boundary (read EXPERIMENT.yaml, parse via `@minsky/experiment-record`, run the check, exit). Three-way verdict: `pass` (allowlist token present — italicised title `*…*`, `Ch. <n>`, `pp. <n>`, ISBN, DOI, `<VENUE> <YEAR>`, internal cross-ref `vision.md §` / `rule #<n>` / `spec-advisories/<date>.md`); `fail` (deny-list token present and no allowlist hit — `medium.com`, `*.substack.com`, `wikipedia.org`, `twitter.com`, `x.com`, `reddit.com`, `stackoverflow.com`, `chatgpt.com`, `claude.ai`, "ChatGPT said", "tweet by", "blog post"); `warn` (neither and anchor <25 chars — residual judgement scope, exit 0 with stderr advisory); long unrecognised prose passes silently. Allowlist wins over deny-list (a primary citation that contrasts against a Medium URL still passes). Opt-out: top-level YAML comment `# rule: ci-lint-anchor-primary-source: skip <reason ≥3 chars>`. Removed A3 from `novel/spec-monitor/SKILL.md` (the slot is reserved as a tombstone). |
| 54 | `novel/mape-k-loop/` (`@minsky/mape-k-loop` — pure `monitor(...)` + `analyze(...)` + `plan(...)` + `execute(...)` + `knowledge(...)` decision functions, `sustainedGain` + `oscillation` guards, and the assembled `tick(...)` cycle; all 4 sub-tasks of `mape-k-loop-v0` shipped) | MAPE-K reference architecture (Monitor + Analyze + Plan + Execute + Knowledge phases) composed with Theory of Constraints (top constraint = max `violationCount × costEstimate`), pure-function decision module (Martin 2017 — every input is data, every output is data; the wrapping I/O is the user's), Kohavi-Tang-Xu sustained-gain window guard (default 7 d), Ries 2011 oscillation guard (default 10-iteration lookback), and a Helland 2007 immutable append-only log (`constraints.md`) as the Knowledge substrate | Kephart & Chess, "The Vision of Autonomic Computing", *IEEE Computer* 36(1) 2003 (the MAPE-K loop — Monitor / Analyze / Plan / Execute / Knowledge); Goldratt, *The Goal*, North River Press, 1984 (Theory of Constraints — the constraint is whatever currently bottlenecks throughput); Martin, *Clean Architecture*, Pearson, 2017 (pure decision module + thin I/O boundary); Gregg, *Systems Performance*, Prentice Hall, 2014 (USE method — the per-rule aggregate-counter shape carries the *utilisation* column; saturation + errors deferred to a follow-up); Kohavi, Tang, Xu, *Trustworthy Online Controlled Experiments*, Cambridge UP 2020, Ch. 3 (the sustained-gain window — 7 d default — operationalised by `sustainedGain`); Ries, *The Lean Startup*, Crown Business 2011 (build-measure-learn — the oscillation guard is the "don't re-pivot to a previously-rejected variant" guardrail); Helland, "Life beyond Distributed Transactions", *CIDR* 2007 (the append-only `constraints.md` Knowledge log); Munafò et al., "A Manifesto for Reproducible Science", *Nature Human Behaviour* 1, 0021, 2017 (the rule-#9 calibration-drift amendment that the Knowledge phase emits when MAE exceeds threshold); rule #1 README opt-out (existing autonomic-manager runtimes — IBM Tivoli, OpenStack Heat, Kubernetes operator-sdk — rejected because their decision modules couple to a specific cluster substrate; Minsky's input shape is markdown + YAML + GH Actions JSON) | full — M+A+P+E+K phases + 2 guards + assembled `tick(...)` shipped; the user-story-003 integration test passes against the assembled package end-to-end | Pure functions `monitor(input)`, `analyze({ snapshot, costs? })`, `plan({ topConstraint, evidence, basePrompt })`, `execute({ variants, evalSet, optimizer, metric, history, now, sustainedGainWindowDays?, oscillationLookback? })`, `knowledge({ verdictLog, calibrationDriftThreshold?, topConstraintRuleId, executeDecision, executeReason, winnerVariantId, now })`, and `tick({ … })` are referentially transparent — no I/O, no shared state, clocks injected via `now`. `tick` runs M → A → P → E → K and threads an optional `emit` seam so the wrapping CLI can carry OTEL spans (`mape.<phase>.<verb>`) without coupling the pure functions to `@minsky/observability`. `knowledge` writes a markdown block to append to `constraints.md` (every tick is logged — even no-ops; Helland 2007), and *conditionally* a `researchMdAmendmentProposal` text when the verdict-log MAE between predicted and observed Δ exceeds the `calibrationDriftThreshold` (default 50 % per Munafò et al. 2017 — pre-registration discipline). `monitor` aggregates parsed `CiRun` / `Advisory` / `ExperimentRecord` rows into a per-rule `HealthSnapshot`; corrupt rows are dropped with a `warnings` entry (graceful-degrade per rule #7) instead of throwing. `analyze` ranks rules by `violationCount × costEstimate(ruleId)`; ties are broken alphabetically by `ruleId` so output is deterministic across runs (matching the prompt-optimizer `pickWinner` tie-break in row 53). `plan` proposes ≤3 variants from a fixed v0 mutation catalogue (`enumerate-failure-modes`, `direct-answer`, `tighten-scope`); empty `ruleId` throws (let-it-crash on caller contract violation). `execute` runs the A/B via the `PromptOptimizer` adapter (row 53), then applies the two guards: `sustainedGain` returns `{ ok: false }` for cold starts, mid-window swaps, or windows shorter than the configured `windowDays`; `oscillation` blocks re-proposal of any variant that was `rejected` or `abstain`-ed within the last `lookbackIterations`. The `costs` argument is the seam where the `costEstimate` schedule sources per-rule severity weights from vision.md § "Cost schedule" (see that section for the v0 default — `rule-9 = 100`, `rule-7 = 50`, `rule-typo = 1`). `parseCostSchedule(visionMdContent)` (`novel/mape-k-loop/src/cost-schedule.ts`) is the upstream parser; the CLI wrapper threads its output into `tick({ ..., costs })` which forwards it to `analyze`. `costEstimate` is now full-conformance — the schedule is configurable, sourced from a literature-anchored constant in source per rule #4, and falls back to `DEFAULT_RULE_COST = 1` for any rule absent from the table (rule #7 graceful-degrade). Severity buckets (`low` / `medium` / `high`) thresholded at product ≥3 / ≥8 — conservative v0 cutoffs. |
| 56 | `scripts/check-mape-k-constraints-md-size.mjs` (CI lint enforcing the 200-entry archive cap on `novel/mape-k-loop/constraints.md` — the autonomic-manager's append-only Knowledge log) | Deterministic CI gate over an append-only knowledge-log size cap; pure decision function over `{ content, capEntries }` with the dormant-on-missing-file short-circuit in the CLI | rule #10 (vision.md § 10 — deterministic enforcement; ratchet rule: every prose-only invariant in a `novel/**/README.md` gets a deterministic linter as soon as the artefact it guards becomes machine-readable); `novel/mape-k-loop/constraints.md` preamble (the prose-only 200-entry archive cap); Helland, "Life beyond Distributed Transactions", *CIDR* 2007 (immutable log — derived data through reissue, not mutation; archive-split discipline); Beck, *Extreme Programming Explained*, 1999, Ch. 17 (CI as the constraint enforcer) | full | Pure function `checkConstraintsMdSize({ content, capEntries }) => { ok, count, reason? }`; CLI is the I/O boundary (read `novel/mape-k-loop/constraints.md`, run the check, exit). Counts lines matching `^## YYYY-MM-DD` (strict ISO-8601 date match — backticked prose mentions of the heading shape in the README preamble are not counted). Boundary semantics: the cap is *inclusive* — exactly 200 entries passes (Beyer SRE 2016 Ch. 3 — "you have used X % of your budget" is not a violation until X *exceeds* it; matches `check-mape-k-budget-cap`'s precedent). `capEntries` defaults to 200 (the README prose anchor); the constant is locked by a paired test so a silent edit surfaces loudly. Dormant state (rule #7 — graceful degrade): missing `constraints.md` exits 0 with a stderr advisory and re-arms the moment the file lands — same precedent as `check-skill-rule-cap.mjs`'s retired-Skill terminal state. Pivot (rule #9): if 200 proves arbitrary (the file is still readable at 400), raise the constant rather than removing the lint — removing it would re-open the unbounded-growth failure mode rule-#6 closes mechanically. |
| 57 | `novel/dashboard-web/` (`@minsky/dashboard-web` — sub-task 1/4 of `dashboard-web-v0`; Hono v4 SSR skeleton with `createServer({ metrics })`, pure `render({ metrics })`, and a placeholder `SuccessMetric` stub) | SSR web rendering as glanceable information-visualization (Card & Mackinlay 1999) anchored to a service-level lens (Wilkie 2018 RED Method); Adapter (Gamma et al. 1994) over the HTTP runtime so Hono can be swapped for native http / Fastify without touching callers; pure-function decision module (Martin 2017) for the renderer | Card, Mackinlay, Shneiderman, *Readings in Information Visualization*, Morgan Kaufmann, 1999 (dashboard as glanceable display); Wilkie, "RED Method", *Weave Works blog* / *USENIX SREcon EMEA* 2018 (rate / errors / duration as the service-level lens); Gamma, Helm, Johnson, Vlissides, *Design Patterns*, Addison-Wesley, 1994 (Adapter — `createServer` is the Hono adapter shape); Martin, *Clean Architecture*, Pearson, 2017 (pure decision module — `render` is pure, the Hono server is the I/O boundary); rule #2 (vision.md § 2 — every dep behind interface); rule #7 (vision.md § 7 — graceful-degrade: empty `metrics` array still renders a well-formed document, HTML metacharacters in metric labels are escaped before reaching the client) | partial — only the skeleton (≤100 LoC) ships in this PR; sub-tasks 2 (`dashboard-web-metrics-enum`), 3 (`dashboard-web-render-all-10`), and 4 (`dashboard-web-lighthouse-ci`) wire the 10 vision.md success metrics, the OTEL backend through `@minsky/observability`, and the Lighthouse Mobile ≥0.9 CI gate respectively | Pure functions `render({ metrics })` (HTML string) and `createServer({ metrics })` (`{ app, fetch }` handle for tests + embedding) are referentially transparent — no I/O, no shared state. The server does not bind a port at construction; the wrapping `distribution/run-dashboard-web.sh` (deferred — covered when `dashboard-web-render-all-10` ships) owns port choice. `escapeHtml` is the rule-#7 XSS guard (chaos table row 3). Cold-start path: `render({ metrics: [] })` returns a well-formed document with an empty `<ul class="metrics">` so the dashboard never crashes on a missing OTEL backend (chaos table row 1). LoC cap: ≤100 in v0 (skeleton), with room to grow to 300 across sub-tasks 1–4 — the `dashboard-web-v0` parent's pivot threshold. |
| 55 | `scripts/check-mape-k-budget-cap.mjs` (CI lint enforcing ARCHITECTURE.md § "MAPE-K cadence" — the ≤5.7 % weekly Max5 budget cap on the autonomic-manager loop) | Deterministic CI gate over a rule-#9 cost-budget contract; pure decision function over `{ config, weeklyBudgetTokens, capFraction }` with the dormant-config short-circuit in the CLI | rule #10 (vision.md § 10 — deterministic enforcement; ratchet rule: every prose-only invariant in ARCHITECTURE.md / vision.md / research.md gets a deterministic linter as soon as the artefact it guards becomes machine-readable); ARCHITECTURE.md § "MAPE-K cadence" (the ≤5.7 % cap); research.md § "MAPE-K cadence" (the cost-math derivation: 14 watchdog passes/week × 0.3 % + 0–3 event passes/week × 0.5 % + a quarterly tick-iteration backstop ≤ 5.7 % of weekly budget); Beyer, Jones, Petoff, Murphy (eds.), *Site Reliability Engineering*, O'Reilly 2016, Ch. 3 (error-budget enforcement — exceed-the-budget triggers a mechanical response, not a discussion); Beck, *Extreme Programming Explained*, 1999 (CI as the constraint enforcer) | full | Pure function `checkMapeKBudgetCap({ config, weeklyBudgetTokens, capFraction }) => { ok, reason? }`; CLI is the I/O boundary (read `config/mape-k.json`, run the check, exit). Config shape: either `{ weeklyProjectedTokens, weeklyBudgetTokens }` or `{ ticksPerWeek, tokensPerTick, weeklyBudgetTokens }` — the linter computes `ticksPerWeek × tokensPerTick` when the direct projection is absent. `capFraction` defaults to 0.057 (the ARCHITECTURE.md prose anchor); the constant is locked by a paired test so a silent edit surfaces loudly. Boundary semantics: the cap is *inclusive* — a config sitting at exactly 5.7 % passes (Beyer SRE 2016 Ch. 3 — "you have used X % of your budget" is not a violation until X *exceeds* it). Dormant state (rule #7 — graceful degrade): missing `config/mape-k.json` exits 0 with a stderr advisory and re-arms the moment the artefact lands — same precedent as `check-skill-rule-cap.mjs`'s retired-Skill terminal state. Pivot (rule #9): if the 5.7 % cap moves to a per-tier adaptive threshold (per `mape-k-loop`'s monthly self-calibration, research.md § "MAPE-K cadence"), retire this lint and replace with one that reads the calibrated value from the `mape-k-loop` Knowledge log. |
| 59 | `scripts/check-tick-loop-backoff-schedule.mjs` + `scripts/check-mape-k-tick-iteration-backstop.mjs` + `scripts/check-mape-k-watchdog-cadence.mjs` + `scripts/check-cadence-pivot-threshold.mjs` (4 dormant CI lints over prose-only numeric anchors — `[5, 30, 300]` tick-loop backoff schedule, 1000-tick MAPE-K backstop, 12 h MAPE-K watchdog cadence, 8 % cadence-pivot threshold) | Deterministic CI gate family over prose-only numeric invariants; each is a pure decision function over `{ config, expected }` with the dormant-on-missing-config short-circuit in the CLI — same shape as rows 55 / 56 (`mape-k-budget-cap` / `mape-k-constraints-md-size`) | rule #10 (vision.md § 10 — deterministic enforcement; ratchet rule: every prose-only invariant in ARCHITECTURE.md / vision.md / research.md gets a deterministic linter as soon as the artefact it guards becomes machine-readable); ARCHITECTURE.md L215 (`5s → 30s → 5min` tick-loop backoff prose anchor) and L218 (1000-tick backstop + 12 h watchdog prose anchors); research.md L92 (8 % cadence-pivot threshold prose anchor: "If measured spend exceeds 8 % for 4 weeks, the pivot in `mape-k-cadence`'s rule-#9 block fires"); Beyer, Jones, Petoff, Murphy (eds.), *Site Reliability Engineering*, O'Reilly 2016, Ch. 3 (error-budget enforcement — exceed-the-budget triggers a mechanical response, not a discussion); Armstrong, *Programming Erlang*, Pragmatic Bookshelf 2007 (let-it-crash + supervised restart with backoff — the tick-loop ladder's anchor); Liu, *Real-Time Systems*, Prentice Hall 2000 § 6.4 (activity-coupled sampling bound — the tick-iteration backstop's anchor); Ries, *The Lean Startup*, Crown Business 2011 (pivot-or-persevere — the cadence-pivot threshold's anchor); Beck, *Extreme Programming Explained*, 1999 (CI as the constraint enforcer) | full | Each lint is a pure decision function over `{ config, expected }` returning `{ ok, reason? }`; CLI is the I/O boundary (read `config/<file>.json`, run the check, exit). Tick-loop reads `config/tick-loop.json`; the three MAPE-K-related lints all read `config/mape-k.json` (separate jobs, narrow assertions) — the cadence-pivot threshold colocates here per its TASKS.md "Files" brief, not in a separate `config/cadence.json`, because it is a property of the MAPE-K cadence. Each accepts two field-name shapes (e.g., `backoff_schedule` OR `backoff_schedule_seconds`; `tick_iteration_backstop` OR `tick_iteration_backstop_ticks`; `watchdog_hours` OR `watchdog_period_hours`; `cadence_pivot_threshold_pct` OR `cadence_pivot_spend_fraction`) so the artefact author can pick whichever shape reads best — the strict equality check (or fraction-percent normalisation, for the cadence-pivot lint) is the gate, not the field name. Default expected values are locked by paired tests so a silent edit to a constant surfaces loudly (rule-#10 ratchet — drift requires a Pivot, not a one-line PR). Dormant state (rule #7 — graceful degrade): missing `config/<file>.json` exits 0 with a stderr advisory and re-arms the moment the artefact lands — same precedent as `check-mape-k-budget-cap.mjs` and `check-skill-rule-cap.mjs`'s retired-Skill terminal state. Pivot (rule #9, per-lint): if the prose anchor itself moves before the corresponding config artefact lands (e.g., the fixed `5s → 30s → 5min` ladder is replaced by exponential backoff, or the 12 h watchdog is replaced by adaptive cadence per `mape-k-loop`'s monthly self-calibration extending T to 18 h), retire that specific lint and write a new one against the replacement anchor. |
| 60 | `novel/adapters/token-monitor/src/maciek.ts` (`MaciekTokenMonitor` Strategy — real `TokenMonitor` implementation that derives a deterministic `TokenSnapshot` by re-implementing Maciek's `claude-monitor` parser shape directly against `~/.claude/projects/<cwd>/<session>.jsonl`, bypassing Maciek's CLI because `claude-monitor==3.1.0` has no `--json` mode) | Strategy (behavioral) of `TokenMonitor` (Gamma 1994) + recursive-descent line-by-line JSON parser (Aho-Sethi-Ullman 1986) + 5h windowed-aggregation (`SessionBlock` rounding rule mirrored from Maciek's `data/analyzer.py`); plan-cap subtraction (Beyer SRE 2016 Ch. 3 — error-budget framing applied to tokens) | Gamma, Helm, Johnson, Vlissides, *Design Patterns*, Addison-Wesley, 1994 (Adapter / Strategy); Aho, Sethi, Ullman, *Compilers: Principles, Techniques, and Tools*, Addison-Wesley, 1986 (recursive-descent parser shape); Beyer, Jones, Petoff, Murphy (eds.), *Site Reliability Engineering*, O'Reilly 2016, Ch. 3 (error-budget framing — token economy is the budget contract); Meszaros, *xUnit Test Patterns*, Addison-Wesley, 2007 (real-implementation contract test against committed fixtures); rule #2 (vision.md § 2 — every dep behind interface; the adapter wraps the JSONL on-disk format Anthropic writes); rule #1 (vision.md § 1 — push upstream first; `weeklyHeadroomFraction` is `0` in v0 because Maciek's P90 ML predictor is not exposed without their CLI, and a `--json` mode is the upstream feature request); upstream reference: `claude_monitor/data/reader.py` (recursive `.jsonl` glob), `claude_monitor/core/data_processors.py` (`TokenExtractor.extract_tokens` summing `input_tokens + output_tokens + cache_creation_input_tokens + cache_read_input_tokens`), `claude_monitor/data/analyzer.py` (5h `SessionBlock` rounding rule), `claude_monitor/core/plans.py` (`PLAN_LIMITS = { pro: 19_000, max5: 88_000, max20: 220_000, custom: 44_000 }`) | full | Constructor opts: `{ configDir: string, now?: () => Date, plan?: 'pro' \| 'max5' \| 'max20' \| 'custom' }`; `now` defaults to `() => new Date()`, `plan` defaults to `'max5'` (the most common Anthropic tier). `PLAN_CAPS` is exported as a named constant (rule #4 — every magic number named) and locked by paired tests. `snapshot()` rglobs `<configDir>/projects/**/*.jsonl`, stream-parses line-by-line with `JSON.parse` (skip blanks; tolerate malformed JSON per rule #7 graceful-degrade), filters to entries with `message.usage` and positive token sum, dedups by `(message.id, requestId)` (Maciek's rule when the same message appears in two files — resumed sessions write a fresh JSONL), groups sorted entries into 5h SessionBlocks (`block.start = floor(first-entry.ts, hour)` UTC; new block when `entry.ts >= block.end_time` OR `gap-since-last-entry >= 5h`), picks the active block (`now ∈ [start, end)`), sums `input + output + cache_creation + cache_read`, returns `{ tokensRemainingInWindow: max(0, planCap − used), windowSizeTokens: planCap, secondsUntilWindowReset, weeklyHeadroomFraction: 0, observedAt }`. Cold-start path (missing `configDir`, no JSONL files, all-malformed) returns the full plan cap as remaining. **Path correction (rule #9 pivot fired)**: prior brief said `~/.config/claude/`; that env-var path (`CLAUDE_CONFIG_DIR`) only governs Maciek's *own* settings cache. The actual data source is `~/.claude/projects/<cwd>/<session>.jsonl` — Claude Code writes one JSONL file per session (filename = session UUID), grouped under one directory per cwd (directory name = cwd path with `/` → `-`). 5 committed synthetic fixtures (`single-block`, `two-blocks`, `empty`, `malformed`, `dedup`) under `novel/adapters/token-monitor/test/fixtures/<scenario>/projects/-fixture-cwd/`; each scenario carries a sibling `expected.json` documenting the expected snapshot. **No real `~/.claude/projects/` data is committed.** |
| 61 | `scripts/omc-roundtrip.mjs` (round-trip parseability check for OMC handoff JSON — pure `roundTripOmcTask(taskJson)` + thin `--omc-checkout=<path>` CLI; closes the `research-omc-handoff-persistence` task's verification cell which read "if 'parseable', `node scripts/omc-roundtrip.mjs --omc-checkout=<path>` exits 0") | Round-trip parsing as a parseability test — `parse(serialize(parse(s))) ≡ parse(s)` modulo whitespace, tightened in v0 to byte-equal modulo whitespace because OMC's serializer is `JSON.stringify(updated, null, 2)` (deterministic for a given parsed value); pure decision function over a string + thin CLI at the I/O boundary, with the dormant-on-missing-checkout short-circuit shape mirrored from `scripts/check-mape-k-budget-cap.mjs` | Aho, Sethi, Ullman, *Compilers: Principles, Techniques, and Tools*, Addison-Wesley, 1986 (round-trip property as the parseability test — the load-bearing anchor); rule #2 (vision.md § 2 — every dep behind interface; the OMC bridge is exactly that, and the parseability test gates the interface contract); rule #7 (vision.md § 7 — graceful-degrade dormant short-circuit when no `--omc-checkout=` is supplied OR no `.omc/state/team/` subdir exists); rule #9 (vision.md § 9 — pre-registered Pivot: a non-whitespace divergence on any sample disproves the parseable-thin-reader hypothesis); rule #10 (vision.md § 10 — deterministic enforcement; the research task's prose Acceptance ("if parseable, round-trip exits 0") now has a machine-readable counterpart); research.md § "OMC handoff persistence" (the parseable verdict + the canonical write site `src/team/state/tasks.ts:90` — `writeAtomic(path, JSON.stringify(updated, null, 2))`); Meszaros, *xUnit Test Patterns*, Addison-Wesley, 2007 (paired positive/negative fixtures in the test file) | full | Pure functions `roundTripOmcTask(taskJson) => { ok, diff? }`, `findOmcTaskFiles(omcCheckout) => string[]`, and `parseOmcCheckoutFlag(argv) => string \| null` are referentially transparent — no I/O, no shared state. The CLI (`main()`) is the I/O boundary: parses `--omc-checkout=<path>`, walks `<path>/.omc/state/team/<teamName>/tasks/*.json`, runs `roundTripOmcTask` on each. Whitespace normalisation strips per-line trailing whitespace and collapses trailing newlines so the `JSON.stringify-indent-2` output is comparable to the OMC on-disk bytes; leading whitespace (indentation) is *not* stripped — a non-canonical indent (e.g., 4-space) is a structural fail (the load-bearing pivot trigger). Dormant state (rule #7): missing `--omc-checkout=` OR `<path>/.omc/state/team/` not a directory → exit 0 with stderr advisory; same precedent as `check-mape-k-budget-cap.mjs`'s dormant-config short-circuit. Test coverage (12 cases in `scripts/omc-roundtrip.test.mjs`): synthetic task round-trips cleanly, whitespace-only trailing diff is OK, non-canonical indent fails with a divergence diff, invalid JSON fails with parse-error diff, non-object top-level fails with shape-error diff, non-string input fails fast (rule-#6 let-it-crash on caller contract violation), `findOmcTaskFiles` walks multi-team directories + ignores non-JSON neighbours + returns empty on missing dir, `parseOmcCheckoutFlag` handles absent / present / empty-value cases. Synthetic fixtures only — no real `~/.claude/` or third-party data committed. |
| 62 | `novel/bridges/omc-tasksmd/` (`@minsky/omc-tasksmd-bridge` v0 — read-only OMC team-task JSON → tasks.md projection: pure `mapOmcToTasksMd(task)` mapper + pure `syncOmcToTasksMd({ omcTasks, existingTasksMd, mode: 'replace-section' })` section-replace + thin `OmcReader.list({ repoRoot, teamName? })` filesystem walker; closes `omc-tasksmd-bridge-v0`'s "Round-trip preserves all task fields" Acceptance through the documented lossy projection — the v0 surface field-mapping is in `novel/bridges/omc-tasksmd/README.md` § "Lossy projection") + `novel/bridges/README.md` (namespace home, intentionally separate from `novel/adapters/` because a *bridge* is a peer-to-peer format pair we own, not an external dep behind an interface) | Read-only adapter bridge between two task-list formats — Helland 2007 (eventual consistency, only the read direction in v0 because OMC's optimistic-concurrency `version` field needs a CRDT story before reverse-sync is safe — filed as `omc-tasksmd-bridge-v1-watcher` in TASKS.md); Hewitt 1973 actor model (TASKS.md is the message store, each emitted block is one message addressed to whoever picks it next); pure-function projection + section-replace idempotency (Knuth 1984 — referential transparency means re-running yields byte-equal output, the load-bearing property for a bridge that may run on a v1+ watch loop); the read-side I/O boundary is a single `fs/promises.readdir` + `readFile` pair (Martin 2017 *Clean Architecture* — I/O at the edge, mapper + sync are pure) | Helland, P., "Life beyond Distributed Transactions: an Apostate's Opinion", *CIDR* 2007 (the load-bearing pattern: a bridge between two stores converges through eventual consistency; v0 ships only the read direction because that's the safe half until the write-side has a CRDT story); Hewitt, Bishop, Steiger, "A Universal Modular Actor Formalism for Artificial Intelligence", *IJCAI* 1973 (TASKS.md as the message store; each emitted block is one message); Gamma, Helm, Johnson, Vlissides, *Design Patterns*, Addison-Wesley, 1994 (Adapter / Bridge — the namespace-level pattern, distinct from rule #2's *dependency* adapters); Aho, Sethi, Ullman, *Compilers: Principles, Techniques, and Tools*, Addison-Wesley, 1986 (parser shape — `JSON.parse` is the only parser; the format is the round-trip-parseable verdict from `scripts/omc-roundtrip.mjs` per row 61); Martin, R. C., *Clean Architecture*, Pearson, 2017 (I/O at the edge — `OmcReader.list` is the only filesystem touch; mapper + sync are pure); Munafò et al., *Nature Human Behaviour* 1, 0021, 2017 (rule-#9 pre-registration — the lossy-projection pivot was declared in `EXPERIMENT.yaml` *before* code so a `metadata: { … }` field that doesn't fit the documented projection cannot retroactively redefine "lossless"; v0 ships *with* the lossy note rather than expanding scope); rule #2 (vision.md § 2 — every dep behind interface; OMC's task store is exactly that, behind `OmcReader.list`); rule #6 (vision.md § 6 — let-it-crash; the v1+ `merge-by-id` mode throws with a `v1+` message so the caller decides next step rather than the bridge silently doing the wrong thing); rule #7 (vision.md § 7 — graceful-degrade on missing `.omc/state/team/`, on malformed JSON, and on unknown `teamName` filter); research.md § "OMC handoff persistence" (the parseable verdict that unblocked v0); TASKS.md `omc-tasksmd-bridge-v1-watcher` (the next-tier follow-up: chokidar / `fs.watch` + reverse-sync once the CRDT story lands) | partial | **Lossy-projection v0 (declared deviation, pre-registered).** v0 surfaces the documented subset — `id`, `subject`, `description`, `status`, `owner ?? claim.owner`, `blocked_by` (comma-space-joined), `created_at`, `version` — and drops `claim.token`, `claim.leased_until`, `blocks`, `depends_on`, `result`, `error`, `requires_code_change`, `metadata`. The drop is *intentional* (the OMC fields are out of v0 scope; surfacing them in tasks.md would require either widening the tasks.md spec or shipping a second projection mode), and is the load-bearing reason conformance is `partial` rather than `full`. The pivot was pre-declared in `EXPERIMENT.yaml`'s `pivot:` block — "if any field can't be losslessly mapped, document the lossy projection in `novel/bridges/omc-tasksmd/README.md` and ship anyway with a 'lossy v0' note" — so the deviation cannot be retroactively explained away (Munafò 2017 pre-registration). 17 paired tests under `novel/bridges/omc-tasksmd/src/{reader,mapper,sync}.test.ts` cover: cold-start (missing `.omc/state/team/`), malformed JSON skip with stderr advisory, unknown `teamName` filter, multi-team walk (alphabetical), minimum-fields and full-record mapping, `[x]` vs `[ ]` checkbox, `owner ?? claim.owner` precedence, idempotent re-run (byte-equal output), preservation of non-OMC sections, replace-in-place over a stale section (no duplicates), empty-list section, and the `merge-by-id` v1+ throw. Synthetic fixtures only under `novel/bridges/omc-tasksmd/test/fixtures/` — `task-001.json` (full record with claim + metadata), `task-002.json` (completed status + completed_at), `malformed.json` (intentionally invalid JSON for the rule-#7 graceful-skip chaos test, biome-excluded so the linter doesn't try to parse it), and `other-team/task-otr-001.json` (multi-team walk fixture). **No real OMC state from a live install is committed.** Cognitive complexity ≤10 enforced by biome (no function exceeds 8). All exported functions/methods carry `@otel bridges.omc-tasksmd.<verb>` JSDoc per rule #4; helper / type-guard / coercion helpers are `@otel-exempt` with reason. Try/catch depth ≤1 per rule #6, with three `// rule-6: handled-locally — graceful-skip …` opt-outs at the file-read / JSON-parse / readDir boundaries. The reader is the *only* I/O boundary; mapper + sync are pure (no clock dependency, no shared state). v1+ direction (`omc-tasksmd-bridge-v1-watcher`, P3 in TASKS.md): chokidar / `fs.watch` reverse-sync after a CRDT story for OMC's `version` field. |
| 66 | `distribution/install-openobserve.sh` + `distribution/openobserve/{com.openobserve.daemon.plist, openobserve.service, README.md}` + `OtelObservability.endpoint` opt in `novel/adapters/observability/src/otel.ts` + `OpenObserveStrategy` (`fetchOpenObserveSnapshot` / `queryOpenObservePromql` / `parsePromqlInstantResponse` / `openObserveGetValue`) in `novel/dashboard-web/src/strategy.ts` + `OBSERVABILITY_BACKEND=openobserve` wiring in `novel/dashboard-web/src/start.ts` + gated integration test `novel/adapters/observability/test/openobserve.integration.test.ts` (env-gated on `OPENOBSERVE_INTEGRATION=1`) — closes `observability-backend-deploy` (P0) by deploying OpenObserve as Minsky's v0 OTLP receiver and threading the live PromQL read path into the dashboard's existing Strategy seam (PR #84's `getValue`). The install script is a thin shell wrapper — no binary bundled in-tree (size + license); pinned at `v0.80.1` with platform-detection for `darwin-arm64` / `darwin-amd64` / `linux-amd64` / `linux-arm64`. | Deployed observability backend (OpenObserve as the chosen v0 OTLP receiver per `research.md` § "Lighter OTEL backend") + per-signal endpoint routing for the OTEL adapter (Adapter / Strategy, Gamma 1994) + read-only HTTP Strategy over Prometheus's instant-query API (the dashboard never POSTs to OpenObserve — write side is OTLP-only via `@minsky/observability`); supervision pattern is OTP "permanent" restart (Armstrong 2007), since the OTLP receiver should always be up — `KeepAlive=true` (launchd) ≅ `Restart=always` (systemd) | research.md § "Lighter OTEL backend" (the load-bearing anchor — the v0 backend selection PR #43, 2026-05-03, named OpenObserve as the choice over VictoriaMetrics + the SQLite-bespoke option); OpenTelemetry specification, CNCF (2020+) (the OTLP HTTP receiver contract OpenObserve implements — three-signal traces / metrics / logs); Prometheus HTTP API (the read-side query contract OpenObserve mirrors — `status: "success" \| "error"`, `data.resultType` ∈ `{scalar, vector, matrix}`, `data.result[].value = [ts, "value"]`); Armstrong, J., *Programming Erlang*, Pragmatic Bookshelf, 2007 (OTP supervision — the OTLP receiver is "permanent" restart, mirroring `minsky-budget-guard.service`'s policy); Gamma, Helm, Johnson, Vlissides, *Design Patterns*, Addison-Wesley, 1994 (Adapter / Strategy — `OpenObserveStrategy` is one implementation behind the `GetValue` seam shipped by `dashboard-web-otel-wiring`); rule #2 (vision.md § 2 — every dep behind interface; OpenObserve is behind `OpenObserveStrategy`, the OTLP exporter is behind `OtelObservability`); rule #4 (vision.md § 4 — every measurement queryable; the PromQL endpoint is the queryability surface — `loop-uptime`, `tokens-per-story`, `mttr`, `wrist-dwell`, `token-budget-honoring` all become live readings instead of `(stub)`); rule #7 (vision.md § 7 — graceful-degrade on backend down: `OpenObserveStrategy` returns `null` per row → `(stub)` rendered; the dashboard never crashes because OpenObserve is); rule #9 (vision.md § 9 — pre-registered pivot in `EXPERIMENT.yaml` to VictoriaMetrics triad if OpenObserve install proves unreliable); rule #10 (vision.md § 10 — deterministic enforcement; the install script's shape is verified by `bash -n` + executable-bit check, not by running it in CI); TASKS.md `observability-backend-deploy` (the parent P0 task this row closes); rows 24 + 56–58 + 65 (the parent observability-adapter / dashboard-web / shortcuts pattern this extends with a deployed backend) | full | **No binary bundled in-tree** — `install-openobserve.sh` downloads OpenObserve `v0.80.1` (pinned) from the upstream GitHub release URL at install time. Platform detection covers `darwin-arm64` / `darwin-amd64` / `linux-amd64` / `linux-arm64`; unsupported platform exits non-zero with a clear error. Binary lands at `${HOME}/.local/bin/openobserve`; data dir at `${HOME}/.openobserve/data`. The launchd plist + systemd unit both use `${HOME}` as their only placeholder (consistent with `distribution/launchd/`'s `${MINSKY_HOME}` precedent — but OpenObserve's daemon doesn't need `MINSKY_HOME` because it's a standalone receiver). **Read-only Strategy by construction** — `queryOpenObservePromql` only issues HTTP GETs against `/api/<org>/prometheus/api/v1/query`; there is no POST / PUT / DELETE code path in the dashboard side. The OTLP write side is handled by `@minsky/observability` via the official OTLP HTTP exporters, never by the dashboard. **18 paired Strategy tests** in `novel/dashboard-web/test/strategy.test.ts` cover: snapshot lookup (existing — 3), constant lookup (existing — 1), Prometheus instant-query parser (5: scalar, vector, error status, malformed JSON, empty vector), `queryOpenObservePromql` (5: GET-shape, basic auth, non-2xx, network failure, trailing-slash normalisation), `fetchOpenObserveSnapshot` (3: fan-out, partial failure, format hook), `openObserveGetValue` end-to-end (1: async pre-fetch + sync render-time lookup). **9 OTEL adapter tests** in `novel/adapters/observability/src/otel.test.ts` cover: existing self-test (5), `endpoint` opt flow (1) + trailing-slash normalisation (1) + undefined-endpoint env-fallback (1) + constructor-doesn't-throw with endpoint (1). **Integration test gated** on `OPENOBSERVE_INTEGRATION=1` env var — skipped in CI by default; runs locally when the user has installed OpenObserve. **Install script syntax-validated** via `bash -n distribution/install-openobserve.sh` (the load-bearing CI gate per rule #10 — no actual install runs in CI). All exported functions carry `@otel <verb>` JSDoc per rule #4; pure helpers (`stripTrailingSlash`, `btoaCompat`, `parsePromqlSuccessData`, `extractScalarValue`, `extractVectorFirstValue`, `resolveOtlpEndpoints`) are `@otel-exempt` with reason. Cognitive complexity ≤10 enforced by biome (the parser is split into 4 helper functions). Try/catch depth ≤1 per rule #6, with two `// rule-6: handled-locally — graceful-degrade …` opt-outs at the network and JSON-parse boundaries. **Resilience-scout follow-ups (≤2 per the scout cap)**: (a) the metric *names* in `OPENOBSERVE_PROMQL_BY_METRIC_ID` (`token_count`, `supervisor_restart_to_claim_latency_seconds`, `http_get_total{path="/watch.json"}`, `claude_code_api_errors_total`) are the exact strings vision.md publishes — if `@minsky/observability` ever emits under different names, the mismatch is filed as `otel-emission-name-audit` (P3 follow-up); (b) the OpenObserve binary URL pattern (`releases/download/${VERSION}/openobserve-${VERSION}-${target}.tar.gz`) may change — if the quarterly review fires the pivot, fall back to VictoriaMetrics triad per `research.md` PR #43 (the runner-up, MetricsQL is a near-superset of PromQL — migration cost is low). |
| 73 | `experiments/` directory + `scripts/check-no-singleton-experiment.mjs` (`experiments-directory-migration` — moves the rule-#9 pre-registration substrate from a singleton `EXPERIMENT.yaml` at the repo root to plural `experiments/<id>.yaml` files, one per pre-registered experiment; backwards-compatible at the per-file CLI boundary — every lint still accepts an explicit path; new directory-walker default for the three rule-#9 lints; a one-way ratchet `check-no-singleton-experiment.mjs` asserts the singleton stays gone after the migration; CI workflows `ci.yml` + `experiment.yml` updated to walk the directory) | Pre-registration record (Munafò et al. 2017 — one record per pre-registration, the singular shape AsPredicted enforces) + parametric path root (Hewitt 1973 — the directory is the substrate; making the root parametric via the lint's CLI argument lets the same lint run against minsky's own `experiments/` and against a host's `${MINSKY_HOST_ROOT}/.minsky/experiments/` once `host-root-resolver-prep` lands) + one-way ratchet (rule #10 — when an advisory or transitional state is replaced by a new structural answer, the old state cannot quietly come back); concurrent-experiments support (Fagerholm et al. RCoSE 2014 — the per-change runner must support multiple A/B tests in flight) | Munafò, M. R., Nosek, B. A., Bishop, D. V. M., et al., "A manifesto for reproducible science", *Nature Human Behaviour* 1, 0021, 2017 (the singular-record-per-pre-registration shape — `experiments/<id>.yaml` matches the AsPredicted source pattern; the singleton was a Minsky-coined deviation this migration retires); Fagerholm, F., Guinea, A. S., Mäenpää, H., Münch, J., "Building Blocks for Continuous Experimentation", *RCoSE* 2014 (the per-change experiment runner is the first building block — and it must support plural concurrent experiments per the paper's "multiple A/B tests in flight" design); Beck, K., *Extreme Programming Explained*, Addison-Wesley, 1999, Ch. 17 (CI as the constraint enforcer — the directory-walker is the smallest deterministic substrate that supports plural experiments); Hewitt, C., "A Universal Modular ACTOR Formalism for Artificial Intelligence", *IJCAI* 1973 (parametric path root — the substrate boundary is the actor's interface; making the experiments root parametric is the precondition for cross-repo support landing in `host-root-resolver-prep`); rule #1 (vision.md § 1 — don't reinvent: the data layer at `experiment-store/<id>.jsonl` is already plural; the migration matches the pre-registration layer to it, not building a new substrate); rule #9 (vision.md § 9 — the migration itself is pre-registered in `experiments/experiments-directory-migration.yaml`; the eat-own-dogfood test); rule #10 (vision.md § 10 — deterministic enforcement; the singleton-ratchet `check-no-singleton-experiment.mjs` is the structural one-way guarantee that the singleton stays gone); TASKS.md `experiments-directory-migration` (the parent task this row closes) + `cross-repo-runner-v0` / `host-root-resolver-prep` (the consumers — both are now unblocked because the per-task substrate is plural); user-stories/006-runner-on-any-repo.md (the cross-repo runner's umbrella experiment record graduates to `experiments/006-runner-on-any-repo.yaml` per this migration). | full | **Backward-compatible at the file CLI; new directory default at the no-arg CLI.** The three rule-#9 lints (`check-anchor-primary-source.mjs`, `check-measurement-inspects-output.mjs`, `check-pivot-success-margin.mjs`) each export a new `mainDirectory(directoryPath)` that walks `experiments/*.yaml` (and `*.yml`) sorted alphabetically, runs the per-file `main(file)` on each, and aggregates exit codes (max wins — any single non-zero fails the run). The CLI default switches from `EXPERIMENT.yaml` to `experiments/`; explicit-path invocations (`node scripts/check-X.mjs path/to/file.yaml`) still work for explicit single-file linting. The new ratchet `scripts/check-no-singleton-experiment.mjs` is a pure check (no LLM, no opt-out): exits 1 if `EXPERIMENT.yaml` exists at the repo root, 0 otherwise. The CI workflow `ci.yml` adds a `no-singleton-experiment` job following the `skill-rule-cap` pattern (runs on every event; required check via `needs:`); existing lint job invocations drop the explicit `EXPERIMENT.yaml` arg so they default to walking the directory. The `experiment.yml` workflow's `gate` and `record` jobs now loop over `experiments/*.yaml` (and a defensive `EXPERIMENT.yaml` if the singleton sneaks back, surfacing the regression at the gate before the ratchet). 18 paired tests ship with the migration: 5 walker tests per lint × 3 lints (15) plus 3 ratchet tests = 18 new vitest cases on top of the pre-existing suites. The migrated `experiments/readme-global-install-claim-fix-2026-05-04.yaml` is the first plural file; `experiments/experiments-directory-migration.yaml` is the migration's own pre-registration (the eat-own-dogfood test). The pivot path — per-experiment-id PR-diff scope when a single bad file gates ≥3 unrelated PRs over 14 days — is recorded on the migration's own EXPERIMENT.yaml so the walker's blast-radius failure mode has a structural escape valve. The `novel/experiment-record/spec.md` § "File location" is rewritten to document the plural shape; the singleton mention is replaced. Cross-repo runs (cross-repo-runner-v0, blocked-by this) consume this substrate as `${MINSKY_HOST_ROOT}/.minsky/experiments/<id>.yaml` once `host-root-resolver-prep` parametrises the lint's root. |
| 74 | `scripts/lib/host-root.mjs` + `scripts/lib/host-root.test.mjs` + parametric `MINSKY_HOST_ROOT` wiring in `scripts/check-anchor-primary-source.mjs` + `scripts/check-measurement-inspects-output.mjs` + `scripts/check-pivot-success-margin.mjs` (`host-root-resolver-prep` — preparation PR per rule #9 that introduces `getHostRoot(repoRoot)` reading `MINSKY_HOST_ROOT` from the environment; the three rule-#9 substrate lints now resolve their `experiments/` root through the helper; behaviour-preserving for minsky-on-itself — the helper returns `repoRoot` when the env var is unset; cross-repo invocations point at a host's `.minsky/` sidecar where the layout mirrors the minsky repo root) | Parametric path root (Hewitt 1973 — the substrate boundary is the actor's interface; making the experiments root parametric via an env var lets the same lint live in many filesystems) + dependency injection at the edge (Martin 2017 — the helper is a one-line override; consumers call once at module init; the rest of each script is unchanged) + preparation-PR pattern (rule #9 — when the metric source needs to exist before the artefact that depends on it can be built, ship the substrate first) | Hewitt, C., "A Universal Modular ACTOR Formalism for Artificial Intelligence", *IJCAI* 1973 (the substrate boundary is the actor's interface; making it parametric is the precondition for cross-repo lint reuse); Martin, R. C., *Clean Architecture*, Pearson, 2017 (DI at the edge — `getHostRoot` is the substrate-root injection point, the only env-var read in the lint); Helland, P., "Life beyond Distributed Transactions: an Apostate's Opinion", *CIDR* 2007 (the substrate-locality boundary defines the consistency boundary; making it explicit is the precondition for cross-system semantics); rule #2 (vision.md § 2 — every external dep through an interface; here the "external dep" is the *filesystem root*, made explicit via the helper); rule #9 (vision.md § 9 — preparation-PR pattern: the helper lands before the cross-repo runner that consumes it, so the runner can ship as a *small* PR rather than co-shipping the substrate refactor); rule #10 (vision.md § 10 — deterministic enforcement; the helper is a pure function over `process.env.MINSKY_HOST_ROOT` + the supplied `repoRoot`, no LLM in the chain); user-stories/006-runner-on-any-repo.md (the umbrella story this helper enables); docs/cross-repo-portability.md (the operative classification — the three rule-#9 lints port into the cross-repo-portable bucket via this helper); rows 71 + 73 (the experiments-directory-migration this helper extends — same substrate, parametric root); TASKS.md `host-root-resolver-prep` (the parent task this row closes) + `minsky-sidecar-bootstrap` / `cross-repo-runner-v0` (the consumers — both unblocked once this row ships). | full | **Behaviour-preserving for minsky-on-itself; new cross-repo path is opt-in via env.** `getHostRoot(repoRoot, envOverride?)` reads `process.env.MINSKY_HOST_ROOT` (or the optional injected env for tests). Returns the env value when set + non-empty (resolved to absolute), otherwise the supplied `repoRoot` (also resolved). 8 paired tests in `scripts/lib/host-root.test.mjs` cover: unset → repo, empty → repo, set → override, relative override resolution, relative repo fallback resolution, env wins over repo, default `process.env` lookup, defensive type-guard for malformed envs. The three rule-#9 substrate lints (`anchor-primary-source` / `measurement-inspects-output` / `pivot-success-margin`) each replace their `resolve(REPO_ROOT, "experiments")` with `resolve(getHostRoot(REPO_ROOT), "experiments")`. Smoke-tested cross-repo: `MINSKY_HOST_ROOT=/tmp/<fixture> node scripts/check-X.mjs` walks the fixture's `experiments/` and exits 0 when the records are valid; same shell command with the env var unset walks minsky's own `experiments/` (the existing two records). 372 vitest cases pass (8 new + the existing 364). Typecheck clean. Biome clean. The helper is the smallest possible carve-out — three lines of substantive logic — so the consumer-side change in each lint is one import + one constant rebinding. The remaining 9 rule lints (rule-1, rule-2, rule-3, rule-4, rule-5, rule-7, pattern-index, skill-rule-cap, mape-k-* family) deliberately do NOT take the env var in this PR — they ship cross-repo only when the host's source taxonomy enters the constitutional gate (per `docs/cross-repo-portability.md`'s classification, that's deferred to a follow-up step or skipped by-design for rule-1). The deferred lints are not blockers for the cross-repo runner v0 because their substrate is host-source-tree, not host-experiments. Pivot path: if the env var proves brittle in a host repo (e.g., the operator's shell strips env vars between minsky CLI invocation and child-process spawn), pivot to a CLI flag (`--host-root <path>`) on each lint as a v1 follow-up; the helper's signature already accepts an `envOverride` parameter, so the CLI wrapper can synthesize an env from the flag without touching the helper. |
| 75 | `novel/sidecar-bootstrap/` (`@minsky/sidecar-bootstrap` v0 — `minsky bootstrap <host-dir>` CLI that materialises a per-host gitignored `.minsky/` sidecar so the cross-repo-runner can govern any host repo; pure planner / inferer / diagnoser / schema in `src/` + thin CLI executor in `bin/minsky-bootstrap.mjs`; idempotent re-runs, `--doctor` self-test with green/yellow/red Avizienis lattice, `--repair` mode; mkdir-based concurrency lock at `.minsky/.bootstrap.lock.d` exits 75 on race) | Command pattern (Gamma 1994 — actions are inspectable data the executor walks) + pure-function-with-I/O-at-edge (Martin 2017 — planner / inferer / doctor are referentially transparent; CLI is the only side-effecting layer) + status lattice (Avizienis et al., *IEEE TDSC* 1 (1), 2004 — `aggregateStatus` red dominates yellow dominates green) + idempotent-bootstrap-via-lock (Armstrong 2007 — `setup.sh`'s mkdir-based lock idiom; second invocation exits EX_TEMPFAIL) | Gamma, Helm, Johnson, Vlissides, *Design Patterns*, Addison-Wesley, 1994 (Command pattern — `BootstrapAction` is a discriminated union the executor switches on; testable as data without I/O); Martin, R. C., *Clean Architecture*, Pearson, 2017 (DI at the edge — the `dist/` exports are pure; the CLI binds to `node:fs` once at the top level); Avizienis, A., Laprie, J.-C., Randell, B., Landwehr, C., "Basic Concepts and Taxonomy of Dependable and Secure Computing", *IEEE Transactions on Dependable and Secure Computing* 1 (1), 2004 (the green/yellow/red lattice of `diagnose()` mirrors the existing `@minsky/adapter-types` `aggregateStatus()`; red dominates yellow dominates green); Armstrong, J., *Programming Erlang*, Pragmatic Bookshelf, 2007 (idempotent supervisor restart — the bootstrap is the host's "start" verb; second invocation must be a no-op, third with `--repair` must restore invariants; the mkdir-based lock at `.bootstrap.lock.d` mirrors `setup.sh`'s atomic-claim idiom and surfaces concurrent races as EX_TEMPFAIL=75 per BSD sysexits.h); Hunt & Thomas, *The Pragmatic Programmer*, Addison-Wesley, 1999, Tip 8 ("Make It Easy to Reuse" — inference reads existing host signals like `package.json` + `.git/config` rather than re-eliciting); rule #1 (vision.md § 1 — don't reinvent: the bootstrap reuses the host's signals, the YAML is hand-rendered to avoid a yaml dep, the CLI reuses the existing `setup.sh` lock idiom); rule #2 (vision.md § 2 — every external dep through an interface; the host's filesystem layout IS the dep, made explicit via the `RepoConfig` schema); rule #6 (vision.md § 6 — let-it-crash AT the boundary; the planner returns the action list, the executor handles per-action errors with operator-actionable messages, no silent failures); rule #7 (vision.md § 7 — failure-mode discipline; the README's chaos table has 7 rows including bad-input / host-not-fully-onboarded / concurrency / host-config-drift / filesystem-permission / upstream-malformed / external-cleanup); rule #9 (vision.md § 9 — pre-registered pivot for the global-ignore mechanism: if `~/.config/git/ignore` proves unwritable on enough hosts, fall back to per-clone `.git/info/exclude`; this pivot is wired into the `RepoConfig.ignore_mechanism` field's three-value enum); rule #10 (vision.md § 10 — deterministic enforcement; the bootstrap's behaviour is fully deterministic given typed inputs and a host filesystem state); user-stories/006-runner-on-any-repo.md (the umbrella story this package enables); rows 71 (experiments-directory-migration substrate) + 74 (host-root-resolver — the env var the bootstrap's sidecar plugs into); TASKS.md `minsky-sidecar-bootstrap` (the parent task this row closes) + `cross-repo-runner-v0` (the immediate downstream consumer that reads `.minsky/repo.yaml`). | full | **No real host filesystem in CI; all I/O at the CLI edge.** 51 paired vitest cases across `src/{schema,inference,plan,doctor}.test.ts` cover: schema (12 tests — happy path × 6, validation failures × 6 covering missing required fields, empty strings, non-string types, invalid enums); inference (15 tests — defaults, git-remote URL parsing for SSH / HTTPS / no-suffix / malformed forms, package.json detection of yarn / pnpm / npm with vs without `lint` script, workspaces detection, default-branch fallback); planner (14 tests — fresh-host all-six-actions emission, idempotent re-run produces only the log-info action, partial-state surfaces only missing artefacts, `ignore_mechanism: per-clone-exclude` and `none` skip the append-to-ignore action, YAML rendering covers all required fields including `null` and empty-overrides cases, round-trip-ability via structural inspection); doctor (10 tests — all-green, each red path individually, yellow on missing global-ignore, red-dominates-yellow aggregation, yellow-dominates-green aggregation, all-green aggregation). Cognitive complexity ≤10 enforced by biome (no function exceeds 8). All exported functions carry `@otel sidecar-bootstrap.<verb>` JSDoc per rule #4 (`parse-repo-config`, `infer-repo-config`, `plan-bootstrap`, `render-repo-yaml`, `diagnose`); helpers (`requireString`, `inferHostRepo`, `inferCommitFormat`, `inferPreCommitCommand`, `inferTicketFormat`, `inferHostPackagesPath`, `quote`) are JSDoc-internal-only. Try/catch depth ≤1 per rule #6 — the planner has zero try/catch; the executor's `applyAction` switches on the action `kind` and lets per-action errors bubble to the CLI's top-level catch. The CLI's `acquireLock` exits 75 on EEXIST per Erlang/OTP idempotent-supervisor-restart discipline. End-to-end smoke-tested 2026-05-04 against a tmpdir host with a synthetic `package.json` + git remote: bootstrap inferred `host_repo: "test-owner/test-repo"` from `git@github.com:test-owner/test-repo.git`, `pre_commit_command: "yarn lint"` from `packageManager: yarn@4.0.0` + a `lint` script, wrote 5 sidecar artefacts + 1 global-ignore entry, doctor returned GREEN, idempotent re-run was a no-op (only the log-info action). The `--repair` mode currently re-runs the planner (the planner skips already-present artefacts so the result is identical to a normal run); a future enhancement could force-overwrite a corrupted `repo.yaml`, but v0 keeps it identical for simplicity. |
| 70 | `novel/adapters/persona-spawner/` (`@minsky/persona-spawner` v0 — closes `persona-spawner-v0` (P0): `PersonaSpawner` interface (Adapter) in `src/index.ts` over `omc /team <persona>` invocations + `StubPersonaSpawner` test fake recording calls in-memory + `OmcPersonaSpawner` Strategy in `src/omc.ts` shelling out via injected `child_process.spawn` to `omc /team <persona>` against a per-task working directory, surfacing `<workingDir>/.omc/state/team/<teamName>/` for the `@minsky/omc-tasksmd-bridge` reader (PR #78) + pure `dispatchPersona(taskTags)` lookup in `src/dispatch.ts` mapping tasks.md task tags to OMC personas (`bug` → `engineer`, `feature` → `engineer`, `research` → `researcher`, `review` → `reviewer`, `refactor` → `engineer`, default → `engineer`); `selfTest()` follows the `green / yellow / red` lattice — `green` if `omc` is on PATH, `yellow` if not (Stub still works for tests; daemon falls back to dry-run path), `red` on a probe-time error; constructor-injected `spawnFn` + `hasBinaryOnPath` are the only I/O seams) | Adapter (Gamma 1994) over the OMC `/team` CLI dependency + Strategy (Gamma 1994) for swap-in-able persona-spawning transports — the v0 ships only `OmcPersonaSpawner`, but the seam keeps a future native CrewAI / Anthropic-Agent-Teams Strategy open without touching the consumer; test fake (Meszaros 2007) for the `StubPersonaSpawner`; role-based agent orchestration (Wooldridge 2009) for the dispatch table; let-it-crash boundary (Armstrong 2007) for the single `try { spawn(...) } catch { resolve(-1) }` at the supervisor seam in `runChild`; DI at the edge (Martin 2017) — `spawnFn` and `hasBinaryOnPath` are constructor-level, the only I/O surfaces | Gamma, Helm, Johnson, Vlissides, *Design Patterns*, Addison-Wesley, 1994 (Adapter — interface over an external dependency; Strategy — `OmcPersonaSpawner` is one implementation, future Strategies plug in behind the same `PersonaSpawner` shape); Wooldridge, M., *An Introduction to MultiAgent Systems*, Wiley, 2009 (role-based agent orchestration — the dispatch table maps tasks.md task tags to OMC personas, closing the "society of specialists" promise from vision.md's north star — single Claude Code can't do this; OMC's `/team` mode is the existing tool, the adapter wraps it rather than reinventing a multi-persona runtime); Meszaros, G., *xUnit Test Patterns*, Addison-Wesley, 2007 (test fake — `StubPersonaSpawner` records calls in-memory and returns a canned `{ exitCode: 0, durationMs: 100, omcStateDir: '/tmp/stub' }` so consumer tests assert call shape without forking a real subprocess); Armstrong, J., *Programming Erlang*, Pragmatic Bookshelf, 2007 (let-it-crash AT the right boundary — the single try/catch in `runChild` is the supervisor seam, returning `-1` rather than swallowing the error inside the function; a missed spawn must never crash the daemon — the daemon's tick cadence + supervisor restart is the liveness boundary); Martin, R. C., *Clean Architecture*, Pearson, 2017 (DI at the edge — `spawnFn` and `hasBinaryOnPath` are constructor-level, the only I/O surfaces; the rest of the class is pure given those seams); Hewitt, Bishop, Steiger, "A Universal Modular Actor Formalism for Artificial Intelligence", *IJCAI* 1973 (each spawned persona is an actor; the dispatch table is the address resolver); Basiri et al., "Principles of Chaos Engineering", *IEEE Software* 33 (3), 2016 (the README's failure-mode table — steady-state hypothesis "spawn() returns ok within one tick against a working OMC install", blast radius "a single spawn call", operator escape hatch "swap OmcPersonaSpawner for StubPersonaSpawner without touching consumers", 3 rows: omc-missing-from-PATH, OMC-session-fails-to-start, OMC-team-state-dir-not-produced); rule #1 (vision.md § 1 — OMC is the existing tool; the adapter wraps it rather than reinventing a multi-persona runtime; opt-out comment in the README documents the no-existing-wrapper rationale); rule #2 (vision.md § 2 — every dep behind interface; OMC's CLI is exactly that, behind `PersonaSpawner`); rule #4 (vision.md § 4 — every exported function carries `@otel adapters.persona-spawner.<verb>` JSDoc, with `@otel-exempt <reason>` opt-outs on helpers / test-fake methods); rule #6 (vision.md § 6 — let-it-crash; one `// rule-6: handled-locally — synchronous spawn-time error` opt-out at the spawn-time boundary, plus one at the probe boundary, plus one at the access-probe boundary); rule #7 (vision.md § 7 — failure-mode discipline; the README's chaos table has 3 rows matching the brief's ≥3 requirement); rule #9 (vision.md § 9 — pre-registered Strategy-level pivot at `EXPERIMENT.yaml`: "if OMC's `/team` mode proves non-detachable, retire `OmcPersonaSpawner` only — the interface and consumers are unchanged"); rule #10 (vision.md § 10 — deterministic enforcement; the chaos table is checked by `scripts/check-rule-7-chaos-coverage.mjs`, the pattern index by `scripts/check-pattern-index.mjs`, the @otel coverage by `scripts/check-rule-4-otel-coverage.mjs`, the rule-1 opt-out by `scripts/check-rule-1-novel-justification.mjs`); TASKS.md `persona-spawner-v0` (the parent task whose Acceptance this row closes) + `tick-loop-daemon-real-spawn` (the consumer that wires the spawner into the daemon's real-spawn path, now unblocked); row 62 (`@minsky/omc-tasksmd-bridge` — the reader that consumes the `omcStateDir` this Strategy produces); Munafò et al., *Nature Human Behaviour* 1, 0021, 2017 (rule-#9 pre-registration — the Strategy-level pivot was declared in `EXPERIMENT.yaml` *before* code so a flaky OMC can't retroactively redefine "spawn API stable enough") | full | **No real subprocess in CI.** 24 paired tests across `src/{index,dispatch,omc}.test.ts` cover: Stub records 2 spawns in FIFO order with full opts, Stub starts empty, Stub `selfTest` is unconditionally green with `latencyMs: 0`, Stub returns canned default result on every call, Stub honours an overridden `cannedResult` from constructor, Stub `reset()` clears recorded calls; dispatch maps `bug` / `feature` → `engineer`, `research` → `researcher`, `review` → `reviewer`, falls back to `engineer` (default) for unknown tags, returns DEFAULT_PERSONA for empty tag list, walks tags left-to-right returning first match, accepts an injected dispatch table, PERSONA_DISPATCH_TABLE has ≥3 mappings; OmcPersonaSpawner invokes `omc /team <persona>` with cwd = workingDir + stdio = `['ignore', 'pipe', 'pipe']`, uses explicit teamName in `omcStateDir` when supplied, returns child's exit code on a clean close, computes `durationMs` from the injected clock, returns exitCode -1 when spawn throws synchronously (binary missing), returns exitCode -1 when child fires an `error` event (post-spawn ENOENT), `selfTest()` returns green when `omc` is on PATH, returns yellow when `omc` is missing from PATH, returns red when the PATH probe itself rejects. Cognitive complexity ≤10 enforced by biome (no function exceeds 8; the spawn path delegates to `runChild` + `runProbe` + `buildSelfTestResult` so each is ≤6). All exported functions/methods carry `@otel adapters.persona-spawner.<verb>` JSDoc per rule #4 (`adapters.persona-spawner.spawn`, `selfTest`, `dispatch-persona`, `run-child`, `run-probe`, `default-has-binary-on-path`); helper / type-guard / test-fake methods are `@otel-exempt` with reason. Try/catch depth ≤1 per rule #6 with three `// rule-6: handled-locally — …` opt-outs at (a) the synchronous spawn-time boundary in `runChild`, (b) the PATH probe boundary in `runProbe`, (c) the per-PATH-entry `access` boundary in `accessSafe` — each documented with the chaos-table row it implements. The constructor-injected `spawnFn` is the only subprocess seam; tests pass a `vi.fn()` mock to assert argv shape without forking a real OMC binary. The `hasBinaryOnPath` seam is independent of `spawnFn` so `selfTest()` doesn't have to fork to check liveness — the lattice mapping (yellow on missing) intentionally distinguishes "OMC not installed" from "spawn itself broken" so the dashboard surfaces a soft-fail rather than a red on a fresh-install host, mirroring Avizienis 2004 status-lattice semantics in `@minsky/adapter-types` `aggregateStatus()`. v0 ships exactly one Strategy (`OmcPersonaSpawner`); future Strategies (native CrewAI, Anthropic Agent Teams) plug behind the same `PersonaSpawner` shape with no consumer-side change. The `omcStateDir` returned by `spawn()` is the path the `@minsky/omc-tasksmd-bridge` reader walks (`<workingDir>/.omc/state/team/<teamName>/`); when the child writes nothing (chaos row 3), the bridge returns `[]` — no spurious tasks land. Default `teamName = taskId` so concurrent spawns don't collide on the filesystem. |
| 67 | `distribution/systemd/run-tick-loop.sh` + `novel/tick-loop/bin/tick-loop.mjs` + `novel/tick-loop/src/daemon.ts` + `novel/tick-loop/src/daemon.test.ts` (`tick-loop-daemon-v0` — production tick-loop daemon orchestrator; v0 ships **dry-run only**: `runDaemon({ tickInterval, maxIterations, dryRun, mockClient, tasksMdReader, pausedSentinelReader, budgetGuard })` loops `pickTask → checkBudget → claim → spawnTickDryRun → emitOtelSpans` honouring `state/PAUSED` sentinel + `BudgetGuard.decide()` circuit-break; the bash bootstrap `exec`s `node bin/tick-loop.mjs --dry-run …` so the supervisor (systemd / launchd) sees the node PID directly; passing `dryRun: false` throws — real `child_process.spawn('claude', …)` is deferred to the follow-up `tick-loop-daemon-real-spawn` per the brief's pre-registered scope guard) | Periodic-task scheduling (Liu & Layland 1973 — `tickInterval` cadence + `maxIterations` cap is the periodic-task envelope) + MAPE-K monitor (Kephart & Chess 2003 — the daemon IS the running monitor that the MAPE pipeline assumes; full conformance after real-spawn lands) + let-it-crash supervision (Armstrong 2007 — the daemon never catches mid-iteration; supervisor `Restart=on-failure` is the respawn boundary) + Adapter (Gamma 1994 — `MockAnthropicClient` is the spawn-step seam, `BudgetGuardLike` is the budget-check seam, both injected) | Liu, C. L., Layland, J. W., "Scheduling Algorithms for Multiprogramming in a Hard Real-Time Environment", *JACM* 20 (1), 1973 (the cadence model — periodic-task envelope with per-tick deadlines and budget-exhaustion halt); Kephart, J. O., Chess, D. M., "The Vision of Autonomic Computing", *IEEE Computer* 36 (1), 2003 (MAPE-K — the autonomic manager assumes a *running* monitor; this daemon IS that monitor); Armstrong, J., *Programming Erlang*, Pragmatic Bookshelf, 2007 (let-it-crash supervision — the daemon never catches mid-iteration, supervisor `Restart=on-failure` is the respawn boundary); Gamma, Helm, Johnson, Vlissides, *Design Patterns*, Addison-Wesley, 1994 (Adapter — `MockAnthropicClient` + `BudgetGuardLike` are seams, the test fakes are stub implementations, the production wiring is a one-line swap); Martin, R. C., *Clean Architecture*, Pearson, 2017 (the bash bootstrap + `bin/tick-loop.mjs` are the I/O boundary; `runDaemon` is pure given injected I/O — `tasksMdReader`, `pausedSentinelReader`, `budgetGuard`, `mockClient`, `now`, `sleep`, `emit`); Beyer, B., Jones, C., Petoff, J., Murphy, N. R. (eds.), *Site Reliability Engineering*, O'Reilly, 2016, Ch. 3 (error-budget gating before each tick — the `BudgetGuardLike.decide()` step is the burn-rate check); Munafò et al., *Nature Human Behaviour* 1, 0021, 2017 (rule-#9 pre-registration — the dry-run-only scope was committed in the brief *before* implementation, not post-hoc, so passing `dryRun: false` throws and the real-spawn path lands behind a follow-up PR); rule #2 (vision.md § 2 — every dep behind interface; the spawn step + the budget step are both behind seams, and the test suite is stub-driven); rule #4 (vision.md § 4 — `@otel tick-loop.<verb>` JSDoc on `runDaemon`, `pickTask`, `claim`, `spawnTickDryRun`); rule #6 (vision.md § 6 — exactly one `// rule-6: handled-locally — file-not-found is the documented graceful-exit path …` opt-out at the `readTasksMd` boundary); rule #7 (vision.md § 7 — failure-mode discipline; the README's chaos table v0 row set adds PAUSED-sentinel + missing-TASKS.md + budget-guard-circuit-break); rule #9 (vision.md § 9 — pre-registered pivot at `EXPERIMENT.yaml`: "if dry-run mode reveals the orchestrator's data-flow assumptions are wrong (e.g., budget-guard's `decide()` shape doesn't compose with the loop's expected state), revisit before unlocking real spawn"); row 64 (`@minsky/tick-loop` v0 — the package this daemon extends); TASKS.md `tick-loop-daemon-v0` (the parent task whose Acceptance this row closes) + `tick-loop-daemon-real-spawn` (the documented follow-up that takes the dry-run guardrail off only after maciek-impl validates real-budget-state matches the loop) | partial-real-spawn | **Dry-run only v0 (declared deviation, pre-registered).** The brief's primary path was a daemon that spawns a real `claude` (or `omc /team`) child process per iteration; v0 ships ONLY the dry-run path because real subprocess spawning has un-mitigated risk surface (env-passing, deadlocks on stdin/stdout, lease-then-spawn race) that's safer to land in a separate, narrower follow-up. Passing `dryRun: false` throws synchronously *before* any I/O so the guardrail is mechanical, not policy. **13 paired tests** in `novel/tick-loop/src/daemon.test.ts` cover (parent task brief): happy-path 4-iteration dry-run completes against a synthetic fixture (parent + child spans recorded — 4 `tick-loop.iteration` + 4 `tick-loop.tick`); PAUSED sentinel honoured within 1 iteration; missing TASKS.md graceful exit (ENOENT → `stoppedReason: 'missing-tasks-md'`); budget-guard circuit-break skips iteration with logged advisory; mock-anthropic 5xx → release-on-failure (status: `failed` with `5xx` in reason); `--max-iterations` cap respected; throws on `dryRun: false`; sleeps `tickInterval` between iterations (but not after the last); `pickTask` picks first unblocked unclaimed P0 task; skips claimed (`(@minsky-tick-loop)` marker); skips blocked (`**Blocked by**:` field); ignores P2 section; returns undefined when nothing pickable. Cognitive complexity ≤10 enforced by biome (`runDaemon` delegates to `runOneIteration` + `runClaimedIteration` so the loop body is dispatch-only, biggest function ≤10). All exported functions carry `@otel tick-loop.<verb>` JSDoc per rule #4 (`run-daemon`, `pick-task`, `claim`, `spawn-tick`); helpers / type guards / sleep are JSDoc-internal-only. Try/catch depth ≤1 per rule #6 with one `// rule-6: handled-locally — file-not-found is the documented graceful-exit path …` opt-out at the `readTasksMd` boundary (ENOENT → graceful exit; other errors propagate up to the supervisor — let-it-crash AT the right boundary). The CLI (`bin/tick-loop.mjs`) is the only I/O orchestrator; `runDaemon` itself is pure given the injected seams (`tasksMdReader`, `pausedSentinelReader`, `budgetGuard`, `mockClient`, `now`, `sleep`, `emit`). The bash bootstrap `exec`s into `node` so the supervisor (systemd `Restart=on-failure` per `minsky-tick-loop.service`; launchd `KeepAlive: SuccessfulExit=false` per `com.minsky.tick-loop.plist`) sees the node PID directly and a SIGTERM reaches it without a shell-wrapper detour. The `claim()` step is in-memory only in v0 (returns the lease shape; persistence to TASKS.md is the documented follow-up). v0 dispatches blocked-by ANY string match on `**Blocked by**:` — sub-string-aware "is *this* blocker resolved?" semantics ship with the persistence follow-up because they need TASKS.md mutation, not just a read. The `BudgetGuardLike` structural type avoids importing `@minsky/budget-guard` itself (which transitively pulls `@minsky/token-monitor`); the production wiring will swap in the real `BudgetGuard.decide()` at `tick-loop-daemon-real-spawn`-time. Conformance is `partial` (not `full`) for the same load-bearing reason as row 64's `tick-loop`: the daemon's MAPE-K monitor role is *partial* until real-spawn lands; once it does, this row + row 64 + the follow-up row collapse into a single `full` MAPE-K-monitor row. **Sub-task 1/3 of `tick-loop-daemon-real-spawn` — `tick-loop-daemon-spawn-strategy`** introduces `novel/tick-loop/src/spawn-strategy.ts` (`SpawnStrategy` interface + `DryRunSpawnStrategy` synthetic + `ProcessSpawnStrategy` over `node:child_process.spawn` with last-4KB stdout/stderr tails + AbortSignal; never throws on non-zero exit — exitCode in result, let-it-crash AT the Strategy boundary per Armstrong 2007). The seam is reachable via `runDaemon`'s optional `spawnStrategy?` opt; production v0 stays defaulted to the legacy dry-run `tick(...)` path (no Strategy injected → `dryRun: false` still throws), so all 13 daemon tests pass UNCHANGED. **Sub-task 2/3 of `tick-loop-daemon-real-spawn` — `tick-loop-daemon-budget-guard-real`** wires the real `@minsky/budget-guard` `BudgetGuard` into the daemon via a thin facade `novel/tick-loop/src/budget-guard-facade.ts` (`fromRealBudgetGuard(guard)`, Adapter — Gamma 1994; the **executed Pivot** pre-registered in the task block, since the real `BudgetGuard`'s public surface is `tick(): Promise<BudgetDecision>` per the watchdog idiom — Beyer SRE 2016 Ch. 3 — rather than `decide()`). `bin/tick-loop.mjs` constructs the real `BudgetGuard` from a `MaciekTokenMonitor` (production — reads `~/.claude/projects/<cwd>/<session>.jsonl`, the same data Maciek's `claude-monitor` reads) or from a `StubTokenMonitor` (when `--dry-run`, so the local smoke stays hermetic). The daemon never imports `@minsky/budget-guard` directly — rule #2's seam stays intact, the facade is the one-line bridge. After this sub-task the row's MAPE-K monitor role advances to **`partial+real-budget`** (the *Monitor* phase now uses a real budget gate; the *Execute* phase is still dry-run until sub-task 3 flips the spawn default). 14 paired tests in `daemon.test.ts` (one new — `real BudgetGuard wired via facade: circuit-break flips iteration to budget-paused` — drives the real `BudgetGuard.tick()` against a fixture `StubTokenMonitor` set past the 85 % circuit-break threshold and asserts the iteration flips to `budget-paused`, while a fresh window completes normally). **Sub-task 3/3 of `tick-loop-daemon-real-spawn` — `tick-loop-daemon-real-spawn-flip` — SHIPPED**: production default flipped to `ProcessSpawnStrategy` (real `claude --print` headless subprocess per iteration — brief on stdin, response on stdout per `claude --help`'s documented non-interactive flag; the legacy `--resume` default — which opened the interactive session picker rather than reading the brief from stdin — was the bug fixed by `tick-loop-spawn-args-fresh-session`); dry-run is now opt-in via the `MINSKY_TICK_DRY_RUN=1` env-var control surface; the legacy `--dry-run` argv flag is retired from `bin/tick-loop.mjs` + `distribution/systemd/run-tick-loop.sh`. The supervisor unit files (`distribution/systemd/minsky-tick-loop.service`, `distribution/launchd/com.minsky.tick-loop.plist`) ship with `MINSKY_TICK_DRY_RUN=1` set during the safe rollout window — an operator drops that line / dict entry to flip to full real spawn (production default = unset). 16 paired daemon tests in `daemon.test.ts` plus a `tick-loop-spawn-args-fresh-session`-gated test in `spawn-strategy.test.ts` (`it.skipIf(!hasClaude)` on `which claude` — asserts the new `["--print"]` default produces a fresh non-interactive response, NOT a "Select a session to resume" picker UI; CI without `claude` skips cleanly, local with `claude` proves the real wiring). The pre-existing v0 production guardrail (`dryRun: false` without an injected Strategy throws) is preserved — the CLI always injects a Strategy, but a misconfigured caller of `runDaemon` directly still hits the throw. After this flip the row's MAPE-K monitor role advances from `partial+real-budget` to **`partial-real-spawn`** — the *Execute* phase now runs against a real subprocess; full MAPE-K conformance lands when `user-story-001-integration-test-real` (now unblocked) drives the daemon end-to-end against a real `claude --print` on the self-hosted runner. Closes the parent `tick-loop-daemon-real-spawn` epic. |
| 65 | `distribution/shortcuts/` (`tokens-remaining.shortcut.json` + `last-task-status.shortcut.json` + `constraint-of-the-week.shortcut.json` + `pause.shortcut.json` + `resume.shortcut.json` + `schema/minsky-shortcut.schema.json` + `README.md` + `test/shortcuts-json.test.mjs` + `test/validate.mjs`) and the new `GET /watch.json` route on `@minsky/dashboard-web` (`novel/dashboard-web/src/watch.ts` + the `/watch.json` handler in `server.ts`) — Apple-Shortcuts manifests + JSON envelope for the iOS / watchOS surface that closes the `watch-shortcuts` task; **rule-#9 pivot fired before code**: Apple's `.shortcut` is signed binary plist since iOS 15, so the brief's authorised pivot path was taken — JSON config files + on-device build runbook, not native importable manifests | Glanceable ambient surface (Card & Mackinlay 1999 — three numbers, no chrome) over a calm-tech wrist channel (Weiser & Brown 1995 — dwell as friction; the surface is ambient background, not foreground); Adapter (Gamma 1994) — the `/watch.json` JSON envelope IS the adapter between Minsky internals (10 `SuccessMetric` rows) and Apple's Shortcuts UI runtime, satisfying rule #2; operator escape hatch / kill switch (Beyer SRE 2016 Ch. 17) for the pause/resume control surface, mapped to a single `POST /control` endpoint with a `{paused: bool}` body | Card, S. K., Mackinlay, J., Shneiderman, B., *Readings in Information Visualization*, Morgan Kaufmann, 1999 (3-number glanceable display); Weiser, M., Brown, J. S., "Designing Calm Technology", *PowerGrid Journal* 1995 (calm tech — dwell as friction; vision.md success #6); Beyer, B., Jones, C., Petoff, J., Murphy, N. R. (eds.), *Site Reliability Engineering*, O'Reilly, 2016, Ch. 17 (operator escape hatch); Gamma, Helm, Johnson, Vlissides, *Design Patterns*, Addison-Wesley, 1994 (Adapter — the JSON envelope is the seam); Munafò et al., *Nature Human Behaviour* 1, 0021, 2017 (rule-#9 pre-registration — the pivot to JSON-config-plus-runbook was committed in the brief *before* implementation, not post-hoc, and disclosed in this row's notes); rule #2 (vision.md § 2 — every dep behind interface; the JSON envelope is the adapter between Minsky internals and Apple's UI runtime); rule #10 (vision.md § 10 — deterministic enforcement; the smoke test is a pure validator + filesystem dispatcher); user-stories/005-three-numbers-watch.md (the parent user story — when extant); user-stories/002-pause-from-iphone (the second user story — pause/resume); TASKS.md `watch-shortcuts` (the parent task this row closes); rows 56–58 (the parent `dashboard-web-v0` epic this extends with the JSON surface); row 65 EXPERIMENT.yaml — `watch-shortcuts-2026-05-03` | partial | **Pivot deviation (2026-05-03):** the brief's primary path was native `.shortcut.json` manifests Apple Shortcuts can import directly. Research (iOS-Shortcuts-Reference, sebj/iOS-Shortcuts-Reference) showed iOS 15+ uses **signed binary plist** for `.shortcut` files; hand-authoring or off-device generating signed binary plists has no documented public path. The brief's documented pivot path was taken: ship JSON config files + a step-by-step on-device build runbook (`distribution/shortcuts/README.md`). The autonomously-verifiable upstream half (URL / port / path consistency, JSON-schema fidelity, metric-id mapping to `SuccessMetric.id`, `/watch.json` returning the right JSON shape) is enforced by the smoke test (`test/shortcuts-json.test.mjs` — 16 assertions including per-file JSON parse + per-file schema check + URL invariant + watch-key drift + pause/resume symmetry) plus the server tests (`novel/dashboard-web/test/server.test.ts` — 5 new `/watch.json` tests on top of the 12 existing HTML tests). Total: 33 tests pass, 0 fail. The `/watch.json` route exposes **exactly four fields** (`tokens-remaining`, `last-task-status`, `constraint-of-the-week`, `paused`) — the 3-value glanceable cap (Card & Mackinlay 1999) plus the single boolean control flag for the pause/resume pair. Field naming maps 1:1 to keys via the typed constant `WATCH_METRIC_IDS` in `novel/dashboard-web/src/watch.ts` (rule #4 — every constant in source); the smoke test asserts each shortcut's `extract.metric_id` matches that constant (no drift between Shortcut and dashboard). Conformance is `partial` (not `full`) because the on-device step is **manual and operator-driven** — the v0 cannot mechanically verify that the operator actually built the Shortcut on iPhone (the verification half of `pnpm vitest run user-stories/005-three-numbers-watch.test.ts` is parent-scoped to user-story 005, not to this row). The pivot's next-tier rule-#9 pivot path (operator rebuild rate >1 / month sustained 90 days → escalate to `native-watchos-app`) is filed as the parent task's documented Pivot in TASKS.md. Cognitive complexity ≤10 enforced by biome (the smoke validator is split across 5 helper functions in `test/validate.mjs`, each ≤10). The pause/resume pair is symmetric: `pause` POSTs `{"paused": true}`, `resume` POSTs `{"paused": false}`; an assertion in the smoke test enforces the symmetry. **The pause/resume control endpoint (`POST /control`) shipped in PR #89** — `novel/dashboard-web/src/control.ts` accepts `{paused: bool}`, writes `${MINSKY_HOME}/.minsky/state/PAUSED` (or removes it), and is the sentinel the tick-loop daemon (row 67) reads each iteration. Both the data surface (`GET /watch.json`) and the control surface (`POST /control`) are now live; the on-device Shortcut build remains the only manual step. The Watch surface respects the 3-value cap from `ci-lint-watch-surface-cap` (P2) — `WATCH_METRIC_IDS` has exactly three keys, asserted by the smoke test. Default URL placeholder is `minsky.tail-scale.ts.net:8080`; the operator substitutes their actual Tailscale host per the runbook. |
| 69 | `novel/mape-k-loop/src/orchestrator.ts` + `novel/mape-k-loop/src/orchestrator.test.ts` + `novel/mape-k-loop/bin/mape-k-orchestrator.mjs` (`mape-k-prompt-rollout-orchestrator` — pure orchestrator above `tick(...)`: composes M+A+P+E+K with a rollout-draft surface (`{variantId, branchSlug, experimentYaml}`) the CLI turns into a draft branch + draft PR; CLI reads `experiment-store/*.jsonl` + `constraints.md` tail + `.claude/skills/<id>/SKILL.md` and writes results to `tmp/proposed-rollouts/` + appends to `constraints.md`; flags `--dry-run` / `--ingest-mode` / `--max-rollouts=N` cap operator surface) | MAPE-K orchestration layer (Kephart & Chess 2003 — the autonomic-manager assumes a *running* orchestrator; this layer wraps the pure `tick(...)` cycle with the rollout-draft surface a human reviewer can act on) + pure-function-with-I/O-at-edge (Martin 2017 — `orchestrate(...)` is referentially transparent over its inputs; the CLI is the only I/O boundary) + operator escape hatch / pre-merge gate (Beyer SRE 2016 Ch. 17 — drafts NEVER auto-merge; human review is the load-bearing safety property) + pre-registered prediction at the rollout boundary (Munafò et al. 2017 — the EXPERIMENT.yaml carries the predicted gain *before* the rollout merges, so post-hoc rationalisation is structurally impossible) | Kephart, J. O., Chess, D. M., "The Vision of Autonomic Computing", *IEEE Computer* 36 (1), 2003 (MAPE-K loop — the orchestrator is the layer that turns a tick into operator-actionable artefacts: a draft branch + a constraints.md append + a research.md amendment proposal); Khattab, O., et al., "DSPy: Compiling Declarative Language Model Calls into Self-Improving Pipelines", *ICLR* 2024 (the rejected baseline whose "compile-time variant catalogue → A/B → winner picks" cycle survives in this orchestrator's flow, minus DSPy's runtime); Kohavi, R., Tang, D., Xu, Y., *Trustworthy Online Controlled Experiments*, Cambridge UP, 2020, Ch. 3 (sustained-gain window — the rollout-draft only fires when the underlying `execute()` passes the 7 d sustained-gain guard); Ries, E., *The Lean Startup*, Crown Business, 2011 (oscillation guard — the orchestrator suppresses re-proposal of any variant that was rejected in the lookback window); Martin, R. C., *Clean Architecture*, Pearson, 2017 (the I/O boundary is the CLI, not `orchestrate(...)` itself); Beyer, B., Jones, C., Petoff, J., Murphy, N. R. (eds.), *Site Reliability Engineering*, O'Reilly, 2016, Ch. 17 (operator escape hatch — drafts never auto-merge); Munafò et al., *Nature Human Behaviour* 1, 0021, 2017 (rule-#9 pre-registration — the predicted gain is committed in the draft EXPERIMENT.yaml *before* merge, so a future replay window can falsify or confirm it); rule #2 (vision.md § 2 — every dep behind interface; the orchestrator never imports a vendor SDK, it takes an injected `PromptOptimizer` Strategy); rule #4 (vision.md § 4 — `@otel mape-k-loop.orchestrate` JSDoc on the exported function); rule #6 (vision.md § 6 — let-it-crash; the CLI's only `try/catch` is at the JSONL parse boundary, depth 1, with the documented graceful-degrade rationale); rule #7 (vision.md § 7 — failure-mode discipline; missing experiment-store / missing constraints.md / corrupt JSONL all degrade gracefully without crashing); rule #9 (vision.md § 9 — pre-registered pivot at the parent task's `EXPERIMENT.yaml`: "if 90 d of orchestrator runs produce zero rollouts (gain threshold unreachable), drop sustained-gain window 7d → 3d OR widen the variant catalogue past v0's 3 mutations"); row 54 (`@minsky/mape-k-loop` core — the `tick(...)` cycle this orchestrator wraps); row 53 (`@minsky/prompt-optimizer` — the injected Strategy seam); TASKS.md `mape-k-prompt-rollout-orchestrator` (the parent task this row closes) + `experiment-tracker-knowledge-ingestion` (the consumer of the `--ingest-mode` flag, now unblocked) | full | **No I/O in the orchestrator itself; all I/O at the CLI edge.** 6 paired tests in `novel/mape-k-loop/src/orchestrator.test.ts` cover: happy-path rollout (decision: rollout → branchSlug + EXPERIMENT.yaml carry the predicted gain at 4-decimal precision), oscillation guard suppresses repeat variant within window (decision: abstain, no rolloutDraft, reason mentions oscillation), sustained-gain failure abstains (in-window span < 7d → abstain, no rolloutDraft, reason mentions sustained-gain), no-op when no experiment verdicts available (no constraint → no rolloutDraft, knowledge.constraintsAppend includes 'no-op'), `--ingest-mode` skips Plan/Execute (rolloutDecision: null even with advisories present, but Knowledge phase still computes calibration MAE → researchAmendmentProposal fires), `--max-rollouts=0` suppresses draft on a passing rollout decision. Cognitive complexity ≤10 enforced by biome (`orchestrate(...)` delegates to `runTickFromArgs`, `maybeBuildDraft`, `renderExperimentYaml` — biggest function ≤8). All exported functions carry `@otel mape-k-loop.<verb>` JSDoc per rule #4 (`mape-k-loop.orchestrate`); helpers are `@otel-exempt` with reason. Try/catch depth ≤1 per rule #6 with the JSONL-parse seam at the CLI as the single graceful-degrade boundary. The rollout-draft is **always a draft** — the CLI writes the EXPERIMENT.yaml to `tmp/proposed-rollouts/` and never opens a PR or pushes a branch (rule #7 — a load-bearing safety property; the operator review gate is the human-in-the-loop seam). The CLI's `--ingest-mode` is the consumer of `experiment-tracker-knowledge-ingestion`'s rule-#9 quarterly automation layer (vision.md § 9 — closes the loop from `validated-learnings.md` updates back into rollout decisions). Default `maxRollouts: 1` (one rollout per orchestrator invocation); the daemon's optional `mapeKOrchestratorIntervalTicks` integration (deferred to follow-up — `tick-loop-daemon-real-spawn`) will fire the orchestrator every Nth tick, with the cap matching operator review cadence. The injected `PromptOptimizer` is the seam — production wiring uses `AnthropicPromptOptimizer` (row 53), tests use `StubPromptOptimizer`. The orchestrator's calibration-drift threshold defaults to the `knowledge` phase's 0.5 (50 % MAE per Munafò et al. 2017); the `researchAmendmentProposal` only fires when MAE > threshold AND sample size > 0 (rule #7 — the "no signal yet" path is graceful). |
| 72 | `user-stories/006-runner-on-any-repo.md` (cross-repo-runner umbrella user story — pre-registers the rule-#9 contract for shipping minsky's full constitution against any host repo via a gitignored `.minsky/` sidecar at the host root and a `MINSKY_HOST_ROOT`-aware lint substrate; story ships before any of the 5 implementing PRs so the success/pivot thresholds cannot be retrofitted) | Sidecar configuration + parametric substrate root (Hewitt 1973 — the actor's "universe" is `MINSKY_HOST_ROOT`; the contract is `.minsky/vision.md` + `repo.yaml`) composed with check-run-as-asynchronous-boundary (Helland CIDR 2007 — `repository_dispatch` → minsky workflow → GitHub-API check-post; eventual-consistency window bounded by dispatch-delivery + workflow-run time) and supervised process spawn (Armstrong 2007 — the existing `ProcessSpawnStrategy` wrapped in `BudgetGuard`; rule #1, no new spawn surface) and pre-registered hypothesis-driven development (Munafò 2017 — the umbrella Hypothesis/Success/Pivot/Measurement/Anchor block is committed before any implementing PR ships) | Hewitt, C., "A Universal Modular ACTOR Formalism for Artificial Intelligence", *IJCAI* 1973 (the substrate boundary is the actor's interface; making it parametric via `MINSKY_HOST_ROOT` is the move that lets the same actor live in many systems); Helland, P., "Life beyond Distributed Transactions: an Apostate's Opinion", *CIDR* 2007 (the substrate-locality boundary defines the consistency boundary; the minsky-side check-run is an asynchronous boundary with a bounded eventual-consistency window); Armstrong, J., *Programming Erlang*, Pragmatic Bookshelf, 2007 (supervisor wrapping the spawn — `BudgetGuard` is the SLA, let-it-crash on rule violations); Kephart, J. O., Chess, D. M., "The Vision of Autonomic Computing", *IEEE Computer* 36 (1), 2003 (cross-repo runs are heterogeneous experiments the MAPE-K loop ingests via the experiment-store; the runner is the *Plan + Execute* phase of cross-repo work regardless of which repo the work happened in); Munafò, M. R., Nosek, B. A., Bishop, D. V. M., et al., "A manifesto for reproducible science", *Nature Human Behaviour* 1, 0021, 2017 (rule-#9 pre-registration — the umbrella experiment is committed before any of the 5 implementing PRs); Basiri, A., et al., "Principles of Chaos Engineering", *IEEE Software* 33 (3), 2016 (the failure-mode table — 14 rows: not-bootstrapped / task-not-found / rule-#9 violation / budget-pause / hook-collision / spawn-without-self-grade / sandbox-leak / mid-spawn budget / dispatch-lost / broken-symlink / bootstrap-race / force-pushed / readonly-ignore-file / TASKS.md schema-drift); rule #1 (vision.md § 1 — don't reinvent — we wrap Claude Code's existing spawn, we do not replace it); rule #2 (vision.md § 2 — every dep behind interface; `MINSKY_HOST_ROOT` is the file-system-root interface; `BudgetGuard` and `ProcessSpawnStrategy` are reused unchanged); rule #6 (vision.md § 6 — let-it-crash; dry-run is the safe default, `--live` is the explicit opt-in; runner exits loudly on rule-#9 violations rather than papering over them); rule #7 (vision.md § 7 — the failure-mode table has 14 rows, exceeding the brief's ≥3); rule #9 (vision.md § 9 — this story IS the rule-#9 contract for the cross-repo-runner stack; the umbrella experiment block is the iron-rule pre-registration; per-step PRs each ship their own `experiments/<id>.yaml` at PR time once `experiments-directory-migration` lands); rule #10 (vision.md § 10 — deterministic CI substrate is the minsky-side GitHub Action + the 12 host-aware lints, no LLM in the constitutional-gate chain); `docs/cross-repo-portability.md` (the operative classification this story acts on); `docs/cross-repo-portability/2026-05-04-classification-run.md` (the archived raw classification run preserving the rejected pivot path); TASKS.md `cross-repo-portability-doc` / `host-root-resolver-prep` / `minsky-sidecar-bootstrap` / `cross-repo-runner-v0` / `cross-repo-runner-aifn-840-integration-test` / `cross-repo-ci-action` (the 6-step implementing stack — step 1 shipped via PR #122) | full (planned) | **Pre-registered, no implementation yet.** Step 1 of 6 (`cross-repo-portability-doc`) shipped via PR #122; the substrate decision is documented; the runner is not yet built. Story status updates to `Implemented` only after step 5 (`cross-repo-runner-aifn-840-integration-test`) ships AND the metric (`cross_repo_runs_validated_pct ≥ 80%` over the trailing 30 days, after a 10-run warm-up) clears. Pre-registered umbrella pivot: if the metric stays below 50 % for two consecutive 30-day windows after the warm-up, the cross-repo architecture is wrong (not just under-implemented); pivot to thin-envoy mode (`BudgetGuard` + supervisor only, no constitutional gate) and retire the sidecar architecture as a load-bearing claim. Per-step pivots are smaller and surface earlier in each implementing PR's own `experiments/<id>.yaml` once the migration lands. The 14-row failure-mode table is exhaustive of the v0 acceptance — every row resolves either to a `loud-crash-supervisor-restart` (rule-#9 violations, sandbox leaks, missing bootstrap), a `circuit-break-and-notify` (budget-pause, dispatch-lost, scope-leak), or a `graceful-degrade` (hook-collision via chain-mechanism, schema-drift via tolerant parser, readonly-ignore-file via fallback to `.git/info/exclude`). Three of the rows (force-pushed, dispatch-lost, broken-symlink) document the asynchronous-boundary failure modes Helland's CIDR-2007 anchor predicts; chaos tests for them ship with the implementing PRs. The 12 host-aware lints (per `docs/cross-repo-portability.md`) plus rule-1's by-design exception are the deterministic substrate the constitutional-gate runs on; rule-6 is host-substrate-deferred to the host's ESLint. Filed before the implementing PRs precisely because rule-#9 is iron — pre-registration without execution is half a rule, but execution without pre-registration is post-hoc rationalisation. The umbrella experiment record will graduate to `experiments/006-runner-on-any-repo.yaml` once `experiments-directory-migration` (P0, ahead of the cross-repo-runner stack) ships the plural-experiments substrate. |
| 68 | `novel/adapters/notifier/` (`@minsky/notifier` v0 — push-channel adapter for the (future) tick-loop daemon: `Notifier` interface + `Notification` + `PushResult` types in `src/index.ts`, `StubNotifier` test fake recording calls in-memory with a `.calls` getter, `NtfyNotifier` HTTP Strategy in `src/ntfy.ts` posting to `https://ntfy.sh/<topic>` (or self-hosted base URL) with `Title` / `Priority` / `Tags` / optional `Authorization: Bearer` headers; `selfTest()` follows the `green / yellow / red` lattice — `green` on a 2xx mock, `yellow` on rate-limit 429 (service is up but throttled), `red` on a network error; constructor-injected `fetch` (defaults to global `fetch`, no `node-fetch` dep — Node 18+) is the only I/O seam) | Adapter (Gamma 1994) over an external push-channel dependency + Strategy (Gamma 1994) for swap-in-able transports — the v0 ships only `NtfyNotifier`, but the seam keeps APNs / Pushover / webhook open without touching the consumer; test fake (Meszaros 2007) for the `StubNotifier`; let-it-crash boundary (Armstrong 2007) for the single `try { fetch } catch { return ok:false }` at the supervisor seam in `push()`; DI at the edge (Martin 2017) — the `fetchFn` opt is constructor-level, the only I/O surface | Gamma, Helm, Johnson, Vlissides, *Design Patterns*, Addison-Wesley, 1994 (Adapter — interface over an external dependency; Strategy — `NtfyNotifier` is one implementation, future Strategies plug in behind the same `Notifier` shape); Hunt, A., Thomas, D., *The Pragmatic Programmer*, Addison-Wesley, 1999, Tip 32 ("Crash Early — but the crash needs to reach the operator"; the notifier is the operator-facing channel that turns a let-it-crash event into actionable feedback — story-001 acceptance #6 "a morning notification summarizes work done" is the load-bearing user-facing surface); Meszaros, G., *xUnit Test Patterns*, Addison-Wesley, 2007 (test fake — `StubNotifier` records calls in-memory and returns a fixed `{ ok: true }` so consumer tests assert request shape without a network); Armstrong, J., *Programming Erlang*, Pragmatic Bookshelf, 2007 (let-it-crash AT the right boundary — the single try/catch in `push()` is the supervisor seam, returning a failure shape rather than swallowing the error inside the function); Martin, R. C., *Clean Architecture*, Pearson, 2017 (DI at the edge — the `fetchFn` opt is constructor-level, the only I/O surface; the rest of the class is pure given that seam); ntfy publish documentation (<https://docs.ntfy.sh/publish/> — `Title`, `Priority` (1..5), `Tags`, `Authorization: Bearer` headers and the plain-text-body convention); Basiri et al., "Principles of Chaos Engineering", *IEEE Software* 33 (3), 2016 (the README's failure-mode table — steady-state hypothesis "push() returns ok:true within 2s p95 against a reachable ntfy", blast radius "a single push call", operator escape hatch "swap NtfyNotifier for StubNotifier without touching consumers", 4 rows: ntfy.sh down, network partition, rate-limit 429, auth-token revoked); rule #2 (vision.md § 2 — every dep behind interface; ntfy.sh is exactly that, behind `Notifier`); rule #4 (vision.md § 4 — every exported function carries `@otel adapters.notifier.<verb>` JSDoc, with `@otel-exempt <reason>` opt-outs on helpers / test-fake methods); rule #6 (vision.md § 6 — let-it-crash; one `// rule-6: handled-locally — fetch rejection is the supervisor boundary` opt-out at the network seam); rule #7 (vision.md § 7 — failure-mode discipline; the README's chaos table has 4 rows, exceeding the brief's ≥3); rule #9 (vision.md § 9 — pre-registered Strategy-level pivot at `EXPERIMENT.yaml`: "if ntfy.sh proves unreliable — ≥1 outage / week sustained over 4 weeks — pivot to APNs or self-hosted ntfy on the user's Tailnet; Strategy-level only, interface and consumers unchanged"); rule #10 (vision.md § 10 — deterministic enforcement; the chaos table is checked by `scripts/check-rule-7-chaos-coverage.mjs`, the pattern index by `scripts/check-pattern-index.mjs`, the @otel coverage by `scripts/check-rule-4-otel-coverage.mjs`); TASKS.md `notifier-adapter-v0` (the parent task whose Acceptance this row closes); Munafò et al., *Nature Human Behaviour* 1, 0021, 2017 (rule-#9 pre-registration — the Strategy-level pivot was declared in `EXPERIMENT.yaml` *before* code so a flaky ntfy can't retroactively redefine "reliable enough") | full | **No real network in CI.** 18 paired tests across `src/{index,ntfy}.test.ts` cover: Stub records 2 pushes in FIFO order, Stub starts empty, Stub `selfTest` is unconditionally green with `latencyMs: 0`, Stub `reset()` clears recorded calls, Stub `.calls` getter returns a stable readonly snapshot; Ntfy POSTs to `https://ntfy.sh/<topic>` by default with body + `Title` + `Priority` headers, Ntfy strips trailing slash on a self-hosted `serverBaseUrl`, Ntfy priority lattice maps `low`→2 / `normal`→3 (default) / `high`→5, Ntfy attaches `Authorization: Bearer <token>` when `authToken` is set, Ntfy omits Authorization when no token is supplied, Ntfy joins tags with commas into a single `Tags` header, Ntfy returns `{ ok: false, reason: 'http 429' }` on rate-limit, Ntfy returns `{ ok: false, reason: 'http 503' }` on 5xx without throwing, Ntfy returns `{ ok: false, reason: 'network: …' }` when `fetch` rejects, Ntfy `selfTest` returns `green` against a mock 200, Ntfy `selfTest` returns `red` on a network error, Ntfy `selfTest` returns `yellow` on a 429 (service-up-but-throttled lattice point), Ntfy `selfTest` sends a low-priority push with title `minsky.notifier.selfTest`. Cognitive complexity ≤10 enforced by biome (no function exceeds 8). All exported functions/methods carry `@otel adapters.notifier.<verb>` JSDoc per rule #4 (`adapters.notifier.push`, `adapters.notifier.selfTest`); helper / type-guard / test-fake methods are `@otel-exempt` with reason. Try/catch depth ≤1 per rule #6 with one `// rule-6: handled-locally — fetch rejection is the supervisor boundary; per rule #7 (chaos table rows ntfy.sh-down / network-partition / rate-limited), a missed push must never crash the daemon` opt-out at the network seam. The `fetchFn` opt is the only I/O surface; the rest is pure given that seam (Martin 2017). Caller is responsible for keychain lookup of `authToken` — same pattern as `MaciekTokenMonitor`'s `configDir` opt; the adapter never shells out to `security`. The `selfTest()` lattice mapping (yellow on 429) intentionally distinguishes "ntfy is broken" from "we're being too noisy" so the dashboard surfaces a soft-fail rather than a red on rate-limit, mirroring Avizienis 2004 status-lattice semantics in `@minsky/adapter-types` `aggregateStatus()`. v0 ships exactly one Strategy (`NtfyNotifier`); future Strategies (APNs via webhook, Pushover) plug behind the same `Notifier` shape with no consumer-side change. |
| 64 | `novel/tick-loop/` (`@minsky/tick-loop` v0 — sub-task 2/3 of `first-integration-test`; deterministic mock-tick daemon that loops `claim → mock-anthropic-call → complete` on a configurable cadence: pure `tick(opts)` per-iteration function + `runSmoke(opts)` I/O orchestrator + `MockAnthropicClient` seam + `TestFakeMockAnthropic` test fake with 4 chaos modes (`none` / `http-5xx` / `network-timeout` / `malformed-output`) + in-memory `SpanRecorder` + `parseFixtureTaskIds` parser over a synthetic `test/fixtures/synthetic-tasks.md`) | Periodic-task scheduling + let-it-crash mock daemon — Liu & Layland 1973 (the cadence model: N ticks within a wall-clock budget; each tick has its own deadline, the loop halts at budget-exhaustion); Armstrong 2007 (let-it-crash supervision: chaos branches return failure shapes rather than throw, so the supervisor — the caller, or `runSmoke` — decides the respawn policy; the mock-client rejection path is mapped at the `tick` boundary, "let it crash AT the right boundary, not at the wrong one"); Adapter (Gamma 1994) — `MockAnthropicClient` is the seam, the test fake is one implementation, production code never imports the SDK | Liu, C. L., Layland, J. W., "Scheduling Algorithms for Multiprogramming in a Hard Real-Time Environment", *JACM* 20 (1), 1973 (the cadence model — periodic-task envelope with per-tick deadlines and budget-exhaustion halt); Armstrong, J., *Programming Erlang*, Pragmatic Bookshelf, 2007 (let-it-crash supervision — the chaos table forces ≥3 rows including a 5xx fixture so the supervisor sees a real respawn signal); Gamma, Helm, Johnson, Vlissides, *Design Patterns*, Addison-Wesley, 1994 (Adapter — `MockAnthropicClient` interface; `TestFakeMockAnthropic` is one implementation; production plugs a real Anthropic-SDK adapter behind the same shape); Martin, R. C., *Clean Architecture*, Pearson, 2017 (`tick` is pure given a deterministic client + clock; `runSmoke` is the I/O orchestrator at the edge); Basiri et al., "Principles of Chaos Engineering", *IEEE Software* 2016 (the chaos table's steady-state hypothesis — every tick produces a `TickResult` with `status` ∈ `{completed, failed}` and exactly one span); rule #2 (vision.md § 2 — every dep behind interface; the SDK is behind the seam); rule #4 (vision.md § 4 — `@otel tick-loop.<verb>` JSDoc on every exported function); rule #6 (vision.md § 6 — exactly one `// rule-6: handled-locally — …` opt-out at the mock-client rejection boundary); rule #7 (vision.md § 7 — failure-mode discipline; the README's chaos table has 5 rows, exceeding the brief's ≥3 requirement); rule #9 (vision.md § 9 — pre-registered pivot at `EXPERIMENT.yaml`: "if the 4-task budget can't fit, drop to single-task smoke + multi-task on self-hosted (sub-task 3)"); user-stories/001-coverage-manifest.test.ts (row 63 — sub-task 1/3, the inventory test the smoke complements); TASKS.md `first-integration-test-mock-tick-loop` (the parent task whose Acceptance this row closes) | full | **In-process only.** No child processes; no real Anthropic SDK; no network. The 4-task synthetic fixture (`synthetic-tasks.md`) follows the canonical `**ID**: <kebab-id>` block-marker shape used in the real TASKS.md (and parsed by `scripts/check-rule-7-chaos-coverage.mjs`'s `parseTaskIds`). 11 paired tests in `novel/tick-loop/src/index.test.ts` cover: single tick succeeds (status: `completed`), 5xx maps to `failed`, span-per-tick (in-memory `SpanRecorder`), client-rejection mapped to `failed` without throwing (lease-expiry), malformed-output returns `success` status with garbage payload (downstream-MUST-validate), 4-task smoke runs within budget, span-per-tick across smoke run with one distinct task ID per span, budget-exhaustion halts the loop with `budgetExhausted: true` and partial results, `maxTicks` cap below `taskIds.length`, fixture parser extracts every `**ID**` marker, parser returns `[]` on malformed source. Cognitive complexity ≤10 enforced by biome (no function exceeds 8). All exported functions/methods carry `@otel tick-loop.<verb>` JSDoc per rule #4; helper / type-guard / test-fake / span-recorder methods are `@otel-exempt` with reason. Try/catch depth ≤1 per rule #6 with one `// rule-6: handled-locally — mock-client rejection is the supervisor boundary …` opt-out. Used by sub-task 3 (`first-integration-test-nightly-self-hosted`, dormant in TASKS.md) as the *unit* the self-hosted runner runs at full 60-min cadence; this v0 ships the 10-min in-process smoke version. Default budget 600_000 ms (10 min), default `maxTicks` 4 (one per task type the parent's P2-task-throughput Acceptance exercises). |
| 63 | `user-stories/001-coverage-manifest.test.ts` (chaos-coverage manifest test — sub-task 1/3 decomposition of `first-integration-test`; maps each of user-story 001's 12 chaos-table rows to either an existing repo test (`covered`), a deferred-to-self-hosted-runner row (`self-hosted` — sub-task 3), or a cross-repo deferred (`deferred` — tasks-mcp lease)) | Chaos-coverage manifest as a steady-state-precondition gate — Basiri et al., *IEEE Software* 2016 (Principles of Chaos Engineering: coverage of declared failure modes is the precondition for fault-injection itself); paired-table inventory with a thin file-existence + grep contract over each cited test; the manifest is a pure constant + four assertion blocks (length, file-grep, ratio, TASKS.md cross-reference), with Beck 1999 Ch. 17's "CI as the constraint enforcer" guaranteeing the manifest's existence at every PR | Basiri, Blohowiak, Hosseini, et al., "Principles of Chaos Engineering", *IEEE Software* 33 (3), 2016 (steady-state hypothesis + coverage as the precondition for fault-injection); Beck, K., *Extreme Programming Explained*, Addison-Wesley, 1999, Ch. 17 (CI as the constraint enforcer); rule #7 (vision.md § 7 — failure-mode discipline); rule #10 (vision.md § 10 — deterministic enforcement; the manifest is a vitest assertion suite, not prose); Munafò et al., *Nature Human Behaviour* 1, 0021, 2017 (rule-#9 pre-registration — the parent `first-integration-test` task's documented Pivot ("reframe as a pair: 10-min smoke in CI + nightly self-hosted") is what justifies counting `self-hosted` toward the ≥80 % Acceptance) | full | The manifest is a `readonly CoverageEntry[]` of length 12 — one entry per row of `user-stories/001-loop-runs-overnight.md`'s "Failure modes & chaos verification" table. Status enum: `covered` (existing repo test — `file` + `testNamePattern` required), `self-hosted` (deferred to nightly self-hosted runner — `selfHostedTaskId` required, must resolve to a `**ID**: …` block in TASKS.md), `deferred` (cross-repo or otherwise out-of-scope — `deferredReason` required). Four assertions: (1) manifest length = 12 with rows 1..12 present; (2) every `covered` entry's `file` exists on disk and contains a test matching `testNamePattern` (case-insensitive grep); (3) `covered + self-hosted` ≥ 0.80 of total — the parent task's Pivot path counts both; (4) every `self-hosted` entry's `selfHostedTaskId` resolves to a `**ID**: <id>` line in TASKS.md (using the canonical block-marker shape so prose mentions don't false-match). Current breakdown: 4 covered (rows 1, 3, 4, 9 — `distribution/test-supervisor.sh`, `novel/handoff-spec/src/index.test.ts`, `novel/adapters/token-monitor/src/maciek.test.ts`), 7 self-hosted (rows 2, 5, 6, 7, 8, 11, 12 — OS-level fault injection: libfaketime, iptables, tc qdisc, dd, pmset), 1 deferred (row 10 — tasks-mcp lease semantics in a third-party repo). Coverage ratio = (4 + 7) / 12 = 0.917, comfortably above the brief's 0.80 threshold. The test runs in <1 s on any CI runner — it is an inventory test, not an OS-level chaos test. Sibling sub-tasks (`first-integration-test-mock-tick-loop`, `first-integration-test-nightly-self-hosted`) close the remaining decomposition: sub-task 2 builds the mock daemon for the 10-min smoke, sub-task 3 wires the nightly self-hosted-runner workflow that actually executes the 7 OS-level rows. The parent `first-integration-test` block in TASKS.md is now a tracker (P2, blocked-by all three sub-tasks). |
| 58 | `.github/workflows/lighthouse.yml` + `distribution/run-dashboard-web.sh` + `novel/dashboard-web/src/start.ts` (Lighthouse Mobile ≥0.85 CI gate against the dashboard-web SSR server — sub-task 4/4 of `dashboard-web-v0`, closes the parent epic; threshold pivoted from 0.9 → 0.85 on 2026-05-04) | Deterministic CI gate over a runtime performance budget — the duration component of Wilkie's RED Method (2018) operationalised as a pre-merge fast-feedback check (Forsgren-Humble-Kim 2018 — DORA fast feedback), with a thin runner script (`run-dashboard-web.sh` — `exec node` so the supervisor sees the real PID) at the I/O boundary | rule #10 (vision.md § 10 — deterministic enforcement; the parent `dashboard-web-v0`'s prose "Lighthouse Mobile score ≥90 in CI" verification cell now has a machine-readable counterpart); Wilkie, T., "The RED Method: How To Instrument Your Services", *Weave Works blog* / *USENIX SREcon EMEA* 2018 (rate / errors / duration as the service-level lens — the gate enforces the *duration* component); Forsgren, Humble, Kim, *Accelerate*, IT Revolution Press, 2018 (DORA — fast feedback on performance regressions; the pre-merge cadence is the load-bearing property); Beck, *Extreme Programming Explained*, Addison-Wesley, 1999, Ch. 17 (CI as the constraint enforcer); Martin, *Clean Architecture*, Pearson, 2017 (the runner script is the I/O boundary; `createServer` / `render` remain pure); ARCHITECTURE.md § "Versioning & dependency evolution" (Lighthouse pin); Munafò et al., *Nature Human Behaviour* 1, 0021, 2017 (rule-#9 pre-registration — the original 0.9 was the pre-registered threshold; lowering it after observation per the documented Pivot is the correct path, not bias) | partial | **Threshold pivoted 0.9 → 0.85 on 2026-05-04 (rule-#9 Pivot fired).** The workflow runs on `pull_request` and `push: branches: [main]`; pinned `lighthouse@12.4.0`; explicit `--throttling.cpuSlowdownMultiplier=4` matches the Lighthouse Mobile default and pins the throttling envelope so a future upstream default change does not silently move the gate (Risk mitigation per the TASKS.md brief). Assertion is now `jq -e '.categories.performance.score >= 0.85' lighthouse.json` (was `>= 0.9` in PR #66) — `jq -e` exits non-zero on `false`/`null` so the comparison is the gate. The Lighthouse JSON is uploaded as a workflow artifact via `actions/upload-artifact@v4` (`if: always()` survives the assertion's non-zero exit) so operators can inspect `.audits` on a regression. The runner script `exec`s into node so the OS supervisor / Lighthouse harness sees the node PID directly — a SIGTERM reaches it without a shell-wrapper detour. **Pivot deviation (2026-05-04):** the original 0.9 threshold proved flaky on GH-hosted runners — observed 3 runs at 0.91 (PR #66 first run, passed), 0.83 (PR #73 first run, failed), 0.89 (PR #73 re-run, failed) = 2 fails / 3 runs (67 %), well above the documented `≥1 false-positive / 10 runs sustained over 30 days` pivot threshold. Per the original task's documented Pivot ("drop the threshold to 0.85 and document the deviation in vision.md § Pattern conformance index"), the threshold was lowered to 0.85 in this row's PR; the gate's semantic is preserved (it still catches a real performance regression that drops the score below 0.85). The pivot path itself was the fail-safe — this confirms rule-#9's design works (pre-registered pivot threshold + measurement + acted-on-evidence). The next-tier pivot (move to a self-hosted runner if 0.85 also proves flaky — ≥2 fails / 10 runs at the new threshold over 30 days) is filed as `lighthouse-self-hosted-runner-pivot` (P3 in TASKS.md), mirroring `supervisor-integration-self-hosted-runner`'s precedent. Conformance is `partial` (not `full`) precisely because the score threshold has moved off the prose anchor in the parent `dashboard-web-v0` task ("≥90 in CI") — the score is now `≥85` in CI, and the deviation is intentional and documented here. The workflow is intentionally separate from `ci.yml` — Chromium installation is materially heavier than the rest of the deterministic linters, and isolating it lets the cache strategy diverge if Chromium proves slow. The `OTEL_*` pass-through in the runner script opens the seam for `dashboard-web-otel-wiring` (P3) to wire real backend reads at start-time without touching this script. |

When you add a new top-level artifact (file, package, interface, named architectural decision, named process step), append a row here in the same commit. When a deviation evolves, edit the row's notes column. When an artifact is removed, remove its row.

## Cost schedule

Per-rule cost weights consumed by `@minsky/mape-k-loop`'s `analyze` phase (Goldratt 1984 Theory of Constraints — `topConstraint = max(violationCount × cost)`). Sourced from this section by `parseCostSchedule(visionMdContent)` (`novel/mape-k-loop/src/cost-schedule.ts`); the CLI wrapper around `tick(...)` reads `vision.md` once at startup and threads the result through `analyze({ snapshot, costs })`. Rules absent from this table fall back to `DEFAULT_RULE_COST = 1` (rule #7 graceful-degrade). v0 defaults; revisit at the next quarterly review (`review-q3-2026`). Anchors: rule #4 (vision.md § 4 — every constant in source); Goldratt, *The Goal*, North River Press, 1984.

| Rule ID   | Cost weight | Rationale                                                        |
|-----------|-------------|------------------------------------------------------------------|
| rule-9    | 100         | Iron rule; pre-registration violation invalidates the experiment |
| rule-7    | 50          | Chaos failures = unobserved blast radius                         |
| rule-typo | 1           | Cosmetic only; high frequency, low semantic cost                 |

## Theoretical foundations

Minsky stands on the shoulders of named giants. Each layer in the architecture maps to a tested pattern with literature behind it.

**Marvin Minsky — Society of Mind.** Intelligence is not a unified mind; it's a society of small specialists, none intelligent alone. Maps to: the persona/agent layer (provided by OMC).

**Stafford Beer — Viable System Model (VSM).** Five recursive levels at increasing timescales: Operations, Coordination, Control, Intelligence, Identity. Maps to: the project's vertical layering.

**Carl Hewitt — Actor Model.** Independent actors that communicate only via messages, never shared state. Maps to: persona-to-persona handoffs through the shared blackboard.

**Joe Armstrong — OTP Supervision Trees.** Let processes die; supervisors restart them with policy. The pattern that produced decade-uptime telecom systems. Maps to: how Minsky recovers from any failure.

**Jeffrey Kephart & David Chess — MAPE-K Loop / Autonomic Computing** (IBM, 2003). Monitor → Analyze → Plan → Execute, sharing a Knowledge base. The reference architecture for self-adaptive software systems. Maps to: the MAPE-K loop (informal label: the *autonomic manager*).

**John Boyd — OODA Loop.** Observe, Orient, Decide, Act. Faster *correct* OODA wins. Maps to: the per-tick decision cycle inside each persona (a tactical inner loop, distinct from the cross-tick MAPE-K outer loop above).

**Walter Shewhart / W. Edwards Deming — PDCA Cycle.** Plan, Do, Check, Act. The basis of kaizen and Lean. Maps to: per-task structure.

**Eliyahu Goldratt — Theory of Constraints.** At any moment one thing is the bottleneck. Improving anything else is waste. Maps to: the autonomic manager's attention discipline.

**Michael Bratman — Belief-Desire-Intention (BDI).** The standard rational agent architecture: Beliefs (what you know), Desires (goals), Intentions (committed plan). Maps to: persona file structure.

**Barbara Hayes-Roth — Blackboard Architecture.** Specialists write partial solutions to a shared workspace; a controller decides who acts next. Maps to: tasks.md + handoffs/ folder.

**Google SRE — Error Budgets.** Quantify acceptable failure as a budget you spend. Maps to: token budget = error budget.

**Maturana & Varela — Autopoiesis.** Self-creating systems whose components participate in producing the network that produces them. Maps to: the long-term ambition — the autonomic manager maintains the personas that maintain the codebase that produces value.

**Douglas Hofstadter — Strange Loops.** Self-referential systems that gain capabilities by observing themselves. Maps to: the autonomic manager observing its own performance.

**Omar Khattab — DSPy / "programming, not prompting".** Treat prompts as code; optimize them with metrics as reward signal. Maps to: how the autonomic manager improves persona prompts over time.

**Klaus Havelund & Allen Goldberg — Runtime Verification.** Monitor an executing system's traces against a formal specification; flag violations as they occur. Maps to: runtime specification monitoring — the project specification lives in `vision.md`; per rule #10 the *load-bearing* monitors are the deterministic linters at `scripts/check-rule-{1..7}-*.mjs` + `scripts/check-pattern-index.mjs` + `scripts/check-pr-self-grade.mjs` (each a required CI gate), with the residual judgement share carved out as the advisory-only `novel/spec-monitor/` Skill (row 35) — never gates CI.

**Mark Weiser — Calm Technology / Ambient Display** and **Stuart Card / Jock Mackinlay — Glanceable Information Display.** Information should be available at the periphery; the user pulls focus only when something demands it. Maps to: the wrist surface (Apple Watch glance widget) — three numbers, no chrome.

**Brendan Gregg — USE Method** (Utilization, Saturation, Errors, for resources) and **Tom Wilkie — RED Method** (Rate, Errors, Duration, for services). Maps to: dashboard methodology.

These are organs of one body, not a checklist. They cohere.

## Glossary — every term has a CS anchor

This table operationalizes constitutional principle 5. Every word Minsky introduces — whether a metaphor for narrative ease or a technical label — points back to a published computer-science / control-theory / formal-methods source. **Code, package names, file paths, test names, and CLI flags use the precise term in the right column.** Prose may use the metaphor in the left column, provided the precise term has been introduced once on the same page.

Default discipline: **use the right-column term directly**. The left-column metaphor is permitted only when (a) it's a primary literature term in its own right (e.g., "society of minds" — Minsky 1986; "strange loop" — Hofstadter 1979) or (b) the metaphor adds narrative weight that the precise term loses (currently: "constitution" for the project-spec-as-authority framing). Every coined word that has a precise alternative in the right column is **retired** for new prose unless the row notes otherwise — see "Retired terms" below the table.

| Term in use | Precise / canonical anchor | Source |
|---|---|---|
| MAPE-K loop / autonomic manager | autonomic computing reference architecture | Kephart & Chess, "The Vision of Autonomic Computing", *IEEE Computer* 2003 |
| Manager agent | manager / dispatcher role in multi-agent systems | Wooldridge, *An Introduction to MultiAgent Systems* (2009); CrewAI's own term |
| Constitution / constitutional rule N | behavioral specification / invariants | Lamport, "Specifying Concurrent Program Modules", *TOPLAS* 1983 |
| Specification drift (system) / concept drift (data) | standard ML / runtime-verification term | Widmer & Kubat, "Learning in the Presence of Concept Drift", *Machine Learning* 1996 |
| Glanceable / ambient display ("Watch surface" remains as the *specific affordance* name) | calm-technology display | Card & Mackinlay 1999; Weiser & Brown, "Calm Technology" 1995 |
| Scheduler iteration ("tick" is acceptable shorthand — already a real-time-systems term) | control-loop period | Liu, *Real-Time Systems* (2000) |
| Lease ("claim" remains in the tasks.md spec as the verb; the underlying mechanism is a lease) | mutual exclusion with timeout | Gray & Cheriton, "Leases", *SOSP* 1989 |
| Handoff (industry-standard in agentic systems; Anthropic Agent Teams uses it explicitly) | actor message-passing with continuation | Hewitt, Bishop, Steiger 1973 |
| Specialist agent / persona | role-based agent | Wooldridge 2009 |
| Society of minds | (primary literature term) | Marvin Minsky, *The Society of Mind* (1986) |
| Strange loop | (primary literature term) | Hofstadter, *Gödel, Escher, Bach* (1979) |
| Watchdog (in `claude-budget-guard`) | (already a precise CS term) | Watchdog timer, hardware / OS literature |
| Inner loop / outer loop | (already precise) | Optimization & control systems |
| Bottleneck | (already precise) | Goldratt TOC (cited above) |
| Error budget | (already precise) | Beyer et al., *Site Reliability Engineering* (Google, 2016) |
| Failure-mode response: `loud-crash-supervisor-restart` / `circuit-break-and-notify` / `graceful-degrade` | (labels for established patterns) | Armstrong, *Programming Erlang* 2007 (let-it-crash); Nygard, *Release It!* 2007 (circuit breaker); AWS Well-Architected, Reliability pillar (graceful degradation) |
| Fault axis | (chaos-engineering term) | Basiri et al., "Principles of Chaos Engineering", *IEEE Software* 2016 |
| Steady-state hypothesis | (chaos-engineering term) | Basiri et al. 2016 (above) |
| Blast radius | (established term) | AWS Well-Architected, Reliability pillar; Beyer et al., *SRE* Ch. 17 2016 |
| Operator escape hatch / kill switch | (established term) | Beyer et al., *SRE* Ch. 17 2016 |

### Retired terms (do not use in new prose)

These coined terms appeared in earlier drafts. Use the canonical term in the right column instead. New PRs that re-introduce a retired term are flagged in review.

| Retired | Use instead |
|---|---|
| CTO loop / CTO meta-loop | MAPE-K loop (and *autonomic manager* for the component) |
| TPM (technical-program-manager agent) | manager agent |
| Constitutional review | runtime specification monitoring — deterministic share = `scripts/check-rule-{1..7}-*.mjs` + `scripts/check-pattern-index.mjs` + `scripts/check-pr-self-grade.mjs`; advisory share = `novel/spec-monitor/` Skill (advisory-only, never gates CI) |
| Drift (standalone) | specification drift (system behavior) or concept drift (data) |

When you introduce a new word in any Minsky doc, **add a row to the in-use table in the same commit** — or, preferably, remove the new word and use an existing row's right-column term.

## What Minsky is not

- Not an IDE plugin
- Not a multi-agent framework — use OMC
- Not a task queue — use tasks.md
- Not a CLI dashboard — use claude-dashboard
- Not a token monitor — use Claude-Code-Usage-Monitor
- Not a mobile app — use Tailscale + claude-code-monitor + ntfy + Apple Shortcuts
- Not a session tool — sessions are an implementation detail; viability is the point
- Not a productivity tool — productivity tools die when you stop maintaining them; viable systems maintain themselves
- Not a vehicle for quick small fixes. Minsky is a long-tail investment in a cybernetic system that runs for years; every change carries the full pre-registered hypothesis-driven contract per rule #9 (hypothesis, success, pivot, measurement, anchor). One-off bugfixes that don't justify that overhead probably belong in another repo. If a change is genuinely too small for HDD discipline, it is too small for Minsky.

## Success criteria

These metrics are tracked on the dashboard from day one. Each has a corresponding integration test in `user-stories/`. Targets are starting points; the MAPE-K loop adjusts them based on observed reality.

Per constitutional rule #9, every row carries a **success threshold**, a **pivot threshold** (below which the *approach* is reconsidered), and a **measurement method** that is an exact runnable command — reproducible by an outside observer with no manual steps. Where the prerequisite system isn't built yet, the command names the future query and tags it `<TBD-AFTER: <task-id>>` linking to the task that lands it.

| # | Metric | Success threshold | Pivot threshold | Measurement method (automated) | Literature anchor |
|---|--------|-------------------|-----------------|-------------------------------|-------------------|
| 1 | Loop uptime, 30 / 90 / 365 d | 99 % / 97 % / 95 % | <90 % over 30 d → reconsider supervisor design | `systemctl --user is-active minsky-tick-loop && journalctl --user -u minsky-tick-loop --since="30 days ago" -o json \| node scripts/uptime.mjs` ⟨TBD-AFTER: supervisor-setup⟩ | Beyer et al., *SRE* 2016, Ch. 4 (SLI / SLO) |
| 2 | Tokens per closed user-story | Decreasing trend month-over-month (≥5 % MoM) | Flat or rising for 3 consecutive months → MAPE-K loop isn't helping; pivot the autonomic manager | OTEL trace query: `sum(token_count{event="user_story.complete"}[30d]) / count(span{name="user_story.complete"}[30d])` ⟨TBD-AFTER: observability-adapter-v0⟩ | Goldratt TOC (improving the constraint should move this metric) |
| 3 | Specification alignment (deterministic-linter green ratio over 30 d) | ≥95 % of CI runs on `main` are green across the rule-#10 lint set | <85 % over 7 d → spec is wrong OR system is misaligned; trigger spec audit | `gh run list --workflow ci.yml --branch main --status completed --created ">=$(date -v-30d +%Y-%m-%d)" --limit 1000 --json conclusion --jq '([.[] \| select(.conclusion=="success")] \| length) / (length \| if . == 0 then 1 else . end)'` ⟨TBD-AFTER: rule-#10 lint set finalised⟩ | Havelund & Goldberg, "Verify Your Runs", *VSTTE* 2008 (operationalised per rule #10 — the deterministic linters at rows 32–37 are the load-bearing monitors; advisory-only `novel/spec-monitor/` does not feed this metric) |
| 4 | Self-improvement velocity | ≥4 prompt rollouts / month with sustained gain (≥10 %, p < 0.05, 7 d post-rollout) after Q1 | <2 / month sustained 3 months → MAPE-K design or DSPy choice is wrong; pivot | `git log --grep='mape-k rollout' constraints.md --since="30 days ago" \| wc -l` plus DSPy A/B export ⟨TBD-AFTER: mape-k-loop-v0⟩ | Khattab DSPy 2023; Kohavi *Trustworthy* 2020 (statistical rigour) |
| 5 | Mean time to recovery (MTTR) | <5 min p95 from process death to next claim | p95 >10 min sustained 7 d → supervisor backoff or claim-resume is wrong | OTEL: `histogram_quantile(0.95, supervisor_restart_to_claim_latency_seconds[7d])` ⟨TBD-AFTER: observability-adapter-v0⟩ | Forsgren et al., *Accelerate* 2018 (DORA MTTR) |
| 6 | Wrist dwell (inverted) | ≤60 s / day | >120 s / day for 14 d → surface is too informative or system is too unhealthy; redesign | `count(http_get_total{path="/watch.json"}[1d]) * estimated_dwell_seconds_per_request` (constant ≈ 2 s) ⟨TBD-AFTER: dashboard-web-v0⟩ | Card & Mackinlay 1999; Weiser & Brown 1995 (calm tech: dwell as friction) |
| 7 | Extraction count | ≥4 OSS repos extracted by month 6 | <2 by month 4 → re-evaluate extraction policy / scope | `gh repo list fyodoriv --json name,createdAt,description --jq '[.[] \| select(.description \| test("@minsky\|claude-")) ] \| length'` | rule #1 (don't reinvent the wheel) — extraction is the operationalisation |
| 8 | Dependency interface coverage | 100 % of named deps behind adapter | ≥1 unhidden dep persisting >1 sprint → fix with adapter wrap or task | `node scripts/check-rule-2-dep-coverage.mjs` (greps `novel/**/*.ts` excluding `novel/adapters/**` for vendor imports listed in ARCHITECTURE.md dependency table; exit 1 on any hit) | rule #2 (every dep behind interface) |
| 9 | Token-budget honoring | 0 hard 429 / week sustained 30 d | ≥1 / week sustained 4 weeks → budget-guard logic is broken; pivot | OTEL counter: `sum(rate(claude_code_api_errors_total{status="429"}[7d]))` ⟨TBD-AFTER: observability-adapter-v0 + budget-guard-v0⟩ | Beyer SRE 2016 (error budget) |
| 10 | Task throughput | Sustained tasks / day at observed budget (≥1 / day at green budget) | <1 / day for 14 d at green budget → bottleneck elsewhere; analyse via TOC | `git log --since="30 days ago" --oneline --grep='^feat\|^fix\|^docs\|^chore' \| wc -l` divided by 30 (close-task removal commits) | Goldratt TOC (throughput as the goal of any system) |

Tooling note: GrowthBook (open-source feature flags + A/B testing, MIT) is the planned `Experiment` adapter for system-level rollouts behind these metrics; see `research.md` § "Hypothesis-driven development tooling".

## License & openness

MIT throughout. Every novel layer extracted from day one as its own MIT repo with clean interface, documentation, and tests sufficient for others to adopt without depending on Minsky proper. Configuration, persona prompts, and the integration spec are open.

Closed parts: nothing.

## North star

> A solo developer, on a $200/month subscription, sleeps soundly while a society of specialists builds their software, supervises itself, repairs itself, improves itself, and pings the developer's wrist only when something genuinely matters. The system stays alive and useful for the developer's career. When better tools appear, Minsky absorbs them and replaces its own pieces. The integration outlives any single tool in the stack.

## Reading next

- `ARCHITECTURE.md` — how the pieces wire together; the adapter pattern; the dependency table; data flow; supervision tree
- `AGENTS.md` (forthcoming) — how any agent should behave when working in this repo
- `TASKS.md` (forthcoming) — current work queue, eats own dog food
- `research.md` (forthcoming) — living scan of replaceable parts and incoming candidates
- `competitors/` (forthcoming) — one doc per competitor with extraction notes
- `user-stories/` (forthcoming) — one file per story with metric, integration test, proof
