# Fixture demonstrating a hypothesis that violates a *deterministic*
# rule — specifically rule #9's vanity-metric anti-pattern, caught by
# `@minsky/experiment-record`'s parser at PR-time (see
# `novel/experiment-record/src/parse.ts` const `VANITY_PHRASES`).
#
# EXPECTED SKILL BEHAVIOUR: SILENT.
# The deterministic linter is the authority. The Skill must produce
# zero advisory entries against this fixture — emitting an A1 ("vague")
# advisory here would duplicate the deterministic verdict and confuse
# the rule-#10 ratchet ("never run both").
#
# This fixture exists precisely to exercise that contract: if the Skill
# starts emitting advice on this fixture, scope has drifted into
# deterministic territory and either the Skill's prompt or one of A1–A5
# needs to be tightened.
id: vanity-metric-example
hypothesis: |
  Adopting the new lint catalogue increases lines of code by ≥5 % and
  the number of commits made per week by ≥10 %, demonstrating that
  the team is making progress on the autonomous-loop initiative.
success: "lines of code up ≥5 %"
pivot: "lines of code flat for 3 weeks → wrong approach"
measurement: "git ls-files | xargs wc -l | tail -1"
anchor: "rule #9 (vision.md § 9 — but note: vanity-metric phrasing here is the violation)"
