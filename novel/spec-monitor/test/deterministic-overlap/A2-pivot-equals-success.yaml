# Fixture relocated from judgement-only/ → deterministic-overlap/ when
# spec-monitor advisory rule A2 was promoted to a deterministic CI lint
# at `scripts/check-pivot-success-margin.mjs` (rule-#10 ratchet, recorded
# in spec-advisories/2026-05-03-quarterly-audit.md). The Skill should NOT
# emit any advisory entry against this record — the deterministic linter
# is now the authority. The linter, fed this record, fails with
# "pivot/success margin too small" because the leading numeric token is
# 95 on both sides.
id: zero-margin-pivot-example
hypothesis: |
  Adapter availability stays at-or-above 95 % over rolling 30-day
  windows once the supervisor's restart-with-backoff strategy lands.
success: ">= 95 % uptime over 30 d"
pivot: "< 95 % uptime over 30 d"
measurement: "node scripts/uptime.mjs --window=30d"
anchor: "Beyer et al., *SRE* 2016, Ch. 4 (SLI / SLO)"
