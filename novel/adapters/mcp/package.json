{
  "name": "@minsky/mcp",
  "version": "0.0.0",
  "private": true,
  "description": "MCP adapter — interface (Adapter pattern, Gamma 1994) + StubMCP test fake + MCPOpenHands scaffold (real @modelcontextprotocol/sdk bridge pending 2026-06-01 OpenHands runtime).",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": [
    "dist/**/*.js",
    "dist/**/*.d.ts",
    "!dist/**/*.test.js",
    "!dist/**/*.test.d.ts",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run --root .",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@minsky/adapter-types": "workspace:*"
  }
}
