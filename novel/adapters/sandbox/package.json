{
  "name": "@minsky/sandbox",
  "version": "0.0.0",
  "private": true,
  "description": "Sandbox adapter — interface (Adapter + Strategy, Gamma 1994) separating sandbox shape (process/docker/remote-vm) from the agent loop + StubSandbox test fake + ProcessSandboxAdapter default (today's host-process behavior). Docker/Remote-VM Strategies are M4 follow-ups.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": [
    "dist/**/*.js",
    "dist/**/*.d.ts",
    "!dist/**/*.test.js",
    "!dist/**/*.test.d.ts",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run --root .",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@minsky/adapter-types": "workspace:*"
  }
}
