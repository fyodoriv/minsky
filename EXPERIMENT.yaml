id: dashboard-web-task-throughput-formula-drift
hypothesis: |
  `vision.md` § "Success criteria" row 10 (Task throughput) specifies the
  measurement command as `git log --since="30 days ago" --oneline
  --grep='^feat\|^fix\|^docs\|^chore' | wc -l` divided by 30, with unit
  `tasks/day`. `novel/dashboard-web/src/metrics.ts`'s `task-throughput`
  entry previously omitted the `/ 30` divisor while keeping the
  `tasks/day` unit — a silent 30× over-read once `dashboard-web-otel-wiring`'s
  Strategy executes the formula. Aligning the formula string with the
  spec (appending `/ 30`) prevents the over-read and restores rule-#4 /
  rule-#8 spec-alignment. The renderer displays `(stub)` today, so the
  hypothesis is observable structurally (string-match against the spec
  row) rather than via a runtime number, until OTEL wiring lands.
success: "`grep -F '/ 30' novel/dashboard-web/src/metrics.ts` matches the
  `task-throughput` formula AND `pnpm vitest run
  novel/dashboard-web/test/metrics.test.ts --reporter=json | jq -e
  '.numPassedTests >= 4 and .numFailedTests == 0'` exits 0 (the new
  `task-throughput formula divides 30-day count by 30` test passes
  alongside the three existing invariants); the
  `dashboard-web-task-throughput-formula-drift` task block is removed
  from `TASKS.md` (`grep -c '^  - \\*\\*ID\\*\\*:
  dashboard-web-task-throughput-formula-drift$' TASKS.md` returns 0)."
pivot: "If `vision.md` row 10 owners later decide the divisor should be
  configurable (e.g., a future `/ N` parameter to support 7-day or
  90-day lookback windows), refactor `SuccessMetric` to carry the
  divisor as a separate numeric property rather than embedded in the
  `formula` string; the divisor becomes data not string-substring, and
  the renderer multiplies/divides explicitly. Quantitative trigger:
  ≥1 row in vision.md § 'Success criteria' adopting a non-30 divisor
  within the next 90 days."
measurement: "grep -F '/ 30' novel/dashboard-web/src/metrics.ts | grep -q
  task-throughput || grep -A2 'id: \"task-throughput\"'
  novel/dashboard-web/src/metrics.ts | grep -q '/ 30'; pnpm vitest run
  novel/dashboard-web/test/metrics.test.ts --reporter=json | jq -e
  '.numPassedTests >= 4 and .numFailedTests == 0'; [
  \"$(grep -c '^  - \\*\\*ID\\*\\*:
  dashboard-web-task-throughput-formula-drift$' TASKS.md)\" = '0' ]"
anchor: "rule #4 (vision.md § 4 — every constant in source; the formula
  string is exactly such a constant and must match the spec row it
  renders); rule #8 (vision.md § 8 — pattern conformance; the spec is
  the source of truth and code conforms to it, never the inverse);
  Aho-Sethi-Ullman, *Compilers*, 1986 (round-trip property — the
  formula is round-trippable to the spec when the two strings agree)."
replay_windows_days: [7, 30]
