# METRICS.md — canonical observability surface

_Generated by `node scripts/generate-metrics-md.mjs`. Each entry is either a fresh observation (within its `freshnessBudgetMs`) or an explicit `(stub)`. Never a silent zero — wrong data is worse than no data (Ries 2011)._

## loop-uptime — Loop uptime, 30 / 90 / 365 d

_Budget: 7d_

**Value:** (stub) — no observation captured yet (wired in canonical-metric-list-per-repo follow-up)

Formula: `systemctl --user is-active minsky-tick-loop && journalctl --user -u minsky-tick-loop --since="30 days ago" -o json | node scripts/uptime.mjs`

## tokens-per-story — Tokens per closed user-story

_Budget: 7d_

**Value:** (stub) — no observation captured yet (wired in canonical-metric-list-per-repo follow-up)

Formula: `sum(token_count{event="user_story.complete"}[30d]) / count(span{name="user_story.complete"}[30d])`

## spec-alignment — Specification alignment (deterministic-linter green ratio)

_Budget: 7d_

**Value:** (stub) — no observation captured yet (wired in canonical-metric-list-per-repo follow-up)

Formula: `gh run list --workflow ci.yml --branch main --status completed --created ">=$(date -v-30d +%Y-%m-%d)" --limit 1000 --json conclusion --jq '([.[] | select(.conclusion=="success")] | length) / (length | if . == 0 then 1 else . end)'`

## self-improvement-velocity — Self-improvement velocity

_Budget: 7d_

**Value:** (stub) — no observation captured yet (wired in canonical-metric-list-per-repo follow-up)

Formula: `git log --grep='mape-k rollout' constraints.md --since="30 days ago" | wc -l`

## mttr — Mean time to recovery (MTTR)

_Budget: 1d_

**Value:** (stub) — no observation captured yet (wired in canonical-metric-list-per-repo follow-up)

Formula: `histogram_quantile(0.95, supervisor_restart_to_claim_latency_seconds[7d])`

## wrist-dwell — Wrist dwell (inverted)

_Budget: 1d_

**Value:** (stub) — no observation captured yet (wired in canonical-metric-list-per-repo follow-up)

Formula: `count(http_get_total{path="/watch.json"}[1d]) * estimated_dwell_seconds_per_request`

## extraction-count — Extraction count

_Budget: 30d · _monotonic: ok__

**Value:** (stub) — no observation captured yet (wired in canonical-metric-list-per-repo follow-up)

Formula: `gh repo list fyodoriv --json name,createdAt,description --jq '[.[] | select(.description | test("@minsky|claude-")) ] | length'`

## dep-interface-coverage — Dependency interface coverage

_Budget: 1d_

**Value:** (stub) — no observation captured yet (wired in canonical-metric-list-per-repo follow-up)

Formula: `node scripts/check-rule-2-dep-coverage.mjs`

## token-budget-honoring — Token-budget honoring

_Budget: 1d_

**Value:** (stub) — no observation captured yet (wired in canonical-metric-list-per-repo follow-up)

Formula: `sum(rate(claude_code_api_errors_total{status="429"}[7d]))`

## task-throughput — Task throughput

_Budget: 7d_

**Value:** (stub) — no observation captured yet (wired in canonical-metric-list-per-repo follow-up)

Formula: `git log --since="30 days ago" --oneline --grep='^feat\|^fix\|^docs\|^chore' | wc -l / 30`
